---
description: удвоенный обратный слэш в команде оболочки приезжает одинарным — код правь файлом-скриптом, не строкой команды
---

Правка через `sed -e '...'` или `node -e "..."` с регуляркой портит обратные слэши: `[\/]`
доезжает до файла как `[\/]`, то есть класс символов молча теряет один из двух. Заметно это
не сразу — код остаётся валидным, просто проверяет не то.

Надёжный путь для правок кода: положить старый и новый кусок в файлы (heredoc с `'EOF'`
кириллицу и UTF-8 держит верно) и заменить их скриптом, который читает эти файлы с диска
и требует ровно одного совпадения. Тогда в командную строку не попадает ни одного спецсимвола.

Если обратный слэш нужен внутри самого скрипта — собирать его из `String.fromCharCode(92)`.

Наблюдение от 2026-09-20; про порчу кириллицы в **выводе** оболочки есть отдельное —
[`кириллица-в-выводе-оболочки-ломается.md`](кириллица-в-выводе-оболочки-ломается.md).
