# Память агентов

Индекс: строка на файл, ничего кроме. Сам факт живёт в файле, сюда попадает только то, по
чему видно, стоит ли его открывать.

Формат строки — область первым словом, дальше суть и ссылка:

```
- карта: состояние прогона пишет сервер, `.state.json` руками не трогают — [файл](состояние-прогона.md)
```

Области — по объектам карты и по крупным темам: `карта`, `core`, `abstract-server`,
`расширение`, `требования`. Папками не раскладываем: индекс дёшев, отбирает читающий.

Файл наблюдения — frontmatter с `description` и текст:

```markdown
---
description: одной строкой, по ней решают, открывать ли
---

Само наблюдение. Дата, если оно может протухнуть.
```

Что сюда кладём и что нет — [`../docs/development-guide/memories.md`](../docs/development-guide/memories.md).
Коротко: не общеизвестное и не выводимое из структуры репозитория и карты. Решения — не сюда,
они заводятся директивой.

## Наблюдения

- карта: живой MCP и вебвью обслуживают `dist` открытого расширения (а если оно установлено — `dist` из `~/.vscode/extensions`, куда `pnpm build` не доезжает вообще) — новый параметр молча проглотится, правка в UI не покажется; проверяй `readMap`, своим `mapward mcp` по stdio или `grep` по `dist/webview.mjs` — [файл](живой-mcp-это-собранная-сборка.md)
- расширение: «вечно грузится и не загружается» — активация упала на внешнем `require`, которого отладочный хост не показывает; смотреть `exthost.log`, проверять `grep require` по `dist/extension.cjs` — [файл](установленное-расширение-падает-на-внешних-require.md)
- расширение: F5 закрывается сразу — рядом грузится установленное `mapward.mapward` с другим id; ошибка в `renderer.log` и `main.log`, а не в `exthost.log` — [файл](f5-с-установленным-расширением-это-два-расширения.md)
- cli: `pnpm check` зовёт несуществующую команду и падает — проверки это test/lint/format:check/typecheck/build — [файл](pnpm-check-зовёт-несуществующую-команду.md)
- карта: текст этапа достаётся `read_index { stage }`, не запуская прогон — с 2026-09-20 — [файл](промпт-этапа-читается-с-диска.md)
- карта: дорогую метрику зови `run_metric { wait: false }` — иначе вызов падает по таймауту, а счёт всё равно идёт — [файл](прогон-дорогой-метрики-переживает-таймаут-вызова.md)
- окружение: кириллица в выводе оболочки ломается — вывод в файл, читать `Read` — [файл](кириллица-в-выводе-оболочки-ломается.md)
- карта: `fields` ведёт путь внутрь значения — `metrics.value.ok` даёт здоровье всех метрик объекта на пару килобайт — [файл](проекция-полей-спускается-внутрь-значения.md)
- карта: обзорный `read_object` на корне раздувается без `fields` — воркфлоу и раскладка ушли к верхнему, вес держат экшоны и `props` — [файл](обзор-карты-без-fields-раздувается.md)
- карта: ответы на вопросы этапа автор дописывает в файл директивы по ходу сессии — перед перезаписью перечитать — [файл](автор-отвечает-в-файл-во-время-сессии.md)
- карта: директива от автора бывает без перевода строки в конце — реплика `>>` приклеивается к его ответу, проверь конец файла — [файл](директива-без-перевода-строки-в-конце.md)
- карта: объекты в `shared-metrics` несут `config.json` вместо `_index.json` — `read_index` по их адресу отдаёт `index: null` — [файл](общие-метрики-лежат-объектом-без-index.md)
- cli: тесты гоняются только с корня — `pnpm --filter <пакет> test` молчит и выходит с нулём — [файл](тесты-гоняются-только-с-корня.md)
- cli: метрику живьём проверяют `node apps/cli/dist/cli.mjs metric <адрес>` после `pnpm build` — [файл](метрику-живьём-проверяют-через-cli.md)
- окружение: обратный слэш в командах оболочки теряется — правки кода делай файлом-скриптом, длинный текст пиши `Write`, а не heredoc — [файл](обратный-слэш-в-командах-оболочки-теряется.md)
- карта: http-MCP путал ответы при одинаковых `id` — починено 2026-09-20, но живой сервер из `dist` до пересборки ещё путает — [файл](http-mcp-путает-ответы-при-одинаковых-id.md)
- доки: дока инструмента лежит в `packages/docs/ru/`, до `read_docs` доезжает через `bundle-docs.mjs` — [файл](дока-инструмента-лежит-в-packages-docs.md)
- карта: `read_index` с `file` отдаёт `status` и `path` директивы, а не только текст — обзорный `read_object` ради статуса не нужен — [файл](read-index-с-file-отдаёт-статус-директивы.md)
- карта: у кого в `transforms` стоит нужный `kind` — MCP не спросить, бюджет режет конфиги; считается grep по `ru/map` — [файл](кто-несёт-шаг-в-transforms-mcp-не-скажет.md)
- карта: подписка отбирает метрики вкладкой и ключами (с 2026-09-21); свёрнутость по-прежнему только прячет — [файл](подписка-идёт-на-объект-целиком.md)
- cli: типы проверяет `pnpm typecheck` через turbo — `tsc` с корня врёт про jsx и пути — [файл](проверки-гоняет-turbo-а-не-tsc-с-корня.md)
- core: фикстура `mcp.test.ts` одна на файл — объект под `packages/` ломает чужие тесты про `children` — [файл](фикстура-mcp-тестов-общая-на-файл.md)
- карта: `bytes` директивы — байты UTF-8, русский текст вдвое короче по знакам; `tail` нужен от ~40 КБ — [файл](bytes-директивы-считает-байты-а-не-знаки.md)
- решения: номер берётся от последнего, `0021` пропущен и не переиспользуется, номер может держать незакрытая директива без файла — grep по `_directives`; перекрытие — цитатой в начале раздела старого — [файл](номер-решения-и-пометки-о-перекрытии.md)
- карта: имя отзыва в `mcp-reviews` даёт только минуту и объект — параллельная сессия затирает чужой файл, свободу имени проверяй `test -e`, а не `ls | tail` — [файл](отзыв-в-mcp-reviews-затирается-параллельной-сессией.md)
- карта: «что просили агенты» лежит только в `mcp-reviews`, 144 КБ — срез снимается `awk` по заголовкам разделов — [файл](срез-по-отзывам-в-mcp-reviews.md)
- карта: сервер падает посреди этапа — `finish_directive` не поставить, `.state.json` не трогаем, дочитываем с диска — [файл](упавший-сервер-оставляет-этап-открытым.md)
- карта: раскладку вкладки MCP не отдаёт — `groups` обрезан до четырёх полей, раскладка сверяется по `_index.json` — [файл](раскладка-вкладки-через-mcp-не-видна.md)
- карта: корень проекта в окружении скрипта-коллектора не приезжает — передавай `${{ mapward://@ }}` аргументом в `run` — [файл](корень-проекта-скрипту-передают-аргументом.md)
- карта: точечный коммит перед прогоном забирает весь индекс — коммить `git commit -- <пути>`, иначе уедет чужое; новый файл сперва `git add` — [файл](точечный-коммит-забирает-весь-индекс.md)
- карта: «не разрешилось: …props.codePath» в `props` прототипа — норма, подстановка ждёт наследника — [файл](не-разрешилось-в-props-прототипа-это-норма.md)
- карта: `metrics: []` вместе с `metrics.*` в `fields` — отказ; перечень ключей берётся вовсе без `metrics` — [файл](пустой-metrics-с-полями-метрик-это-отказ.md)
- сборка: mtime `src` против `dist` врёт — `tsc` кладёт `.d.ts` рядом с исходниками; спрашивай `turbo run build --dry=json` — [файл](сравнение-времён-src-и-dist-врёт.md)
- сборка: `_index.json` карты форматирует `oxfmt` только через stdin — по пути `ru/**` игнорируется, prettier даёт другой стиль — [файл](json-карты-форматирует-oxfmt-через-stdin.md)
- расширение: VS Code ставит в вебвью `scrollbar-color` на `html` — пока не снят, `::-webkit-scrollbar` не работает, полосы со стрелками и чёрной дорожкой — [файл](вебвью-vscode-ставит-scrollbar-color.md)
