# Идеи


## Микро фиксы

- В `0004` про `link` написано «`https://` наружу», а наружу уходит любая схема — поправить фразу

## Простые

0. Улучшения отображения
  1. Компактный режим метрики "Название:значение"
  2. Добавить возможность скролла метрик
  3. Добавить возможность создавать секции метрик с названиями и которые можно скрывать открывать
  4. Метркика без раскладки вообще не рисуется. Подготовка к разным view
1. Сохранять состояние открытости папки с файлами между переходами по докам
2. В mcp есть инструкция по всем метрикам и работе с картой
3. метрика files - вотчер
4. На всех вьюхах метрик есть возможность вывести статус - кайне важная фича для метрики
5. Метрика md текст, для описаний пояснений и пд
6. Проработать базовые промпты!
7. Отобжажать md файл в метрике
8. Вместо фиксированных действий над директивами- конфигурируемый набор этапов
9. Скролл с поиском в селекте директив

- Во время сбора метрики  показывать лоадер а не 3 точки + Возможность, посмотреть логи процесса бы где нибудь
- Добавить кнопку для просмотра файлов метрик

## Сложные

1. Сделать архитектурной карты вывод. 
    Для этого: Система плагинов метрик нужно продумать
    Сделать плагин который: Засмечается классы модулей, дальше на основе классов выводится дерево модулей, и связи между нимы выводятся из ссылок typescript Этакий архитектурный ревьювер. 

2. Зависимости в коллекте
  Список файлов на которые зависим. Можно взять с них хэш и сравнивать

3. Создание mcp для чтения проекта

**WIP** 4. Форовый арекстратор сбора метрик, что бы можно было переходить по карте и их значения кэшировались

5. Рисовать на карте как miro для проектирования и удобства создания базовых объектов, и дальнейшая работа с ними

6. Открытие В отдельных окнах как объекты так и отдельные метрики

7. Проекции карты под разные роли и форматы работы

8. ПОДсветка гита в файлах в дереве

## Архитектурные проблемы

MapFile - на все случаи жизни, нужно разделить на специфичные типы

## Баги

- collect.json прям в прототип записываются
- Поза холста не возвращается: `viewport` читается асинхронно, `Graph` уже смонтирован, а xyflow применяет `defaultViewport` только при инициализации — каждое открытие делает fitView (`children-map.tsx:15`, `graph.tsx:82`)
- У 10 из 18 переменных `--mw-*` нет запасных значений — хост, который их не заполнил, получит чёрное на чёрном (кнопки в `maps/ui/empty.tsx`, узлы графа в `_children-map/ui/graph.tsx`)
- `preview-size`, `preview-metrics-layout` и `preview-style` доезжают до модели, но превью их не рисует


## По самой карте

- Добавить метрику по Проверке архитектуры
- Вынести требования на верхний уровень **Готово**
- ADR добавить для архитектурный решений

## Из отзывов об MCP — отложенное

Тридцать шесть отзывов в `ru/mcp-reviews/` за 2026-09-20. Что чинилось в транспорте, сделано
директивой `2026-09-20-1810-улучшение-mcp` и описано решением `0022`. Здесь — остальное:
это не про транспорт, а про модель карты. MCP отдаёт то, что в модели есть, а этого в ней
нет — каждый пункт тянет своё решение и свою директиву. Число в скобках — сколько отзывов
просили.

- **Тексты решений и требований через MCP** (4). Метрика `decisions` отдаёт имена файлов, и
  этап «Проверка» каждый раз читает два десятка файлов с диска, поимённо и вслепую. Это
  метрика, а не инструмент: отдавать тексты должна та, что их и находит.
- **Обратные ссылки — «кто ссылается на этот файл»** (5). Самая настойчивая из отложенных
  просьб. Правя решение или требование, агент не видит, что на него ссылается, и оставляет
  битые ссылки; проверить их можно только обходом всего дерева.
- **Тронутые директивой файлы и сцепка «директива → коммит»** (4). Сейчас связь между
  прогоном и диффом живёт в голове у человека: какой прогон что наменял, видно только по
  времени. Требует, чтобы карта знала про git, — это открытая директива `git-status`.
- **Связь «метрика ↔ на что она смотрит»** и «что пересобрать после переноса» (2). Перенесли
  папку — какие метрики протухли, никто не скажет; агент пересобирает всё или не пересобирает
  ничего.
- **Поиск по карте — «где упоминается X»** (2). Сейчас это обход всех объектов по одному, и
  на большой карте он не окупается.
- **Признак свежести хоста: `dist` против `src`** (3). Живой MCP отвечает из собранного
  расширения, и правка в `src` до него не доезжает — агент проверяет ветку, которой в ответе
  нет. Чинится сборкой, а не полем в ответе, поэтому и не сделано.
- **Диф директивы с прошлого прогона** (2). Копия текста у карты есть, но диф — это уже не
  чтение; `read_index { tail }` закрывает тот же расход дешевле.
- **Статусы и `object` требований машинно, счётчики по статусам, скрытые `draft`** (3).
  Требования приходят деревом для человека, а спрашивают у него машинные вопросы: сколько в
  каком статусе, что к какому объекту привязано. Метрика.
