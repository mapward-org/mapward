
## 

Сейчас кнопка назад поднимает по дереву, но удобнее было бы если бы она возвращала обратно в тот объъект от которого ты пришёл. 

Видимо тут нужна будет память и стрелки

> [AI] Фаза 1. Сейчас «←» над заголовком — это просто последняя крошка пути, то есть «к родителю».
> Истории переходов нет вовсе: адрес живёт одной переменной, и прыжок по ссылке из метрики или
> по карте детей её затирает. Предлагаю так: «←» и «→» становятся настоящими «назад/вперёд» по
> истории, как в браузере, а к родителю ведут только крошки — они это и так умеют. Шаг истории —
> переход на другой объект; вкладки, мета-экран и метрика во весь экран шагами не считаются.
> История своя у каждого вида (сайдбар, каждый таб) и живёт, пока вид открыт. Так подходит, или
> «к родителю» должно остаться отдельной кнопкой рядом со стрелками?

Да всё верно

> [AI] Фаза 2. История живёт, пока вид открыт, — значит после перезагрузки окна она пустая, и это
> нормально. Но рядом есть дыра хуже: таб не помнит даже, где ты сейчас. При открытии он
> запоминает стартовый объект, а дальнейшие переходы не записывает. Ушёл из таба на три объекта
> в сторону, перезагрузил окно — таб снова на том, с чего открывался. Предлагаю заодно чинить это
> здесь: таб сохраняет текущий объект (история по-прежнему не переживает перезагрузку). Берём в
> эту директиву или заводим отдельную?
Хм, а может всё таки историю заперсистентить 
Ето будет удобно, перезагрузил расширение, и раскладка осталась таже и вернуться даже можно

> [AI] Фаза 2. Хорошо, историю сохраняем вместе с текущим объектом. У таба она живёт, пока таб
> открыт: закрыл его — история ушла вместе с ним. В сайдбаре у каждой карты своя история. Ты
> пишешь «раскладка осталась та же», и тут есть развилка. Кроме объекта, экран держит ещё
> открытую вкладку метрик, мета-экран и метрику во весь экран, и сейчас всё это при перезагрузке
> сбрасывается. Сохранять и их, чтобы после перезагрузки экран вернулся точь-в-точь? Или хватит
> объекта и истории, а вкладки пусть открываются по умолчанию?

Давай всё же в точь в точь, возможно можно бахнуть тут реакт роутер и эти штуки будут параметрами урла типо квери или хэш (понятно дело что эмуляция)

> [AI] Фаза 3. Идея адреса-строки отличная: состояние экрана — это одна строка вида
> `mapward://packages/core?group=тяжёлое&meta`, а история — список таких строк. Только в query,
> а не в хэше: `#` в адресах карты уже занят путём к полю. А вот сам React Router я бы не брал.
> Его история в памяти не отдаёт наружу список шагов, а именно этот список нам и нужно сохранять.
> Ещё в сайдбаре несколько карт живут в одном дереве, и у каждой своя история. Роутеры друг в
> друга не вкладываются, пришлось бы их обходить. Предлагаю свой маленький стек строк рядом с
> хлебными крошками: положить, назад, вперёд, обрезать хвост. Сохраняется он целиком. Или
> роутер тебе нужен ради чего-то дальше, например будущей веб-версии?
Давай свой стек

> [AI] Фаза 3. Остались жесты кроме экранных стрелок. Напрашиваются Alt + ← / → и боковые
> кнопки мыши «назад/вперёд», но с обоими есть неясность. Alt + ← в редакторе уже занята его
> собственной навигацией по файлам. А доходят ли боковые кнопки мыши до вебвью или редактор
> перехватывает их раньше, по коду не видно, это надо пробовать руками на живом расширении.
> Клавиатуры в клиенте сейчас нет вообще, так что любой хоткей будет первым. Предлагаю в этой
> директиве сделать только экранные стрелки, а жесты записать в идеи. Или нужны сразу, и какие?

Не жесты и клавиши не нужны, просто кнопки пока

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — модель истории, без React.
> Где: `packages/abstract-react-client/src/features/map-object/pure-model/` — типы и чистые
> функции фичи объекта; там уже лежат крошки. Новый файл `navigation.ts` и тест к нему.
> - Экран — это объект плюс вкладка, мета-экран и метрика во весь экран. В строку он пишется так:
>   `mapward://packages/core?group=тяжёлое&meta&solo=files`. Разбор обратный, кириллицу
>   экранирует стандартный разбор query.
> - История — список таких строк и номер текущей. Переход на другой объект кладёт новую строку и
>   отрезает всё, что было «вперёд». Смена вкладки, мета-экрана или метрики переписывает текущую
>   строку, а не кладёт новую: шагами они не считаются, но после перезагрузки восстанавливаются.
> - Переход на тот же объект, где ты стоишь, ничего не кладёт — иначе «назад» стоял бы на месте.
> - Длина ограничена: 50 шагов, самые старые уходят. Сохраняется история целиком, и без предела
>   она росла бы вечно.
> - Назад и вперёд перешагивают через адреса, которых на карте больше нет: объект переименовали
>   или удалили, пока таб был открыт. Сейчас мёртвый адрес молча роняет вид на корень карты.

Хм, надо будет потом зарефакторить, у нас гига фича получилась

Но сейчас ок

> [AI] План, кусок 2 — где история хранится. Таб и сайдбар хранят её по-разному, а сам экран
> объекта не знает где: хранилище ему передают снаружи.
> - `features/map-object/compose/` — сборка экрана объекта. Вместо четырёх отдельных состояний
>   (объект, вкладка, мета, метрика) — одна история из куска 1. Снаружи экран получает её
>   начальное значение и функцию «сохрани».
> - `entry/app.tsx` в клиенте — сборка всего. Решает, откуда история берётся. У таба она едет
>   вместе с тем, на чём таб открыт, у сайдбара лежит в хранилище вида по ключу карты. Это то же
>   хранилище, где лежат свёрнутые метрики: своё у каждого человека, в репозиторий не попадает.
> - `apps/vscode-extension/src/apps/webview/main.tsx` — вход вебвью. Таб записывает историю в
>   состояние своей панели при каждом переходе, а не один раз при открытии. Закрыл таб — редактор
>   сам выбросит это состояние, и история уйдёт вместе с табом.
> - Таб, сохранённый до этой правки, истории не несёт. Его история начинается с того, на чём он
>   открыт, как сейчас.
> - Хранилище вида отвечает не сразу. Пока ответа нет, сайдбар показывает «читаем», а не корень:
>   иначе можно успеть перейти, и пришедшая следом старая история затрёт переход.

> [AI] План, кусок 3 — стрелки на экране.
> Где: `features/map-object/ui/breadcrumbs.tsx` — строка крошек над заголовком. Сейчас там одна
> «←», которая ведёт к родителю.
> - В начале строки встают «←» и «→». Они ведут назад и вперёд по истории, а к родителю ведут
>   только крошки.
> - Стрелки видны всегда, даже на корне карты. Когда идти некуда, стрелка гаснет, но не пропадает:
>   иначе крошки прыгали бы по ширине при каждом переходе. «Некуда» считается с учётом мёртвых
>   адресов: если позади только удалённые объекты, «←» тоже гаснет.
> - Подсказка у стрелки называет, куда она ведёт: «Назад: core».
> - Ctrl + клик по стрелке открывает этот объект отдельным табом, как у крошек и любой другой
>   ссылки на объект.
> - Экран одной метрики во всю ширину остаётся как есть: крошек и стрелок там нет, к объекту
>   возвращает строка сверху. Стрелки — на экране объекта.

> [AI] План, кусок 4 — доки и решения.
> - `ru/docs/decisions/` — новое решение `0036`, «назад — по истории, а не к родителю». Номер 0034
>   уже занят директивой про уведомления, она сейчас в реализации. В решении: экран — строка,
>   история на вид, где она хранится, предел в 50 шагов, почему не React Router. В старом решении
>   про мета-экран есть абзац: стрелку «назад» там не ставили, потому что истории нет. Я не
>   переписываю его, а ставлю в начало цитату со ссылкой на новое. Тогл в мета-экране при этом
>   остаётся.
> - `ru/requirements/ui/объект/` — требования к экрану объекта. В «объект в сайдбаре» фраза «крошки
>   с кнопкой назад» меняется на стрелки по истории. Рядом новое требование: «назад возвращает
>   туда, откуда пришёл, и переживает перезагрузку».
> - `ru/docs/architecture/` — `abstract-react-client.md` и `vscode-extension.md`, по абзацу: где
>   живёт история и что таб теперь сохраняет её при каждом переходе.
> - Комментарии в сборке экрана объекта: сейчас там написано, что мета-экран и вкладка
>   перезапуск переживать не должны. Это больше не так, комментарии правятся вместе с кодом.
> - `ru/docs/ideas.md` — строка про рефакторинг: фича экрана объекта разрослась, её пора делить.

План собран, давай переходить к реализации.