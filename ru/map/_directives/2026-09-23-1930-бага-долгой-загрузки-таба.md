
## 

Раньше как будто быстрее вызывались экшоны on-display 

Открывается там, и через небольшое время подгружается значение

У нас продолжает работать вариант, что выводится сначала значение уже собранное и в фоне запускается обновление?

> [AI] **Фаза 1.** Да, в коде это по-прежнему так: при подписке сервер поднимает из кэша уже собранное значение, показывает его и только потом запускает досчёт `on-display` в фоне. Но перед показом он ждёт две вещи: полное чтение карты и чтение кэша *всех* метрик вкладки. Открыть объект с группами теперь значит открыть вкладку (с 21-го), и каждое переключение — это новая подписка, а с ней заново читается вся карта. Там и ищу задержку.
>
> Уточни, где именно ждёшь: (а) переключаешь вкладку метрик внутри объекта, (б) открываешь отдельный таб редактора по ctrl+клику или (в) и там и там? И что видно, пока ждёшь: пустые клетки, «Читаем карту…» или старое значение с «…»? Если пусто — значит, не доходит показ кэша; если старое значение с «…» — показ работает, а медленный сам досчёт.
Я вижу все метрики но там отображается "Не собиралось" 
При переключении групп метрик

> [AI] **Фаза 1 → 2.** Причина видна. Переключение вкладки заводит новую подписку, и пока с сервера не пришёл её первый снимок, у клиента значений нет вовсе — а «нет значения» клетка рисует как «не собиралась». Это враньё: значения у сервера в памяти уже есть, но первый снимок он шлёт только после того, как перечитает всю карту и поднимет кэш всех метрик вкладки. Раньше вкладок не было, подписка при переходе не пересоздавалась, и этого окна не было видно.
>
> Предлагаю чинить с двух сторон: сервер отдаёт первый снимок сразу из памяти, не дожидаясь чтения карты и всей вкладки, а клиент различает «ещё не пришло» и «не собиралась» и в первом случае не пишет неправду. Вопрос о требовании: хватит ли этого, или при возврате на уже открытую вкладку значения должны стоять мгновенно, без даже короткого «загружается»? Второе значит держать значения ещё и на клиенте между подписками — это заметно больше работы.

ДА хватит

> [AI] **Фаза 3.** Тяжёлое место — чтение карты: сервер не держит её в памяти и на каждую подписку, то есть на каждое переключение вкладки, заново читает с диска все объекты, конфиги и директивы. Только после этого он узнаёт, какие метрики во вкладке, и может отдать их значения.
>
> Выхода два. Первый — клиент называет в подписке адреса метрик, которые рисует: карта у него уже есть, и сервер отвечает из памяти сразу, а чтение карты ради интервалов и вотчеров идёт следом. Второй — сервер держит прочитанную карту у себя и сбрасывает её по изменению файлов; это ускорит и всё остальное, но заводит вопрос свежести, которого сейчас нет. Предлагаю первый: он узкий и не трогает, как карта перечитывается. Согласен?
Давай второй он логичнее, и лечит сразу много. 

Заодно можно задуматься об рефакторинге всего модуля

Так как у нас в одной фиче вообще кучу всего

+ Я думаю если переписать на классы юз кейсы и сервисы станет читаемее

+ Что бы каждый юзкейс отвечал за 1 функцию а не как сейчас

Да это сильно увеличивает скоуп но зато мы готовимся к дальнейшим сильным изменениям

> [AI] **Фаза 3.** Беру второй: сервер держит прочитанную карту и сбрасывает её по изменению файлов, и рефакторинг идёт в этой же директиве. Сейчас в фиче «объект карты» на сервере лежит почти всё: чтение карты (580 строк одним файлом), сбор и трансформ метрик, стор метрик с подписками и таймерами, директивы, сборка дисплеев, git.
>
> Упираемся в действующее решение о раскладке пакетов (0015): сервер написан без классов и контейнеров — юзкейс там функция, принимающая порты. Классы его отменяют, и нужно новое решение поверх. Уточни, что тебе важнее: сами классы (тогда заводим такое решение — только для сервера или и для клиента?) или то, чтобы юзкейс делал одно дело? Второе достижимо и без классов — разрезать фичу на мелкие юзкейсы и сервисы по одному на файл, не трогая 0015.
Классы удобны. так как разделяют зависимости этапа инициализации и рантайм. Без них всё просто приходит в функцию

Это сейчас касается только сервера

> [AI] **Фаза 3.** Принял: на сервере юзкейсы и сервисы становятся классами — порты и соседние сервисы приходят в конструктор один раз при сборке, а в метод едут только данные вызова. Домен остаётся как есть, типами и чистыми функциями: разделять ему нечего, зависимостей у него нет. Под это заводим новое решение поверх 0015, только для сервера.
>
> Кто будет собирать классы вместе? Предлагаю вручную, в одном месте — в сборке сервера (`entry/server.ts`), где и сейчас связываются юзкейсы: явные `new` в порядке зависимостей, без библиотеки. Контейнер внедрения зависимостей (вроде tsyringe) даёт автоматическую сборку, но приносит декораторы и магию разрешения, которую не видно при чтении. Вручную или контейнер?
Давай вручную

> [AI] **Фаза 3.** Собираем вручную в `entry/server.ts`. Теперь — на какие фичи резать «объект карты». Предлагаю по тому, что меняется вместе:
>
> - **карта** — чтение с мерджем и подстановками, тот самый кэш прочитанной карты со своим вотчером, положение узлов. Все остальные берут карту только отсюда;
> - **метрики** — сбор, трансформ, стор со значениями и подписками, встроенные шаги и git-пометки;
> - **директивы** — создание, удаление, этапы и промпты, реплика в тред;
> - **дисплеи** — сборка компонента, её кэш и схема данных.
>
> Запуск скрипта и агента сейчас общий у метрик и экшонов — он уходит в общий сервис, а не в одну из фич. Так режем или ты видишь границы иначе?
Да давай так попробуем

Связывай фичи через внутренние порты 

Порты нужно разделить сейчас один интерфейс на весь сервер что ISR сильно нарушает

> [AI] **Фаза 3.** Режем на четыре фичи, связь между ними — через внутренние порты. Внешние порты уже описаны по отдельности (файлы, оболочка, агент, часы, таймеры), но в каждый юзкейс едет вся связка разом, в двадцати с лишним местах. Теперь конструктор класса берёт только то, чем пользуется; широкий порт файлов заодно делится на чтение, запись и слежку.
>
> Вопрос, где живёт внутренний порт. Предлагаю у потребителя: метрики сами объявляют в своей фиче «мне нужен источник карты» из одного метода, фича карты этому интерфейсу просто соответствует, а стыкует их сборка сервера. Тогда потребитель видит ровно то, что ему нужно, и фича карты может расти, не задевая его. Сейчас же по раскладке все порты, и между фичами тоже, лежат одной папкой `ports/` — это и есть тот общий интерфейс на весь сервер. Порт у потребителя — согласен?
да давай сделаем порт у потребителя

> [AI] **Фаза 4.** Порт у потребителя — принял. Одно противоречие с тем, что есть. Сейчас каждый вызов, включая чтение агентом через MCP, перечитывает карту и потому всегда свеж. Кэш же сбрасывается вотчером, а вотчер смотрит только в папку карты. Прототип, до которого `extends` дотягивается за её пределы — файлом снаружи или по сети, — поменяется, а кэш об этом не узнает, и агент будет читать старое, пока не поменяется что-то внутри карты.
>
> Экран карты живёт так уже сейчас: он перечитывается тем же вотчером. Предлагаю принять такую же свежесть и для кэша, а сетевые `extends` перечитывать не реже, чем раз в заданный срок. Или внешние слои тоже должны сбрасывать кэш сразу — тогда вотчер ставится на каждый файл, который `extends` прочитал за пределами карты?
Давай пока остановимся на слежке за картой + функцию типо рефреша как у браузера рядом с навигацией все привыкли

> [AI] Понял всё, давай переходить к плану.

> [AI] **solution — одна прочитанная карта на сервер.**
>
> Сервер держит у себя прочитанную карту, по одной на каждую карту, и все, кому она нужна, берут её отсюда: экран карты, подписка на метрики, запуск экшонов и директив, чтение агентом через MCP. Сейчас каждый из них читает с диска сам.
>
> Вотчер становится один на карту и живёт, пока жив сервер, а не пока открыта подписка на экран: иначе агент без открытого окна читал бы протухшее. Своё он пропускает так же, как сейчас: кэши и логи метрик, положение узлов, прогоны. Любое другое изменение в папке карты сбрасывает прочитанное, и все подписчики экрана получают новую карту.
>
> Пока карта перечитывается, одновременные запросы ждут одно и то же чтение, а не заводят каждый своё.
>
> Сброс руками — новый вызов сервера «перечитать карту». Его зовёт кнопка обновления рядом со стрелками навигации; нужен он для того, чего вотчер не видит: прототипа за пределами карты и сетевого `extends`.
Я правильно понимаю что любое изменение перечитывает прям всююю крату а не только кусочки?

> [AI] Да, по этому куску — всю. Причина в том, что объект после сборки зависит не только от своего файла: от прототипов по цепочке наследования и от подстановок, которые берут поля у других объектов. Поменялся прототип — поменялись все наследники; поменялось поле, на которое ссылается подстановка, — поменялся ссылающийся. Чтобы перечитывать кусочек, надо держать обратный граф «файл → кого он задевает», а подстановки делают его нетривиальным.
>
> Выигрыш при этом уже есть: сейчас карта перечитывается целиком на каждый запрос, а станет — на каждое изменение. Пачку изменений подряд (агент пишет директиву, сервер следом её состояние) склеиваю в одно перечитывание короткой паузой.
>
> Предлагаю так и оставить, а частичное перечитывание вынести отдельной директивой, если замер покажет, что полное чтение карты медленное само по себе. Ок?
А могли бы мы сделать модель карты реактивной, например через тот же mobx тогда не тривиальный граф управляется решением для этого созаднным

> [AI] **solution — одна прочитанная карта на сервер, реактивная (новая версия куска).**
>
> Да, MobX ровно под это: вычисляемое значение само запоминает, что оно прочитало, и пересчитывается, только когда поменялось прочитанное. Граф «файл → кого задевает» тогда никто не строит руками — он складывается сам из того, что читала сборка. В проекте уже есть rxjs, но он зависимостей не отслеживает, и граф пришлось бы вести самим; поэтому MobX, и только для модели карты на сервере.
>
> Для этого чтение карты разрезается надвое. Первое — загрузка: файлы карты (и то, куда ведут `extends`) лежат наблюдаемыми значениями, вотчер обновляет изменившийся файл. Второе — сборка: наследование, мердж и подстановки остаются чистыми функциями, а объект после сборки — вычисляемым значением поверх загруженного. Сейчас эти два шага перемешаны: сборка сама читает файлы посреди мерджа, и это главное, что придётся переделать.
>
> Остальное из прошлой версии остаётся: одна модель на сервер для всех потребителей, вотчер на всё время жизни сервера, кнопка «перечитать карту», которая перезагружает внешние слои. Наружу по-прежнему уходит карта целиком, просто собирается она заново только в задетой части. Ок?

Или может быть сделать через rxjs раз он уже есть, но только через behaviur subject 

То есть модель, это объекты с обсерваблами в полях, на которые мы подисываемся

И граф так же строится но в rxjs больше контроля

> [AI] **solution — одна прочитанная карта на сервер, реактивная на rxjs (новая версия куска).**
>
> Да, на rxjs тоже ложится, и у него есть плюс, которого у MobX нет: чтение файла и сетевой `extends` асинхронные, и в rxjs они встают в граф как есть, а у MobX вычисления синхронные, и загрузку пришлось бы держать сбоку. Граф при этом виден в коде явно, это и есть обещанный контроль.
>
> Модель — объекты, у которых поля это потоки с текущим значением. Внизу файлы: у каждого свой такой поток, вотчер кладёт в него новое содержимое. Над ними объект после сборки: его поток собран из потоков своего файла и прототипов. Куда ведёт цепочка, зависит от содержимого — поменяли `extends`, и объект переподписывается на другой прототип; подстановка так же подписывается на поле объекта, на который ссылается. Сами наследование, мердж и подстановки остаются чистыми функциями, поток только зовёт их.
>
> Цену за контроль платим в двух местах. Одно изменение, которое доходит до объекта двумя путями (прототип и подстановка из того же файла), даст промежуточную, наполовину старую сборку — поэтому наружу карта уходит, только когда изменение улеглось. И подписками управляем сами: объект, который пропал из карты, должен отписаться, иначе граф течёт.
>
> Остальное как раньше: одна модель на сервер для всех, вотчер на всё время жизни сервера, кнопка «перечитать карту». Ок?

Получается что бы не получать устаревшую карту нам нужен механизм на подобии батчинга из реактивнхы фреймворков?

Кстати раз всё обсёрваблы, то слежение за extends становится тоже решаемой в общем виде задачей. И тогда проблем в результате может быть мало 

> [AI] **solution — реактивная карта на rxjs: батч и внешние слои (дополнение к куску).**
>
> Да, нужен батч, и он получается простым, если асинхронное держать только внизу графа. Файл сначала читается, и только готовое содержимое кладётся в его поток; всё, что выше — сборка объектов, мердж, подстановки, — считается синхронно. Вотчер собирает изменения, пришедшие почти разом, в одну пачку и кладёт их вместе. Промежуточные сборки при этом возникают, но все внутри одного синхронного прохода, а наружу карта уходит один раз, после того как проход кончился. Отдельного механизма транзакций, как во фреймворках, не нужно: хватает правила «граф синхронный, пачка кладётся целиком».
>
> С внешними слоями ты прав, это решается в общем виде. Поток файла заводится, когда на файл кто-то сослался, и слежка ставится на сам файл, где бы он ни лежал: в карте или снаружи. Перестали ссылаться — поток и слежка гаснут. Остаётся только сеть: за ней следить нечем, поэтому сетевой слой перечитывается по сроку, и его же перечитывает кнопка «перечитать карту». Ок?
Да ок решили! 

> [AI] **solution — подписка на метрики отвечает сразу, клиент не врёт про пустое.**
>
> Это сама бага. Подписка на метрики вкладки больше не ждёт ни чтения карты, ни кэша всех метрик вкладки. Карта уже в памяти, поэтому список метрик вкладки известен сразу, и первый снимок уходит в том же такте: у метрики, чьё значение сервер уже держит, — это значение. Метрика, чей кэш ещё не поднят с диска, приходит в снимке с пометкой «ещё поднимается», и следующий снимок уходит, как только поднялась она, а не вся вкладка разом. Досчёт в фоне идёт, как и раньше, после показа.
>
> Клиент различает три состояния, а не два: «значение есть», «метрика не собиралась ни разу» — это говорит сервер — и «ответа ещё нет» — снимок не пришёл или метрика ещё поднимается. Третье клетка рисует как загрузку, а не как «не собиралась».
>
> Так выполняется требование к вкладкам, которое сейчас нарушено: при переключении собранное значение остаётся на месте. Ок?

> [AI] **software — четыре фичи вместо «объекта карты» и связь через порты потребителя.**
>
> Фича «объект карты» на сервере делится на четыре, по тому, что меняется вместе:
>
> - **карта** — реактивная модель из первого куска: потоки файлов, сборка объектов, батч, слежка, перечитывание, положение узлов. Наружу даёт три вещи: текущую карту, поток карты и «перечитать»;
> - **метрики** — сбор, трансформ, стор значений с подписками и таймерами, встроенные шаги, git-пометки;
> - **директивы** — создание и удаление, этапы и промпты, состояние прогона, реплика в тред;
> - **дисплеи** — сборка компонента метрики, её кэш и схема данных.
>
> Запуск скрипта и агента, общий у метрик и экшонов, становится отдельным сервисом вне фич. Фичи прогонов экшонов и списка «ждут ответа» остаются как есть, только переходят на те же правила.
>
> Фичи друг друга не импортируют. Кому что нужно от соседа, тот объявляет у себя порт из нужных ему методов: метрикам от карты — «дай объект и его метрики», директивам — «дай объект и его этапы». Фича-поставщик этим портам просто соответствует, а стыкует их сборка сервера. Ок?
Отдельных сервисов вне фич нет, это могут быть отдельные фичи, которые просто предоставляют порты другим фичам а так ок

> [AI] Принял: вне фич ничего не живёт, запуск скрипта и агента — своя, пятая фича, которая отдаёт порт метрикам и экшонам.
>
> **code — классы на сервере, домен чистый, сборка вручную.**
>
> Юзкейс и сервис на сервере — класс. В конструктор приходит то, что известно при сборке: внешние порты и порты соседних фич, каждый ровно тот, которым класс пользуется. В метод — только данные вызова. Общая связка всех портов, которую сейчас получает каждый, пропадает; широкий порт файлов делится на чтение, запись и слежку.
>
> Один юзкейс — одно действие: «создать директиву», «завершить этап», а не набор функций в одном модуле. Сервис — то, что живёт дольше вызова: модель карты, стор метрик, кэш сборок.
>
> Домен остаётся типами и чистыми функциями: зависимостей у него нет, и класс ему ничего не даёт. Потоки карты зовут эти функции, а не прячут логику в себе.
>
> Собирается всё вручную в одном месте, в сборке сервера: явные создания в порядке зависимостей, без контейнера и декораторов. Это меняет действующее решение о раскладке пакетов, где сказано «без классов», — заводим новое решение поверх него, только для сервера, и правим под него архитектурную доку сервера.
>
> План собран, давай переходить к реализации.
Важно ещё нужно поправить документы архитектурные 