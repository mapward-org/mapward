
## 

В информации об объекте может быть много директив

Давай добавим иконочку поиска (ищет по названию)
простой клиентский фильтр списка (инпут появляется при клике на поиск, под заголовком)

> [AI] Треда не было — не сжимал; снимок перед работой — коммит `c20059b`.
> В мета-экране у раздела «Директивы» в заголовке лупа; клик открывает под заголовком поле
> с фокусом, список фильтруется по мере набора. Повторная лупа или Escape закрывают поле и
> сбрасывают запрос. Раздел с поиском теперь целиком живёт в `_directives/compose/directive-section.tsx`,
> фильтр — `matchDirectives` в `pure-model/directives.ts`, с тестами.
> Умолчания вместо ответа: ищу подстроку без регистра по имени файла целиком, а не только по
> подписи — дата тоже находится («2026-09-21»); дефис в имени совпадает с пробелом в запросе
> («мини поиск»). Ничего не нашлось — строка «не нашлось». Лупа только в мета-экране, как
> просили; на первом экране (незакрытые) её нет. Цвета поля взяты из темы редактора — в
> `theme.css` добавлены `--mw-input-*` и `--mw-focusBorder`.
> Проверки: typecheck, test, lint, format:check, build — зелёные. Живой вебвью обслуживает
> собранный `dist` расширения, так что увидеть лупу получится после перезагрузки окна.
>
> сделал, давай переходить к тестированию

Можешь комитить

> [AI] Проверил: всё из отчёта на месте, typecheck, test, lint, format:check, build зелёные,
> лупа доехала в собранный `dist/webview.mjs`. На карте через MCP эта правка не видна — она
> только в интерфейсе. Коммичу по слову выше.
