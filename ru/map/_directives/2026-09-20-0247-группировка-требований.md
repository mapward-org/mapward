
## 

Давай перегруппируем требования, так что бы с ними было удобнее работать

Сейчас смешано, то как работает сервер, нет под cli и под mcp разделов 

Отдельный раздел хочется под ui 

Предложи разделы новые

## Вопросы

> [AI] Сначала про ось, потому что от неё всё остальное. Сейчас разделы — предметные
> («метрики», «директивы», «объект», «карта»), и внутри каждого лежит вперемешку то, что
> человек видит, то, что получает агент, и то, как это устроено внутри. Твои «cli», «mcp»,
> «ui» — не предметы, а **поверхности**: кто этим пользуется. Значит ось меняется, а не
> добавляется пара папок.
>
> Вариантов ровно три:
> **а)** поверхность сверху — `интерфейс/`, `mcp/`, `cli/`, а предметные разделы остаются
> для того, что от поверхности не зависит;
> **б)** предмет сверху, поверхность подпапкой — `метрики/интерфейс/`, `метрики/mcp/`;
> **в)** оставить предметы и просто добавить `ui/`, `mcp/`, `cli/` рядом — тогда одно и то же
> требование можно положить в два места, и через месяц так и будет.
>
> Предлагаю **(а)**: жалоба именно на то, что слои смешаны, а поиск «где про показ метрик»
> в (б) всё равно требует обойти все предметы. (в) отпадает — это не группировка, а вторая
> ось поверх первой.

> [AI] Тогда конкретный список — семь разделов, всё 57 файлов разложены без остатка:
>
> | Раздел | Сколько | Про что |
> | --- | --- | --- |
> | `интерфейс/` | 18 | всё, что человек видит: объект в сайдбаре, сетка метрик, списки директив и экшонов, кнопки этапов, терминалы, аккордеон карт, пустые состояния, activity bar |
> | `mcp/` | 12 | что получает агент: поля и метрики по ключам, обход по ссылкам, бюджет ответа, сводка директив, тексты директив и экшонов, промпт этапа |
> | `cli/` | 1 | работа без редактора |
> | `метрики/` | 9 | сбор и хранение: коллекторы, агент-коллектор, кэш, свежесть, интервал, таймаут |
> | `директивы/` | 5 | механика: этапы, состояние прогона, хук, промпты объекта и директивы |
> | `модель/` | 3 | объект с диска, карта по конфигу, вотчеры |
> | `устройство/` | 8 | инварианты: контракт в `core`, направление зависимостей, порты, capabilities, клиент в любом хосте, rpc, доки с инструментом |
>
> `основа/` при этом исчезает — два её требования расходятся в `интерфейс/` и `устройство/`,
> и правильно: раздел без предмета собирает всё подряд. `архитектура/` превращается в
> `устройство/`, потеряв MCP-требования и CLI. Возражения по составу?

> [AI] `cli/` с одним требованием — раздел ради раздела. Но ты просишь его завести, и я
> согласен: это заявка на рост, а без папки следующее CLI-требование опять уедет в
> «архитектуру». Предлагаю завести и не смущаться одним файлом. Или подождать второго?

> [AI] Имена: все папки сейчас русские, а `mcp` и `cli` по-русски не пишутся. Предлагаю
> смешать осознанно — `интерфейс/`, `mcp/`, `cli/`: первое переводится, второе и третье имена
> собственные. Альтернатива — `ui/` вместо `интерфейс/`, тогда латиницы три из семи. Как?

> [AI] Двойственные требования: «Видно, какие этапы есть у объекта» — это и меню на объекте,
> и поле в ответе MCP (`object:` у него оба пакета). Таких несколько, и правило нужно заранее,
> иначе каждый раз спор. Предлагаю: **раздел по тому, что требование обещает наружу**.
> Обещает увиденное — `интерфейс/`; обещает отданное агенту — `mcp/`; обещает поведение
> независимо от того, кто смотрит, — предметный раздел. Обещает и то и другое всерьёз —
> это два требования, а не одно. Годится?

> [AI] Подпапки. `интерфейс/` на 18 файлов — уже много, и внутри видны две связки: дисплеи
> (дерево, граф, форма данных, свежесть) и терминалы (терминал на объект, вкладка, кнопки,
> уведомления). Предлагаю `интерфейс/дисплеи/` и `интерфейс/терминалы/`, остальное плоско.
> Существующая `метрики/дисплеи/` при этом переезжает целиком.

> [AI] Экшон «Добавить требование» сейчас говорит: «кладётся в подходящую группу, а если такой
> нет — заводится новая папка». С фиксированной осью это неправда: новая папка сбоку сломает
> ровно то, что мы сейчас чиним. Предлагаю переписать его — перечислить семь разделов и дать
> то самое правило выбора «по тому, что требование обещает». Иначе через две директивы реестр
> поплывёт обратно, и виноват будет не человек, а инструкция.

> [AI] Решение: ось группировки — это `0011`, там прямо написано «папки остаются смысловой
> группировкой». Смысл не исчезает, но появляется правило, которого не было. Предлагаю
> дописать в `0011` раздел «Разделы — по поверхности», как мы на днях дописывали в `0017`,
> а не заводить новое решение: предмет тот же, отмены прежнего нет.

> [AI] Механика переноса. Это `git mv` пятидесяти файлов, и два места ломаются молча:
> в `хук-примешивается-к-промпту-этапа.md` лежит относительная ссылка на соседнее требование,
> а кэш `code-drift` у `vscode-application` держит пути вида `ru/requirements/объект/…` —
> после переноса они станут битыми, пока метрику не пересоберёшь. Предлагаю на «Реализации»:
> перенос одним прогоном, правка ссылки, следом пересбор `code-drift` на обоих приложениях.
> И ещё: прямо сейчас в `директивы/` пишет другая сессия — два новых файла ещё не в гите.
> Перенос стоит делать после неё, иначе разъедется. Подождём?
