##

Заведи требования и реализуй: конфигурацию метрик объекта видно из интерфейса — пунктом меню
на объекте, — а сырой конфиг, до мерджа, достаётся из MCP.

Требования по экшону `mapward://prototypes/system` → «Добавить требование», реализация — по
«Реализовать требование». Сколько нужно, столько и заводи; раздел выбирается по тому, что
требование обещает наружу: меню — `ru/requirements/ui/метрики/`, сырой конфиг —
`ru/requirements/mcp/`. Объекты — `mapward://packages/abstract-react-client` и
`mapward://apps/vscode-application` для интерфейса, `mapward://packages/abstract-server`
для MCP.

## Зачем

Текущую конфигурацию метрик неоткуда посмотреть, хотя она важна для работы с картой: какие
метрики у объекта есть, чем они собираются, откуда приехал конфиг. Открыть `_metrics/`
в проводнике — не ответ: конфиг чаще всего лежит у прототипа, а рядом с объектом только кэши.

## Что решено

**Меню на объекте**, рядом с «Директивами», «Этапами» и «Экшонами». Пункт на метрику: `label`,
откуда приехал конфиг — свой, от прототипа, через `extends`, — `refresh` и виды коллекторов
подсказкой.

**Клик открывает мердж виртуальным документом**: свой `TextDocumentContentProvider` на схему
`mapward:`, только чтение, язык `json` — подсветка включается языком, а не расширением. Не
временный файл: тот просит имя, папку, уборку и при закрытии спрашивает «сохранить?» то, чего
на диске нет.

**Под виртуальный документ — отдельный порт моста**, принимающий текст, а не параметр
к `openPath`. Мердж у клиента уже на руках, серверу работы нет.

**Своё умение хоста**, рядом с `openFile`: по `0014` клиент рисует только то, что хост обещал.
Хост без провайдера пункт меню не показывает — это честнее, чем открывать пустоту или молчать
в ответ на клик.

**Строка под пунктом — ряд кнопок по слою**: `свой`, `Пакет`, `shared-metrics/files`. Форма
готова — `runs` у `MenuButton`, так сделаны этапы под директивой.

**`read_index` по адресу метрики** отдаёт её собственный `config.json`, прочитанный с диска, —
ровно так же, как без `file` отдаёт `_index.json` объекта. Ни нового параметра, ни нового
инструмента: адрес метрики уже приходит в ответе, и звать по нему — тот же приём, что с
текстом директивы.

**Слой — ссылка, а не копия.** Ответ несёт адрес следующего слоя, а не его конфиг; дальше
идут тем же вызовом. Поэтому вес ответа не растёт — `0016` не зря говорит, что конфиги и есть
самое тяжёлое, — и в `read_object` слои не показываются вовсе.

**Решение `0019` заводим** — записать в него то, что здесь решено.

## Почему не так, как задумывали сначала

Первой напрашивалась **файловая метрика**, `read-dir` по `_metrics/`. На вопрос не отвечает,
проверено на `packages/core`: в папке шесть директорий, а `config.json` есть у двух —
остальное кэши и логи, и дерево показало бы состояние как определение, против `0002`. При этом
`code`, `docs`, `files` в папке отсутствуют вовсе: их конфиг лежит у `prototypes/package`,
а `files` ещё и через `extends` на `shared-metrics/files`. Ровно то, что просят увидеть, —
результат резолва `extends` и подстановок, а его в файлах нет по определению (`0006`).

Вторым — **коллектор `object-metrics`**, читающий модель. Отменён: коллектор нужен один раз и
только самому инструменту, а распространять его пришлось бы всем. Меню показывает метрики
любого объекта само, ничего не подключая, — поэтому отпал и общий прототип-корень, который
заводили ради слепоты `apps`, `packages` и `relations`. Общий прототип нужен, но по другой
причине и отдельной директивой.

Отдельный **инструмент MCP** не заводится: расширяется `read_index`. `0016` отказался от
инструмента обхода тем же рассуждением — второй способ читать то же самое.

## Что нашли по коду

Куда ходили и что там лежит — чтобы «План» не искал заново:

- **адресация метрики уже есть.** `findMetric` — `packages/core/src/model/model.ts:125`,
  метрика несёт собственный `address` (`mapward://packages/core/_metrics/files`), `run_metric`
  его принимает, и в ответе MCP он отдаётся (`mcp.ts:117`). Спецкейс в `findObject` не нужен;
- **чтение файла у `read_index` уже такое.** Без `file` он зовёт `readIndexFile(object.path)`
  (`mcp.ts:459`); для метрики симметрично — `readMapFile(metric.configPath)`;
- **ссылка на следующий слой в модели уже есть.** У метрики от прототипа — `owner`, адрес
  объекта-владельца, и `configPath`, указывающий на прототипов файл (`adoptMetric`,
  `model.ts:28`). У цепочки метрик — `config.extends`, он переживает мердж. Сам мердж
  схлопывает конфиг на месте и родительский `configPath` не хранит
  (`read-map.ts:237`, `merge.ts:26`);
- **мост.** `openPath` и `openExternal` — `packages/core/src/contracts/config.ts:29`; умения
  хоста `{ terminals, openFile, ask }` — `contracts/map.ts:20`; расширение объявляет все три —
  `apps/vscode-extension/src/apps/extension/ports/index.ts:146`.
  `TextDocumentContentProvider` в репозитории нет вовсе;
- **дорожка «открыть файл» проложена.** Красная точка метрики открывает
  `${cachePath}/collect.logs.json` через `onOpen` → `openPath` → `showTextDocument(Uri.file)`;
- **форма меню есть.** `MenuButton` — `features/map-object/ui/menu-button.tsx`: пункт с `hint`
  и `runs` — рядом маленьких кнопок под пунктом;
- **`config` в клиенте используется**, вопреки тому, что писали сначала:
  `metric-grid.tsx:60–72` берёт `label`, `collapsed`, `collectorsCache`, `display.kind`,
  `display.empty`. Не используется только `configPath`;
- **`MapMetric` — публичный тип**: `packages/core/src/index.ts` экспортирует `model/model.ts`,
  у `packages/core` есть метрика `public-api`;
- **документация переехала** в `packages/docs/ru/` (коммит `bd43210`) и собирается
  в `packages/docs/src/content.generated.ts` скриптом `bundle-docs.mjs`. Про MCP —
  `packages/docs/ru/mcp.md`, таблица коллекторов — `packages/docs/ru/metrics.md`;
- **раздел реестра выбирается по обещанному наружу** — экшон «Добавить требование»
  на `prototypes/system`: увиденное `ui/`, отданное агенту `mcp/`, предмет — доменный раздел.

## Осталось уточнить

### Сохранять слои, похоже, нечего

Ты сказал «начнём сохранять слои», но потом сам же предложил хранить ссылку, а не значение, —
и по коду выходит, что ссылки уже есть. Свой конфиг лежит файлом по `configPath`, следующий
слой назван `owner` у прототипа и `config.extends` у цепочки. `read_index` читает файл и
отдаёт адрес дальше; правки модели ноль, и отдельное требование в `модель/` не нужно.

> [AI] Так и делаем — или ты всё же хочешь слои собранными в модели и отданными одним ответом,
> чтобы не ходить по цепочке вызовами?

Да если этого достаточно то давай так оставим
## План

### Что проверено по коду

**Цепочка слоёв прослежена целиком** на `files` у `packages/core`: `configPath` ведёт в
`ru/map/prototypes/package/_metrics/files/config.json`, там одна строка
`{ "extends": "mapward://shared-metrics/files" }`, а по этому адресу лежит
`ru/map/shared-metrics/files/config.json` — `label`, `refresh`, коллектор `read-dir`, дисплей.
Два слоя, оба читаются файлом, собирать в модели действительно нечего.

**Но по ссылке цепочка сегодня обрывается на втором шаге.** Адрес в `extends` ведёт не на
метрику, а на объект-носитель: `shared-metrics/files` — обычный объект карты, только в его
папке лежит `config.json` вместо `_index.json`. `read_index` разрешает адрес через `findObject`
(`mcp.ts:441`) и читает `readIndexFile` (`mcp.ts:459`) — по такому адресу объект найдётся, а
ответ придёт `index: null`. Это единственное место, где «слой — ссылка» не работает само.

Остальное подтверждено и лежит в «Что нашли по коду»: `findMetric` (`model.ts:125`), контракт
моста (`contracts/config.ts:29`), умения (`contracts/map.ts:20`), объявление умений
(`ports/index.ts:146`), отсутствие `TextDocumentContentProvider`, форма `runs` у `MenuButton`.
Сверх того: `read_index` уже покрыт тестом в `mcp.test.ts:348` — образец для новых;
`useCapabilities` держит дефолт `{ terminals: false, openFile: false, ask: false }`, значит
новый флаг добавляется в трёх местах; иконки — простые SVG 16×16 в `ui/icons.tsx`.

### Какие файлы придётся тронуть

**Ядро** — `packages/core/src/contracts/config.ts` (новый метод моста),
`contracts/map.ts` (`Capabilities` плюс одно поле).

**Сервер** — `packages/abstract-server/src/entry/mcp.ts` (разрешение адреса в `read_index`),
`entry/mcp.test.ts` (тесты), описание инструмента там же.

**Клиент** — `features/map-object/ui/icons.tsx` (иконка),
`compose/map-object.tsx` (`MenuButton` «Метрики»), `adapters/use-map.ts` (проброс метода),
`adapters/use-capabilities.ts` (дефолт нового флага).

**Расширение** — новый файл под провайдер рядом с `map-view.ts`,
`apps/extension/main.ts` (регистрация), `features/maps/adapters/editor.ts` (адаптер),
`features/maps/compose/handlers.ts` (проводка), `apps/extension/ports/index.ts` (умение).

**Документация** — `packages/docs/ru/mcp.md`, абзац про `read_index` по адресу метрики.
`content.generated.ts` пересобирается скриптом `bundle-docs.mjs` на сборке, руками не трогаем.

**Карта** — решение и два требования, см. ниже.

### Решения

Одно, `ru/docs/decisions/0019-metric-config.md`. В нём:

- конфигурация метрики — данные объекта, и смотрится она интерфейсом объекта, а не второй
  панелью и не метрикой; почему это не спорит с `0016` — там отказались от второго способа
  **обходить** карту, а не от показа объектом своих же данных;
- почему не коллектор `object-metrics`: нужен один раз и только инструменту, а раздавать
  пришлось бы всем;
- мердж показывается виртуальным документом, у него свой порт моста и своё умение — по `0014`
  клиент не рисует того, чего хост не обещал;
- сырой конфиг отдаётся тем же `read_index`, по адресу; слои — ссылками, а не копиями, потому
  что по `0016` конфиги и есть самое тяжёлое в ответе.

### Требования

Два, оба со статусом `draft` — писал агент:

1. `ru/requirements/ui/метрики/конфигурация-метрики-видна-на-объекте.md`,
   `object: [mapward://packages/abstract-react-client, mapward://apps/vscode-application]`.
   Меню на объекте, пункт на метрику, клик открывает мердж виртуальным документом, ряд кнопок
   под пунктом — по кнопке на слой. Хост без умения пункт не показывает;
2. `ru/requirements/mcp/сырой-конфиг-метрики-читается-по-адресу.md`,
   `object: mapward://packages/abstract-server`. `read_index` по адресу метрики отдаёт её
   собственный `config.json` до мерджа; следующий слой назван адресом, не вложен значением;
   `read_object` слои не показывает.

Третьего требования, в `ru/requirements/модель/`, не заводим — сохранять в модели нечего.

### Что переписывается из существующего

- `Capabilities` — три поля станут четырьмя, и это три правки: контракт, дефолт в
  `useCapabilities`, объявление в `ports/index.ts`;
- `configBridge` — плюс метод рядом с `openPath` и `openExternal`;
- разрешение адреса в `read_index` — сейчас одна ветка (`findObject`), станет три: адрес
  метрики, объект с `_index.json`, объект-носитель конфига;
- `mapsHandlers` — одна строка;
- шапка объекта в `map-object.tsx` — четвёртое меню рядом с тремя;
- описание `read_index` в `mcp.ts` и абзац в `packages/docs/ru/mcp.md`.

Сетку метрик не трогаем: меню живёт в шапке объекта, `metric-grid` остаётся как есть.

### Порядок

1. **Требования и `0019`** — первыми, чтобы реализация шла по написанному, а не наоборот;
2. **ядро**: метод моста и флаг умения — от них зависят и клиент, и расширение;
3. **сервер**: разрешение адреса в `read_index`, описание инструмента, тесты в `mcp.test.ts`;
4. **расширение**: провайдер на схему `mapward:`, адаптер, проводка, `virtualDocs: true`;
5. **клиент**: иконка, меню, проброс метода, дефолт умения;
6. **документация**: `packages/docs/ru/mcp.md`;
7. **проверки**: `test`, `lint`, `format:check`, `typecheck`, `build`. Единой команды `check`
   в репозитории нет — она зовёт несуществующее и падает.

Шаги 3 и 4–5 независимы: MCP и интерфейс не пересекаются нигде, кроме решения и требований.

### Чего в этом прогоне не делаем

- **не трогаем модель** — слои не собираем, `MapMetric` остаётся как есть, публичный API не
  меняется;
- **не трогаем `read_object`** — слои туда не попадают, `config` приходит, как приходил;
- **не заводим коллектор** `object-metrics` и **не заводим инструмент MCP**;
- **не заводим общий прототип-корень** — он нужен, но по другой причине и отдельной директивой;
- **не чиним расхождения**, которые показывает `decisions-drift` (сетка метрик против `0003`,
  переходы по ссылкам против `0004`) — они не про эту директиву;
- **не коммитим после прогона**: результат смотрится диффом.

### Вопросы

> [AI] Обрыв цепочки на объекте-носителе. Предлагаю не спецкейс, а честное правило:
> `read_index` по адресу объекта отдаёт то, что в папке реально лежит, — `index` и `config`,
> любое из них может быть `null`. Тогда `mapward://shared-metrics/files` отвечает конфигом сам,
> а метрика остаётся третьей веткой разрешения адреса. Так — или пусть `read_index` по адресу
> метрики отдаёт сразу все ссылки цепочки списком, чтобы второго вызова не было вовсе?

Реши сам


> [AI] Имя порта и флага. Предлагаю `openVirtual({ title, text, language })` и умение
> `virtualDocs` — имя по тому, что хост умеет, а не по тому, чем это показывают. Или ближе
> к соседям — `openDocument` и `openDocument: true`?

Ок

> [AI] Что кладём в подсказку пункта меню. Предлагаю `refresh`, виды коллекторов и трансформов,
> дисплей — то, что отвечает «чем собирается», одной строкой. Весь json открывается кликом, и
> дублировать его в тултипе незачем. Или в подсказке хватит одного `refresh`?

Реши сам

> [AI] Подписи на кнопках слоёв. Предлагаю: свой слой — «свой», доставшийся от прототипа — имя
> объекта-владельца («Пакет»), пришедший по `extends` — адрес («shared-metrics/files»). То есть
> ровно то, что уже пишется в `hint` у этапов и экшонов. Или короче — номером слоя?

Реши сам

> [AI] Статус требований. По экшону «Добавить требование» агентское требование заводится
> `draft`, и в `ready-to-implement` его переводит человек, вычитав. Предлагаю так и сделать:
> оба пишу `draft`, ты вычитываешь. Или ставить сразу `ready-to-implement`, раз решения приняты
> здесь же?

Сразу имплементируй
## Реализация

Сделано по плану, порядок соблюдён. Перед стартом коммитить было нечего: все файлы, которые
предстояло править, были чистыми.

### Карта

- `ru/docs/decisions/0019-metric-config.md` — решение. Четыре инварианта: конфигурация
  показывается средствами объекта; цепочка передаётся ссылками; модель слои не хранит; ручка
  без пути объявляется своим умением;
- `ru/requirements/ui/метрики/конфигурация-метрики-видна-на-объекте.md`;
- `ru/requirements/mcp/сырой-конфиг-метрики-читается-по-адресу.md`.

### Ядро

- `contracts/config.ts` — ручка `openVirtual({ title, text, language })`;
- `contracts/map.ts` — умение `virtualDocs`.

### Сервер

- `entry/mcp.ts` — `read_index` разрешает адрес тремя ветками: объект, метрика (`findMetric`),
  иначе отказ. По метрике отдаёт `configPath`, `owner`, сырой `config` и `extends` — адрес
  следующего слоя; по объекту — `index` и `config`, любое может быть `null`. Хелпер
  `extendsOf` читает ссылку из самого файла и не считает сломанный json ошибкой: сырое чтение
  обязано отдать то, что лежит;
- описание инструмента переписано под это;
- `entry/mcp.test.ts` — три теста и два файла в фикстуре: общая метрика объектом без
  `_index.json` и метрика, подключившая её одной строкой.

### Расширение

- `features/maps/adapters/virtual-doc.ts` — новый: провайдер на схему `mapward:`, документ
  только на чтение, язык ставится `setTextDocumentLanguage`. Повторный показ того же заголовка
  обновляет вкладку, а не открывает вторую;
- `apps/extension/main.ts` — регистрация на всё время жизни расширения;
- `features/maps/compose/handlers.ts` и `index.extension.ts` — проводка;
- `apps/extension/ports/index.ts` — `virtualDocs: true`;
- `apps/cli/src/ports.ts` — `virtualDocs: false`. Это ровно тот хост, ради которого умение и
  заводилось, и типизация о нём напомнила сама.

### Клиент

- `ui/icons.tsx` — `MetricsIcon`;
- `ui/menu-button.tsx` — у пункта появился необязательный тултип;
- `compose/map-object.tsx` — меню «Метрики» рядом с экшонами, плюс `layersOf` и `howCollected`;
- `adapters/use-map.ts`, `adapters/use-capabilities.ts` — ручка и умение.

### Документация

`packages/docs/ru/mcp.md` — строка в таблице инструментов и раздел «Конфиг метрики без
мерджа». `content.generated.ts` пересобирается скриптом на сборке, руками не трогал.

### Где решил за человека

Три вопроса были оставлены на меня, по ним взял то, что предлагал в плане:

- **обрыв цепочки** — `read_index` по адресу объекта отдаёт и `index`, и `config`. Проверено на
  настоящей карте: `mapward://shared-metrics/files` теперь отвечает конфигом при `index: null`;
- **подсказка пункта** — `refresh`, виды коллекторов и трансформов, дисплей, одной строкой
  в тултипе. Справа, где у этапов стоит владелец, — откуда приехал конфиг;
- **подписи кнопок слоёв** — «свой», имя объекта-владельца, адрес из `extends`.

Сверх того решил сам, потому что всплыло при реализации:

- **слоёв в меню максимум два** — свой файл и тот, на который он ссылается. Больше клиенту
  неоткуда: модель слои не хранит, а `extends` в мердже остаётся только свой. На этой карте
  цепочки как раз двухслойные, и требование написано под это. Понадобится глубже — это правка
  сервера, не интерфейса;
- **статус требований — `implemented`**, а не `draft`: ты сказал «сразу имплементируй», и код
  написан в этом же прогоне. По экшону агентское требование заводится черновиком, так что это
  отступление намеренное — вычитка за тобой;
- **тултип у пункта меню** — в `MenuButton` добавлено необязательное поле `title`. Строки не
  хватало на два разных ответа: «откуда конфиг» и «чем собирается».

### Проверки

`test` — 93 из 93, `typecheck`, `lint` без ошибок, `format:check`, `build` — зелёные.
Единой команды `check` в репозитории по-прежнему нет.

Сверх проверок — прямой прогон по настоящей карте, мимо живого MCP: он обслуживает `dist`
открытого расширения и новые ветки не видит, пока расширение не перечитают. Прогон показал всё
три случая — метрику по адресу со ссылкой на слой, слой с `index: null` и конфигом, объект
с индексом и без конфига.

### Что осталось человеку

Меню и виртуальный документ работают только после перезапуска расширения — сборка есть, но
запущенный экземпляр держит прежнюю. Результат не коммичен: смотрится диффом.



### Правки

1. Путь до родительской метрики обрезает название метрики, лучше просто замени на "Из прототипа"

2. Давай всё же добавим в модель не только родителя а весь путь. Так как в интерфейсе ты не можешь пройтись по цепочке

3. Такой же путь давай выводить и в мердже объектов. Что бы можно было проследить конфиги + Выводи мердженый конфиг объекта так же как и с метрикой по умолчанию

Обнови требования решение и код
