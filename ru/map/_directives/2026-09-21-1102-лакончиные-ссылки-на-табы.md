
## Сейчас иконка открыть в табе занимает часто место и коратит выравнивание

Давай уберём эту иконку и сделаем просто подчёркивание на ховер, где уместно + ctrl clic что бы работал

Эта функция базовая и привыкнуть к ней легко без доп подсказок

Единственная кнопка которая остаётся на объекте и метриках, там это не очевидно

> [AI] Фаза 1. Сейчас иконка таба выезжает на ховере у пяти видов ссылок: ссылки в значениях
> метрик, хлебные крошки, вкладки объекта, файлы-объекты в дереве и узлы карты детей. Плюс
> две «кнопочные»: в шапке объекта и у заголовка метрики. Предлагаю: иконка остаётся только в
> шапке объекта и у заголовка метрики, у всех пяти ссылок её нет — только ctrl + клик, а
> подсказка «Ctrl + клик — открыть отдельным табом» остаётся в title (места не занимает).
> Вкладки и узлы карты не подчёркиваем — у них уже есть свой ховер. Так?

Да

> [AI] Фаза 2. Без иконки не видно, что откроется табом, а что нет: файл в дереве или ссылка на
> файл по ctrl + клику откроются как обычно, объект — табом. В редакторе VS Code это решено так:
> пока зажат ctrl, то, что откроется иначе, подчёркивается под курсором. Предлагаю так же: при
> зажатом ctrl подчёркивание и курсор-«рука» получают только то, что откроется табом, а при
> обычном наведении всё как сейчас. Нужна такая подсветка по ctrl или хватит подсказки в title?

Давай нативно для vscode сделаем

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — как клиент узнаёт, что ctrl зажат.
> Где: `packages/abstract-react-client/src/features/map-object/ui/` — общий UI карточки объекта.
> Новый хук `useTabModifier` вешается один раз в корне карточки
> (`compose/map-object.tsx`) и ставит на корневой элемент атрибут `data-tab-mod`, пока зажат
> ctrl (или cmd на маке). Ссылки ничего не слушают сами, подсветку даёт один css-класс:
> tailwind `in-data-[tab-mod]:hover:underline` и курсор-«рука».
> Решил сам: состояние берём не только из keydown/keyup, но и из `ctrlKey` на mousemove —
> вебвью без фокуса нажатий не получает, а мышь модификатор несёт. На blur окна атрибут снимается,
> иначе ctrl «залипает» после alt+tab. Сайдбар и таб — разные вебвью, у каждого свой хук.
> Хост без табов хук не включает: подсветка обещала бы то, чего не будет.

Добавь в воркфлоу мити текст в брейншторм и план, что если я запускаю форфлоу без ответа это дальше или ок

> [AI] Правило «запустил без ответа — значит ок» дописал в тексты этапов Брейншторм и План
> (`prototypes/base/_directives.workflow/`). Кусок 1 считаю принятым.
>
> План, кусок 2 — сами ссылки: иконку убираем, подсветку по ctrl включаем. Шесть мест, три модуля:
> - `features/map-object/_metrics/ui/` — отображения значений метрик: ссылки в значениях
>   (`displays.tsx`), ссылки в тексте разметки (`markdown.tsx`), дерево файлов (`file-tree.tsx`);
> - `features/map-object/ui/` — шапка карточки: хлебные крошки и вкладки объекта;
> - `features/map-object/_children-map/ui/` — карта детей: подпись узла (`graph.tsx`).
> Классы подсветки — одна константа рядом с хуком из куска 1, а не строка в каждом файле.
> Решил сам: ссылка, которая откроется табом, подчёркивается и при обычном наведении, и под
> ctrl. Ссылка на файл под ctrl подчёркивание теряет — так видно, что табом она не откроется.
> Вкладки и узлы карты подчёркиваются только под ctrl. Отступы, которые держали место под иконку
> (`pr-4`, `pr-5`), уходят вместе с ней. Подсказка в title остаётся.

> [AI] План, кусок 3 — решение и требование. Сейчас записано: «иконка обязательна, про
> ctrl + клик, которого не видно, никто не узнаёт». Директива это отменяет, значит:
> - `ru/docs/decisions/` — новое решение `0035`: ctrl + клик — базовый жест, у ссылок иконки нет,
>   видно его по подсветке под ctrl, как в редакторе; иконка остаётся в шапке объекта и у заголовка
>   метрики, где жест не очевиден. В старом решении про табы в начало раздела «что можно открыть
>   табом» — строка-цитата, что его переопределило новое, сам текст не трогаю;
> - `ru/requirements/ui/объект/объект-открывается-табом.md` — абзац про иконку переписать под
>   новое правило;
> - комментарии в коде клиента, которые цитируют «иконка обязательна», — поправить заодно с кодом.
> Решил сам: заголовок метрики тоже получает подсветку под ctrl — та же константа, иконка при нём
> остаётся. С решением «действие берут кликом, а не наведением» не спорит: подсветка ничего не
> открывает, а иконки, выезжавшие под курсором, как раз уходят.

> План собран, давай переходить к реализации.
