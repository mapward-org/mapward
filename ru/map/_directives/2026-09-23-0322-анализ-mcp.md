
## 

Проанализируй отчёты по работе mcp все, и посмотри старые идеи улучшения в идеях

И давай подумаем что сейчас можно сделать для его улучшения

mcp для тебя а не для меня. Поэтому я больше прислушиваюсь к тебе чем сам решаю

> [AI] **Фаза 1.** Прочитал все 181 отзыв и раздел идей. Если считать по частоте, главное
> лежит не в самом MCP, а в том, что есть на карте. Тридцать пять отзывов пишут, что объект
> заканчивается на пакете, а дальше агент ищет код grep-ом. Около тридцати говорят, что
> сверку с решениями и требованиями (обратные ссылки, «что решено про X») делают мимо карты.
> Собственно MCP касается вот что: около двадцати пяти отзывов про тред — ответил ли автор,
> что дописано с прошлого прогона, висит ли незакрытый прогон, есть ли перевод строки в
> конце. Около пятнадцати про то, что непонятно, свежая ли сборка отвечает. Меньше — ловушка
> `metrics: []`, раскладка вкладок, падение сервера посреди этапа. Предлагаю так: в этой
> директиве делать только то, что про сам MCP, начиная с треда и свежести сборки. Код внутри
> пакета и решения на карте — это устройство карты, их вынести в отдельные директивы, а
> `ideas.md` переписать по новому счёту. Согласен с такой границей?

Что из этого прям важно, а не просто придирка? 
Что сильно сэкономить ресурсы?

> [AI] **Фаза 1.** Если честно, частота здесь обманывает. «Код внутри пакета» — самая
> частая жалоба, но во многом придирка: grep по папке пакета стоит пару вызовов, и планы всё
> равно получаются. Жалуются потому, что промпт велит «читать карту, а не репозиторий». Так же
> с ловушкой `metrics: []` и раскладкой вкладок: это один лишний вызов.
> По-настоящему важно то, от чего агент **ошибается**, а не просто тратит время:
> (1) отвечает старая сборка, а неизвестный параметр молча проглатывается — агент проверяет
> не то и уверенно делает неверный вывод; (2) реплику в тред я дописываю руками: приклеивается
> к ответу без перевода строки, спорит с автором за файл; (3) обратных ссылок нет — правка
> решения оставляет битые ссылки, и этого никто не видит.
> Сильнее всего сэкономят две вещи: отзыв после каждого прогона (181 штука за три дня,
> половина — «без затруднений») писать только когда что-то мешало, и не отдавать заново
> полный промпт этапа (~5 КБ) на каждом круге, если он не менялся. Предлагаю взять в директиву
> эти пять, остальное записать в `ideas.md`. Так?

Да так, + можно пока выключить отчёты по mcp вернём чуть позже, или оставим только на этап тестирования по всей чесси что бы он вывод сделал

> [AI] **Фаза 3.** Отзыв оставляю только на Тестировании, одним файлом по всей сессии. Теперь
> главная развилка. Чтобы реплику дописывала карта, MCP должен писать в файл директивы. А по
> устройству MCP он только читает: из записей у него есть одна, состояние прогона. Предлагаю
> исключение без нового инструмента: `finish_directive` принимает текст реплики и сам
> дописывает его в конец файла. Перевод строки он ставит сам, и дописывает, а не перезаписывает,
> так что свежий ответ автора не затрёт. Копия директивы, которую сервер и так снимает на
> конце этапа, тогда уже содержит реплику. Руками агент пишет только там, где правит план или
> текст. Согласен расширить «только чтение» ровно на это?

Не понял поясвни подробнее разрешаю отвечать более длинно

> [AI] **Фаза 3, подробнее.** Как сейчас идёт круг Брейншторма. Агент вызывает
> `run_directive`, читает конец директивы, сочиняет реплику и **сам** дописывает её в файл
> своими инструментами. Потом вызывает `finish_directive`, и сервер запоминает, что этап
> кончился, и снимает копию файла.
>
> На ручной записи агенты спотыкаются дважды, и оба раза случалось на этой директиве.
> Первое: ваш ответ кончается без перевода строки. Допиши я реплику командой «добавить в конец
> файла», она прилипла бы к вашей строке, и `> [AI]` перестал бы быть цитатой. Я заметил это
> только глазами. Второе: вы пишете в тот же файл, пока я работаю. Мой инструмент правки видит,
> что файл поменялся, и отказывает, или, хуже, я записываю версию, в которой нет вашей свежей
> строки.
>
> Что предлагаю. Агент не пишет в файл сам, а отдаёт текст реплики серверу: `finish_directive`
> получает ещё один параметр, текст реплики. Сервер в момент вызова сам берёт файл, ставит
> перевод строки и пустую строку, если их нет, и приписывает реплику в самый конец. Всё, что
> выше, он не трогает, поэтому что бы вы ни дописали, пока агент думал, останется.
>
> Почему это вопрос к вам. Сейчас в доке MCP прямо сказано: сервер только читает, а пишет одно —
> состояние прогона. Так решили, чтобы не было двух способов править карту с разными правилами.
> Моё предложение — узкое исключение: сервер только **приписывает реплику в конец** директивы,
> больше ничего. План и правки текста агент по-прежнему делает сам.
>
> Есть вариант подешевле: сервер ничего не пишет, а только говорит при чтении «в конце нет
> перевода строки». Он лечит первую беду, но не вторую. Я за исключение. Разрешаете?

Да это правило не что что бы нужное на практике оказалось. Так что да mcp может записать в конец директивы текст

> [AI] **Фаза 3.** Принято. Следующая развилка — обратные ссылки. Решения, доки и файлы этапов
> — это не объекты карты, поэтому спросить «кто ссылается на решение» у самого файла нельзя.
> Предлагаю метрику на корне «Ссылки». Она проходит по markdown в `ru/docs`, `ru/requirements`,
> этапах, экшонах и памяти. Битые относительные ссылки она показывает красным, и их видит и
> человек, а не только агент. Ещё она отдаёт для каждого файла, кто на него ссылается. Сейчас
> похожая метрика есть только для адресов `mapward://` в требованиях. Агент зовёт её до правки
> решения — узнать, что ещё задеть, — и после неё: проверить, что ничего не разбил. Упоминания
> решений в коде («см. 0022») я бы пока не ловил: это текст, а не ссылка, и там много ложных
> совпадений. Такой охват подходит, или код тоже нужен сразу?
Да добавь метрику

> [AI] понял всё, давай переходить к плану

> [AI] **План, кусок 1 — реплику дописывает `finish_directive`.**
> У `finish_directive` появляется необязательный параметр `reply`, текст реплики. Если он
> передан, сервер перед записью состояния читает файл директивы, добивает конец до пустой
> строки и приписывает текст. Выше конца файла ничего не трогает. Если `reply` нет, всё работает
> как сейчас: этапы, где агент правит текст сам, не ломаются.
> Где правка:
> - `packages/abstract-server/src/entry/mcp.ts` — описание инструментов MCP и разбор вызова:
>   новый параметр в схеме `finish_directive` и передача его дальше.
> - `packages/abstract-server/src/entry/server.ts` — сборка сервера из сценариев, метод
>   `finishDirective` передаёт `reply`.
> - `packages/abstract-server/src/features/map-object/application/use-cases/directives.ts` —
>   сценарии прогона директивы: рядом с `finishStage` встаёт дописывание в конец через порт
>   файлов, который у сервера уже есть. Тесты — рядом, в `directives.test.ts`.
> Решил сам: дописывание идёт до записи состояния, поэтому этап, который помечает директиву
> выполненной, снимает копию уже с репликой. Поправлю себя с Брейншторма: копию снимает только
> такой этап, а не любой. Между чтением и записью файла сервером есть щель в миллисекунды —
> принимаю её, это несравнимо меньше, чем минуты, пока агент думает.

> [AI] **План, кусок 2 — сервер говорит, какая сборка отвечает, и не глотает чужие параметры.**
> Сейчас метрика «Сборка свежее исходников» на корне отвечает, догнала ли сборка на диске
> исходники. Но сама она пишет, что не знает главного: перезагрузили ли окно после сборки. Это
> знает только сам сервер. Поэтому хост при запуске передаёт серверу путь к бандлу, из которого
> тот загружен, и время загрузки. `list_maps` и `run_directive` отдают поле `build`: откуда
> загружен, когда собран (время файла бандла), когда загружен, и `stale: true`, если файл на
> диске уже новее загруженного, то есть собрали, а окно не перезагрузили.
> Второе: неизвестный параметр вызова — отказ с перечнем известных, а не молчание. Проверка одна,
> по схеме инструмента, до разбора вызова.
> Где правка:
> - `packages/abstract-server/src/entry/mcp.ts` — `serveMcp` принимает сведения о сборке,
>   `call` сверяет параметры со схемой, `list_maps` отдаёт `build`.
> - `packages/abstract-server/src/entry/server.ts` — `runDirective` добавляет `build` в ответ.
> - `apps/vscode-extension/src/apps/extension/` — точка входа расширения: передаёт свой бандл.
> - `apps/cli/src/cli.ts` — то же для `mapward mcp`.
> Решил сам: время сборки беру по времени файла бандла, а не вшиваю при сборке — так не
> трогаются конфиги сборки. Метрику `agent-build` не меняю: она про диск, а `build` — про
> процесс, и вместе они дают полную картину.

> [AI] **План, кусок 3 — промпт этапа не повторяется, если не менялся.**
> `run_directive` всегда отдаёт вместе с промптом его короткий отпечаток — поле `promptHash`.
> Агент, у которого промпт уже есть в контексте, при следующем вызове передаёт этот отпечаток
> параметром `known`. Совпал — вместо промпта приходит `unchanged: true`, а отметка о начале
> прогона ставится как обычно. Не совпал или `known` не передан — приходит полный текст. Новая
> сессия ничего не знает и ничего не передаёт, поэтому без промпта остаться нельзя.
> Где правка:
> - `packages/abstract-server/src/features/map-object/domain/prompts.ts` — сборка промпта этапа:
>   рядом с `stagePrompt` встаёт функция отпечатка.
> - `packages/abstract-server/src/entry/server.ts` — `runDirective` сравнивает отпечатки.
> - `packages/abstract-server/src/entry/mcp.ts` — параметр `known` в схеме `run_directive`.
> Решил сам: отпечаток считается по собранному промпту целиком, вместе с общими правилами объекта
> и подставленными путями — поменялось что угодно, и агент получит новый текст. Хэш — простая
> функция без внешних зависимостей: это не защита, а сравнение. Кнопка на карте уходит той же
> фразой, что и раньше, `known` агент решает передавать сам.

> [AI] **План, кусок 4 — метрика «Ссылки» на корне.**
> Новая метрика `links`. Она проходит по markdown в `ru/docs`, `ru/requirements`, `ru/memories`,
> а также по этапам, экшонам и директивам карты, и собирает относительные ссылки
> `[текст](путь)`. Даёт две вещи. Первая — список битых ссылок: файл, строка, куда вела. Есть
> хоть одна — метрика красная, человек видит её на вкладке «Работа с продуктом» рядом с «Битыми
> адресами требований». Вторая — для каждого файла, кто на него ссылается. Это часть значения,
> агент берёт её по ключу перед правкой решения. На экране показывается только список битых
> ссылок, иначе вкладка утонет в перечне.
> Где правка:
> - `ru/map/_metrics/links/` — новая метрика: `config.json` и скрипт-коллектор `links.mjs`.
> - `ru/map/_index.json` — метрика в списке корня и в группе «продукт».
> Решил сам: скрипт сам обходит папки. Штатный обход папок берёт одну корневую папку, а их здесь
> шесть. Корень проекта передаётся скрипту аргументом — через окружение он не доезжает. Ссылки
> наружу (`https://`) и якоря внутри файла не проверяются. Директивы пропускаются, если они уже
> выполнены: их текст — история, и старые ссылки в них битыми не считаются. Отбор по git, как у
> соседних метрик, ставлю тем же штатным шагом.

> [AI] **План, кусок 5 — тексты этапов, доки и решение.**
> Отзыв. Сейчас просьба об отзыве приклеивается ко всем этапам общим правилом прототипа
> `ru/map/prototypes/base/_index.json`. Убираю её оттуда и дописываю в конец этапа
> «Тестирование»: один отзыв на сессию, итогом по всей работе с картой.
> Реплика. В этапах `ru/map/prototypes/base/_directives.workflow/` — Брейншторм и План — «допиши
> блок `> [AI]` в конец файла» меняется на «отдай текст реплики в `finish_directive`». Этапы, где
> правится сам текст директивы, пишут файлом, как раньше.
> Доки инструмента `packages/docs/ru/` — то, что агент получает через `read_docs`:
> - `mcp.md` — строка «писать — ничего» уточняется: сервер дописывает реплику в конец директивы.
>   Там же — поле `build`, отказ на неизвестный параметр и `known` для промпта этапа.
> - `directives.md` — реплика уходит через `finish_directive`.
> Решение `ru/docs/decisions/0039-…` — одно на всю директиву. В нём: зачем серверу разрешено
> дописывать в директиву; что сборку сервер называет сам; зачем промпт не повторяется; зачем
> отзыв только на Тестировании; что ссылки проверяются метрикой. Прежние решения про MCP и про
> отзыв после каждого этапа получают пометку, что это место перекрыто.
> `ru/docs/ideas.md` — раздел об отзывах переписываю по новому счёту: что сделано здесь и что
> осталось (код внутри пакета, поиск по решениям, проверка интерфейса).
> Решил сам: номер `0039`, потому что `0037` и `0038` уже заняты.
> план собран, давай переходить к реализации
