
## 

Хочется перенести в инструмент удобную фичу из редактора

Если файл был вычитан и гит репозитория. Отмечать цветом он был изменён новй или нет. То есть гит статус его прям как в vscode

## Решено

- **механизм** — git отдельно от `StatusMark`: «Да так как не просто цвет, то отдельное поле (там ещё и буква)». У узла заводится своё git-поле, метрика и git за пометку не конкурируют.
- **источник** — «Да давай через ShellPort»: `git status --porcelain` гоняется на сервере, одна реализация закрывает редактор, CLI и MCP.
- **кто считает** — «Возможно нам нужен механизм встроенных трансформов. И этот трансформ принимать будет список файлов и возвращать с результатами гита + у трансформа свой вотчер и тд».
- **кэш и свежесть** — «Давай просто тут мы staleTime дропнем. И добавим в коллект функцию вотчинга с дебаунсом как общий механизм». Дальше автор: «Это новоре требование и правки доков».
- **где показывать** — «Да давай везде»: всюду, где у узла есть `link` — файлы, списки, ссылки, требования, доки, директивы, экшоны.
- **чем показывать** — «Цвет + буква да. Сохраняем тот же язык»: цвет имени и буква справа, как в vscode; точка `StatusDot` остаётся метрике.
- **папки** — «Да агрегировать»: изменение внутри подсвечивает папку, иначе свёрнутое дерево его прячет.
- **набор статусов** — «Ок Средний»: `M`, `A`, `D`, `U`, `C`, без разделения staged и unstaged.
- **буквы из порцелейна** — «Реши сам, что бы не ломать привычные паттерн работы с vscode»: правило перевода `XY` и `??` в одну букву выбирается при реализации, ориентир — привычное поведение редактора.
- **обновление** — вотчер на `.git/index` и `.git/HEAD` с дебаунсом ~300 мс и подписка в клиент такая же, как у метрик: «Да». Вотчер живёт у встроенного трансформа.
- **репозитории и файлы вне git** — «Да»: корень репозитория ищется вверх по пути и кэшируется по корню; файл вне git и отсутствующий git — без пометки.
- **упавший вызов git** — «Молча но в логи запиши».
- **MCP** — «Да правда, это есть в метрике! Поэтому это и м mcp всё нормально»: git приезжает в `read_object` внутри значений метрик, отдельного канала не заводим.
- **игнорируемое серым** — «Отдельная директива потом».

## Ход обсуждения

Началось с простого переноса: раскрасить имена файлов, как это делает vscode. Первый круг
ушёл на то, чтобы не смешать git с `StatusMark` — там уже сидит статус метрики, и две пометки
про разное не должны затирать друг друга.

Ответ «дисплей просто принимает цвет» это смешение вернул: цвет у узла один и уже занят.
Развернулись на втором круге — у git своё поле, потому что показывать надо не только цвет,
а ещё и букву.

Второй разворот — про то, кто git считает. Сначала прозвучало «трансформ без кэша или прямо
в collect», но collect упирается в `collectorsStaleTime`: вшитый в значение git обновлялся бы
по расписанию метрики, а не по сохранению файла. Отсюда вышли на встроенные трансформы —
со своим вотчером и со сброшенным staleTime; это новое требование, и доки под него правятся.

Третий круг — границы. «Везде, где `link`» при git-в-коллекторе означало бы, что каждый чужой
скрипт сам зовёт git; встроенный трансформ это снимает. Вопрос про MCP закрылся сам собой:
git лежит в значении метрики, значит в `read_object` он уже есть.

Из побочного: игнорируемое серым признано хорошей идеей, но вынесено в отдельную директиву —
`--ignored` дорог, а `exclude` у `read-dir` и так прячет почти всё.

Сжато до коммита `5ff77d8`.

## Открытые вопросы

Не осталось.

## План

Второй круг: переписан по ответам автора. Git — шаг `transforms` с именем `kind: "git-status"`,
а не наложение поверх значения; отсюда логи, кэш и свежесть считаются по стадии трансформа.

### 1. Встроенный шаг — третий вид трансформа

Тело в инструменте, конфиг называет его именем и ничего не описывает командой. Универсально:
шаг принимает данные любой формы дисплея, поэтому встаёт после любого коллектора.

- `packages/abstract-server/src/features/map-object/application/use-cases/transform.ts:26` —
  в `switch` третий case: `kind` ищется в реестре встроенных, найден — зовётся его тело.
  `default:50` остаётся прежним, и неизвестный `kind` по-прежнему ошибка.
- `packages/abstract-server/src/features/map-object/application/services/builtin.ts` — новый:
  реестр встроенных шагов. У шага `run(input, deps)` и необязательный
  `watch(input, deps, onChange)` — тем и отличается от `script`, что умеет сказать, когда его
  ответ протух.
- `packages/core/src/model/schema.ts:50` — `transforms` не меняется: `Record<string, unknown>`
  принимает `{ "kind": "git-status" }` как есть. Нового поля в `MetricConfig:35` не заводим —
  включение теперь строкой в конфиге метрики.
- `packages/abstract-server/src/features/map-object/application/use-cases/transform.ts:55` —
  `transform` получает реестр аргументом: звать его напрямую нельзя, состояние шага живёт
  у сервера, а не у функции.

### 2. Git-состояние: откуда берётся и когда протухает

Состояние по корню репозитория, а не по метрике: один `git status --porcelain` отвечает всем
метрикам всех объектов этого репозитория.

- `packages/abstract-server/src/features/map-object/domain/git.ts` — новый. `parsePorcelain(text)`
  → `Map<относительный путь, буква>`; правило перевода `XY` и `??` в одну из `M A D U C`
  (таблица — в тесте, блок 7). Чистая функция, платформы не знает.
- `packages/abstract-server/src/features/map-object/application/services/git-status.ts` — новый.
  `createGitStatus(ports)`: `rootOf(path)` — корень, кэш по папке; `statusOf(root)` — прогон
  и кэш по корню; `watch(root, onChange)` — вотчер на `.git/index` и `.git/HEAD`, дебаунс
  300 мс, счётчик подписчиков на корень.
  Корень ищется `git rev-parse --show-toplevel` через `ports.shell.run`
  (`packages/abstract-server/src/ports/index.ts:32`): подъём по файлам не видит ни сабмодуля,
  где `.git` это файл, ни worktree. Пустой ответ — корня нет, и это не ошибка.
- `packages/abstract-server/src/lib/debounce.ts` — новый, на `ports.timers.after`
  (`packages/abstract-server/src/ports/index.ts:61`): своих таймеров у сервера нет.

  Или с помощью rxjs


- `packages/abstract-server/src/ports/index.ts:27` — `watch(root, onChange, options?: { include?: string[] })`.
  Сейчас паттерн зашит в реализацию, и `.git/index` под `**/*.{json,md}` не подходит вовсе.
- `apps/vscode-extension/src/apps/extension/ports/index.ts:69` — паттерн из `options.include`,
  умолчание прежнее.
- `apps/cli/src/ports.ts:57` — то же фильтром по имени.
- `LogPort` не заводим: упавший git — это лог трансформа, у него своё место (блок 4).

### 3. Тело шага: где у узла появляется буква

- `packages/core/src/model/display.ts:14` — `GitMark = { git?: "M" | "A" | "D" | "U" | "C" }`
  рядом со `StatusMark`, примешать в `LinkNode:17` и `TreeNode:19`. Строкой, а не объектом:
  через MCP каждый узел каждого дерева отдаётся целиком, расшифровку для тултипа знает клиент.
- `packages/core/src/model/display.ts:42` — `SHAPES` не трогаем: поле проставляет шаг,
  и агенту, пишущему трансформ, называть его незачем.
- `packages/abstract-server/src/features/map-object/application/use-cases/git-mark.ts` — новый,
  тело шага. `markGit(data, lookup)`: обходит `children` дерева, `items` списка, узел `link`
  и `nodes` карты; у узла с файловым `link` ставит букву, папке — агрегат по потомкам без
  буквы (цвет считает клиент). Чистая функция: вход — данные предыдущего шага и справочник.
- Сравнение путей: `link` абсолютный, порцелейн даёт относительный от корня и в кавычках,
  когда в имени не-ascii; обе стороны приводятся к прямым слэшам и нижнему регистру диска.

### 4. Стор: свежесть, вотчер и кэш

Шаг с состоянием ломает три умолчания стадии трансформа — каждое надо снять явно.

- `.../services/metric-store.ts:74` — `createMetricStore` поднимает реестр встроенных шагов
  и отдаёт его в `transform`.
- `.../metric-store.ts:123` — `stale` для стадии трансформа возвращает `true`, когда среди
  шагов есть встроенный с состоянием: `transformsStaleTime` на него не действует, свежесть
  у него от `.git`, а не от часов. Это и есть «staleTime дропнем».
- `.../metric-store.ts:130` — `runOnce` принимает, что форсить: сейчас `force` форсит обе
  стадии, а тик вотчера должен гонять только трансформ. Путь уже есть —
  `.../metric-store.ts:171` умеет трансформ на готовых данных, ему нужен только флаг.
- `.../metric-store.ts:269` — в `watch` подписка поднимает вотчеры встроенных шагов каждой
  метрики объекта; тик → `start` только стадии трансформа, рядом с таймерами интервала
  (`.../metric-store.ts:301`), и гасится там же, где они.
- `.../metric-store.ts:344` — `read` не меняется: с `refresh: "on-display"` трансформ идёт
  своим чередом, с `none` отдаётся то, что в памяти.
- `.../use-cases/transform.ts:106` — `transformsCache` игнорируется, когда в шагах есть
  встроенный с состоянием: git в `transform.json` — дифф в репозитории на каждый взгляд
  на карту. Причина уходит в `transform.logs.json`, чтобы молчание не выглядело поломкой.
- `.../use-cases/transform.ts:86` — упавший git метрику не роняет: шаг возвращает вход без
  пометок и пишет причину в лог. В отличие от `script`, где ошибка — это провал трансформа:
  здесь не сошлась пометка, а не данные.

### 5. Клиент: цвет имени и буква справа

- `packages/abstract-react-client/src/features/map-object/_metrics/pure-model/display.ts:16` —
  рядом с `KNOWN` цвета git темой редактора: `gitDecoration.modifiedResourceForeground`
  и соседние. `statusColor:24` не трогаем — он про точку метрики.
- `.../ui/git-mark.tsx` — новый: буква справа, цветом статуса, с расшифровкой в `title`.
- `.../ui/file-tree.tsx:32` — имя красится цветом git, `:34` — `GitMark` рядом со `StatusDot`.
  Папка красится, буквы не получает.
- `.../ui/displays.tsx:7` — то же в `Link`: список, ссылка и карта детей получают букву тем же
  компонентом.

### 6. Требования и доки

- `ru/requirements/метрики/трансформ-бывает-встроенным.md` — новое: третий вид шага, тело
  в инструменте, своё состояние и свой вотчер, `staleTime` и кэш на него не действуют.
- `ru/requirements/метрики/файлы-показывают-статус-git.md` — новое: `git-status` в `transforms`
  даёт цвет и букву везде, где у узла файловый `link`; папка красится без буквы; файл вне git
  и отсутствующий git — без пометки; упавший git молчит и пишет в лог трансформа.
- `packages/docs/ru/metrics.md:78` — в «Трансформы» третий вид рядом со `script` и `prompt`,
  с оговоркой про свежесть и кэш.
- `ru/docs/development-guide/metrics.md` — то же для того, кто пишет метрику: одна строка
  в `transforms`, и почему её ставят на метрики с файлами, а не на все подряд.
- `packages/docs` — пересобрать: текст едет в инструмент через `scripts/bundle-docs.mjs`.

### 7. Тесты

- `packages/abstract-server/src/features/map-object/domain/git.test.ts` — новый: таблица
  `XY` → буква, включая `??`, `R `, ` M`, `MM`, `UU`, и путь в кавычках с не-ascii.
- `.../use-cases/git-mark.test.ts` — новый: дерево, список, узел `link`, карта детей;
  агрегат на папке; узел без `link` и узел с `mapward://` не трогаются.
- `.../use-cases/transform.test.ts` — новый: встроенный шаг зовётся по `kind`; упавший git
  отдаёт вход без пометок и пишет лог; `transform.json` при нём не пишется даже с
  `transformsCache: true`.
- `.../services/metric-store.test.ts` — дописать: метрика с `git-status` не считается свежей
  по `transformsStaleTime`, а тик вотчера гонит трансформ, не трогая коллекторы.
- `packages/abstract-server/src/lib/debounce.test.ts` — новый: серия тиков даёт один вызов.

### Решение 0023 — встроенный трансформ и git-статус на узлах

- `ru/docs/decisions/0023-builtin-transforms-and-git.md` — новое. Причина: пометка про файл
  не собирается коллектором (каждый чужой скрипт звал бы git сам) и не считается трансформом
  из конфига (`script` без состояния не знает, когда его ответ протух). Отсюда третий вид
  шага — тело в инструменте, своё состояние, свой вотчер.
- Там же — почему включается строкой `{ "kind": "git-status" }`, а не действует всегда:
  шаг универсален и ставится к любому коллектору, а «всегда и везде» означало бы поведение,
  которого в конфиге не видно.
- Там же — почему git не `StatusMark` (`packages/core/src/model/display.ts:14`): цвет у узла
  один и занят статусом метрики, а показать надо цвет и букву.
- Там же — почему шаг с состоянием отменяет `transformsCache` и `transformsStaleTime`.
- Инвариант: в `transform.json` поля `git` нет ни у одной метрики.

## Порядок

1. Блок 1 — реестр и третий case: без него шаг некуда положить.
2. Блок 2 — состояние и порты: вотчер и корень репозитория.
3. Блок 3 — тело шага: чистая функция, проверяется тестом отдельно от стора.
4. Блок 4 — стор: здесь всё сходится, и здесь же три снятых умолчания.
5. Блок 5 — клиент.
6. Блок 7 — тесты идут рядом со своими блоками, здесь собраны списком, а не отложены.
7. Блок 6 и решение — после того, как поведение устоялось.

## Не делаем

- Игнорируемое серым — отдельная директива, как решено в обсуждении: `--ignored` дорог.
- Разделение staged и unstaged — набор статусов «средний», две колонки порцелейна схлопываются
  в одну букву.
- Ветка, отставание от `origin`, стеш — это про репозиторий, а не про узел.
- Изменение `read_object`: git приезжает внутри значений метрик, отдельного канала не заводим.
- Вторая встроенная реализация в довесок к git: реестр заводится на один шаг, и обобщать его
  до того, как появится второй, нечем.

## Стоит учесть

- Шаг ставится строкой в конфиг, поэтому на своей карте пометки появятся там, где её допишут:
  общие метрики в `mapward://shared-metrics/files` закрывают файловые деревья всех пакетов
  разом, а метрики чужих карт останутся без пометок, пока их не научат.
- Вотчер `vscode` вне папки воркспейса работает, но `.git` у многих исключён настройкой
  `files.watcherExclude`; если пометка не обновляется, причина будет здесь, а не в дебаунсе.
- `git status --porcelain` на большом репозитории идёт сотни миллисекунд. Один прогон на корень
  и кэш по корню это закрывают, но карта с объектами из разных репозиториев поднимет столько
  прогонов, сколько корней.
- Номер 0021 в `ru/docs/decisions/` пропущен — следующий свободный 0023, а не 0021.
- Метрика с `git-status` пересчитывает трансформ на каждое открытие объекта: свежести у неё
  нет по решению. Стадия сбора при этом свежесть спрашивает как раньше, и `read-dir` заново
  не ходит.
- Дока едет в инструмент сборкой (`packages/docs/scripts/bundle-docs.mjs`), а живой MCP
  обслуживает уже собранный `dist` расширения: до пересборки `read_docs` отдаёт прежний текст.

## Уточни

> [AI] Включение: `git: false` выключает наложение, а по умолчанию оно включено на каждой
> метрике, у чьих узлов есть файловый `link`. Предлагаю так: «да давай везде» иначе означает
> «допиши строку в конфиг каждой метрики каждой карты». Альтернатива — `kind: "git-status"`
> руками в `transforms`, и тогда чужая карта пометок не увидит, пока её не научат.

Давай kind: "git-status" всё же, это универсально, и можно с разными коллектами использовать

> [AI] Упавший git: предлагаю новый `LogPort` в `ports/index.ts:75` — `OutputChannel`
> в редакторе, `console.error` в cli. Альтернатива — писать в `collect.logs.json` той метрики,
> что ждала пометку, но сбой общий для репозитория, и лог попал бы в случайную метрику.

ТАк как это транформ для них есть 

> [AI] Папка при агрегации: предлагаю красить имя и буквы не ставить — так и в редакторе.
> Приоритет цвета сверху вниз: `C` конфликт, `D` удалён, `A` добавлен, `M` изменён, `U` не
> отслеживается. Альтернатива — буква самого сильного потомка на папке, но тогда свёрнутая
> папка читается как изменённый файл.

Да папку красим без буквы

> [AI] Форма поля: предлагаю `git?: "M" | "A" | "D" | "U" | "C"` строкой на узле. Объект
> `{ status, hint }` весил бы втрое больше в каждом узле каждого дерева, а через MCP это
> отдаётся целиком; расшифровку для тултипа знает клиент.

да давай

### Второй круг

> [AI] Кэш: метрика с `git-status` не пишет `transform.json`, даже если `transformsCache: true` —
> иначе git уезжает в репозиторий диффом на каждый взгляд на карту. Предлагаю игнорировать флаг
> молча для интерфейса и с причиной в логе. Альтернатива — считать такую пару ошибкой конфига
> и краснить метрику, но тогда `git-status`, дописанный к метрике с кэшем, ломает её вместо
> того, чтобы работать.

Давай не игнорировать. Просто кто такую комбинацию поставит, сразу выключит из-за шума

> [AI] Где ставим шаг на своей карте: предлагаю в `mapward://shared-metrics/files` — тогда
> пометки приезжают во все файловые деревья пакетов разом, без правки каждой метрики.
> Метрики с другими коллекторами (требования, доки, директивы) — отдельным проходом, когда
> станет видно, что там пометка читается, а не шумит.

Думаю везде полезно будет, не только в files проставь сразу я выключу где не надо
