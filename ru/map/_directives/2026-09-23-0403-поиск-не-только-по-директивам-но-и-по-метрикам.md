
## 

Сделать просто общий поиск по всей вкладке вместо отдельного на директивы (useDefferedValue не забыть)

Я про об объекте вкладке

Кстати табы смены режима отображения, можно рядом с кнопками истории поставить для экономии пространства

> [AI] Фаза 1. Сейчас на вкладке «об объекте» лупа есть только у раздела «Директивы», а рядом ещё «Объект» (поля), «Метрики», «Этапы директив» и «Экшоны». Предлагаю так: одно поле поиска над всей вкладкой, стоит всегда, а не по лупе, — раз поиск теперь по всему экрану, прятать его незачем. Набранное фильтрует строки в каждом разделе по тому, что в строке видно: ключ и значение поля, ключ и подпись метрики, имя этапа, экшона, название и дата директивы. Раздел, в котором ничего не нашлось, прячется целиком; не нашлось нигде — одна строка «не нашлось». Фильтр идёт через `useDeferredValue`, чтобы набор не тормозил на длинных списках. Так — или поиск должен не фильтровать, а, например, подсвечивать и прокручивать к найденному, или искать глубже видимого текста (внутри конфигов, текстов директив)?

Да всё так

> [AI] Фаза 1. Теперь вкладки «метрики / об объекте / прогоны». Сейчас они занимают свою строку под заголовком, а строка истории выше — это стрелки «← →» и хлебные крошки до корня. Предлагаю поставить вкладки в строку истории сразу за стрелками, перед крошками: так они стоят на одном месте на любом объекте, а длинные крошки переносятся после них и их не двигают. Чтобы строка не распухла, вкладки там — только иконки, название во всплывающей подсказке, открытая подсвечена фоном. Так, или названия у вкладок оставить (хотя бы у открытой), или поставить их в правый край строки?

Там места предостаточно

Можешь даже иконки стрелок побольше сделать

> [AI] Фаза 2. Принято: вкладки с названиями встают в строку истории за стрелками, стрелки крупнее. Теперь про то, сколько живёт набранный запрос. Сейчас запрос в директивах сбрасывается, когда поле закрыли, и никуда не сохраняется. Для общего поиска предлагаю так: запрос держится, пока открыт вид «об объекте», в том числе при переходе на другой объект — тогда можно пройти по соседям и на каждом сразу видеть ту же метрику или поле. Сбрасывается он при уходе на «метрики» или «прогоны» и по Escape; в файлы и между перезапусками не сохраняется. Так, или на новом объекте начинать с пустого поля?

Давай проще, да с пустого поля каждый раз начинаем

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — правило совпадения, чистой функцией без React.
>
> Где: `packages/abstract-react-client/src/features/map-object/pure-model/` — чистые правила объекта, которыми пользуются и экраны, и тесты. Сейчас там `directives.ts` с `matchDirectives`: подстрока без регистра, дефисы и подчёркивания читаются пробелами, дата в имени тоже ищется. Это правило я выношу в общее `matches(query, ...тексты)` рядом, а `matchDirectives` становится его частным случаем — чтобы директивы и остальные разделы искались одинаково.
>
> `features/map-object/_meta/pure-model/meta.ts` — правила мета-экрана. Сюда добавляю `searchMeta(object, query)`: отдаёт по каждому разделу только совпавшие строки. Что с чем сравнивается — ровно то, что видно в строке:
> - «Объект»: имя объекта; строки «адрес» и «на диске» и каждое поле — ключ и значение;
> - «Метрики»: подпись и ключ (ключ виден, когда подписи нет, но ищется всегда — по нему метрику зовут);
> - «Этапы директив»: имя этапа; «Экшоны»: подпись и ключ; «Директивы»: имя файла целиком, с датой.
>
> Подсказки справа («из прототипа», «по умолчанию») и всплывающие описания не ищутся: это не название, и на запрос «прото» совпало бы полкарты. Пустой запрос отдаёт всё как есть. Тесты — рядом, как у `directives.test.ts`.

> [AI] План, кусок 2 — поле поиска на вкладке «об объекте».
>
> `features/map-object/_meta/` — мета-экран. В `compose/meta-screen.tsx` живёт запрос: состояние поля и `useDeferredValue` над ним, так что поле откликается на каждую букву, а фильтр по разделам догоняет, когда есть время. Отфильтрованное считает `searchMeta` из первого куска. В `ui/meta-view.tsx` — само поле: над разделами, вне прокрутки, чтобы не уезжало, когда листаешь длинный архив директив. Раздел без совпадений не рисуется; не совпало нигде — одна строка «не нашлось». Escape очищает поле. Фокус поле само не забирает: вкладку открывают и чтобы просто посмотреть.
>
> `features/map-object/_directives/compose/directive-section.tsx` — раздел директив. Своя лупа, своё поле и свой запрос из него уходят: он получает уже отобранный список и только рисует его. Поэтому раздел директив считается тем же фильтром, что и остальные, а не вторым поиском.
>
> `features/map-object/compose/map-object.tsx` — сборка объекта. Мета-экран получает ключом адрес объекта: переход на другой объект, в том числе стрелкой истории, пересоздаёт его, и поле начинается пустым — как решили. Уход на другую вкладку вида тоже сбрасывает его сам, потому что экран снимается.

> [AI] План, кусок 3 — вкладки видов в строке истории и крупные стрелки.
>
> `features/map-object/ui/` — простые компоненты объекта без своей логики. В `breadcrumbs.tsx` строка получает место под вкладки сразу за стрелками «← →», перед крошками. Сами вкладки строка не знает — ей их передают готовыми, как сейчас передают стрелки: крошки остаются про путь, а не про виды. Стрелки крупнее: знак примерно вдвое больше и площадь нажатия шире, чтобы в них попадать не целясь. Сейчас вся строка приглушена полупрозрачностью — теперь приглушаются только крошки, иначе открытая вкладка выглядела бы погасшей. В `view-tabs.tsx` вкладки теряют свою отдельную строку с отступами и становятся просто рядом кнопок с названиями и иконками, как сейчас.
>
> `features/map-object/compose/map-object.tsx` — сборка объекта: вкладки вида переезжают из-под шапки внутрь строки истории, их прежняя строка исчезает. На экране метрики во весь таб строки истории нет и сейчас — там ничего не меняется.
>
> План собран, давай переходить к реализации.
