
## 

Вызов следующего этапа удобнее всего было бы сделать прямо в файле директивы а не на карте!

> [AI] Фаза 1. Где именно в файле должна быть кнопка? Сейчас кнопка этапа на карте отправляет
> в сессию объекта ту же фразу «выполни этап…», и её можно переиспользовать как есть. Вопрос в том,
> где кнопка стоит. Предлагаю строку-ссылку (CodeLens) внизу файла, прямо под последним ответом:
> «▶ План · Брейншторм ещё круг · …». Дописал ответ и тут же нажал, не уходя к началу файла. Другой
> вариант — иконка в заголовке вкладки редактора. Или нужны оба?

Обе будут просто огонь. Тут ещё нужно сразу продумать как искать нужный терминал в который это нужно вставить! Потому что сейчас переключаться приходится

> [AI] Фаза 1. К какому терминалу привязать кнопку? Сейчас выбор идёт по объекту: активный терминал
> объекта, а если активен чужой — первый из открытых. При двух сессиях на объекте фраза легко уезжает
> не туда. Предлагаю привязать директиву к терминалу, в котором её последний раз запускали: там уже
> лежит контекст этого треда. Если он закрыт — активный терминал объекта, если терминалов нет —
> завести новый. Терминалы, открытые руками мимо карты, расширение не знает, их не ищем. Так подойдёт?

Да так отлично!

> [AI] Фаза 1. Какие этапы показывать на кнопках? В начале директивы написано «следующего этапа», но
> следующий не всегда один: после круга брейншторма это либо ещё круг, либо План. Предлагаю сделать
> первой кнопкой «ещё круг» того этапа, что шёл последним, второй — следующий по порядку, а остальные
> этапы убрать в «…». Если директиву ещё не запускали, первой идёт первый этап. Если директива
> выполнена, кнопок нет. Или показывать все этапы в ряд, как на карте?

Вообще можно просто подставить все элементы воркфлоу. По факту многоэтапность воркфлоу это запуск одного этапа несколько раз, не думаю что есть смысл под это отдельный механизм карты делать. 

Короче "Не следущий флоу" а просто "Все флоу

> [AI] Фаза 2. Куда переводить фокус после нажатия? Сейчас кнопка на карте показывает терминал и
> забирает фокус в него. Агент при этом отвечает в тот же файл директивы, где вы и так сидите. Предлагаю
> показать терминал в панели, чтобы было видно, что агент работает, а курсор оставить в файле: ответ
> агента появится там, и вы допишете свой, никуда не переключаясь. Перед отправкой файл сохраняется,
> иначе агент прочитал бы с диска старую версию. Или удобнее, чтобы фокус уходил в терминал?

Да я даже не думал об этом! Хорошая идея. Главно что бы прямо запускался терминал. Сейчас кода нажимается директива она вставляется но не запускается. Учти это

> [AI] Фаза 3. Как узнавать терминал, если этап запустили словами, а не кнопкой? Договорились слать
> фразу туда, где директиву запускали последний раз. Но сейчас расширение знает это только про запуски
> кнопкой. Когда вы пишете «выполни этап…» руками, как в этом треде, карта видит прогон и не видит,
> из какого он терминала. Предлагаю, чтобы расширение, открывая терминал, вписывало его номер в адрес
> MCP-сервера. Тогда каждый вызов этапа приходит уже с номером, и привязка работает при любом способе
> запуска. Или хватит того, что помнятся только запуски кнопкой?

Да давай просто запуск кнопкой. Так как я на практике текстом ни разу не запускал, даже с текущими неудобными кнопками)

> [AI] Фаза 4. Это расходится с решением о кнопках этапов. Там записано, что получателя выбирает
> карта по объекту: активный терминал объекта, потом первый живой, потом новый. И что кнопка
> терминалами не владеет, а только пользуется. Мы же вводим память «директива → терминал её последнего
> запуска», пусть и маленькую. Предлагаю завести новое решение: кнопки этапа есть и в файле директивы,
> получатель — терминал, где эту директиву последний раз запускали кнопкой, остальное как было. В старом
> решении у абзаца про получателя будет пометка, что его перекрыли. Заводим так?

Да давай

> [AI] понял всё, давай переходить к плану

> [AI] План, кусок 1 — выбор терминала.
> Сейчас получателя выбирает `pickSession` в модуле терминалов расширения: активный терминал объекта,
> иначе первый живой. Расширение уже помнит, какой этап в какой терминал ушёл, но только до конца
> этапа: по `finish_directive` запись стирается, потому что нужна она лишь для имени вкладки.
> - Рядом заводится вторая запись: «объект + директива → терминал последнего запуска кнопкой». Она
>   не стирается по концу этапа, иначе второй круг брейншторма её бы уже не застал. Ключ — адрес
>   объекта вместе с именем файла, потому что у разных объектов имена директив совпадают.
> - Выбор становится таким: терминал этой директивы, если он жив, потом активный терминал объекта,
>   потом первый живой, а если нет ни одного — новый. Правило выносится чистой функцией рядом с
>   правилами имён вкладок и покрывается тестами. `pickSession` остаётся для кнопки «терминал» на
>   объекте, где директивы нет.
> - Память живёт в процессе редактора: перезапустил VS Code — терминалы всё равно закрылись, и
>   помнить их незачем. На диск ничего не пишется.
> - Сама отправка («кнопка этапа», сейчас лежит в обработчике моста для карты) выносится в
>   отдельную функцию. Её зовут и карта, и новые кнопки в файле: одна логика, два входа.

Перепиши пункт плана, но сориентируй в каких модулях собираешься править, что бы я мог посмотерь. То есть ссылки на код можно, но скорее на основные структурные модули

> [AI] План, кусок 1, вторая версия — выбор терминала. Вся правка в расширении, в двух модулях.
>
> **Фича терминалов** — [features/terminals](../../../apps/vscode-extension/src/features/terminals/).
> Она владеет живыми сессиями и отвечает на вопрос «куда слать».
> - В `pure-model/` (там уже лежат правила имён вкладок) — новое чистое правило выбора: терминал
>   этой директивы, если жив, потом активный терминал объекта, потом первый живой, потом «заводи
>   новый». С тестами рядом.
> - В `adapters/terminals.ts` — запись «объект + директива → терминал последнего запуска кнопкой».
>   Ключ — адрес вместе с именем файла: у разных объектов имена директив совпадают. По концу этапа
>   не стирается, живёт в памяти редактора, на диск не пишется. Старый `pickSession` остаётся для
>   кнопки «терминал» на объекте, где директивы нет.
>
> **Сборка расширения** — [apps/extension](../../../apps/vscode-extension/src/apps/extension/).
> Сейчас отправка этапа сидит в обработчике моста для карты (`bridge-handler.ts`), а память о
> запущенном этапе для имени вкладки — в `stage-tabs.ts`. Отправка выносится оттуда в отдельный
> файл этого же модуля: её зовут и карта, и новые кнопки в файле директивы. Здесь, а не в фиче
> терминалов, потому что ей нужны и сервер карты, и терминалы, а сводить фичи — работа этого слоя.

Ок. 

То что я дописал выше про план, добавь в базовый промпт directive.workflows плана
Да это другая директива но для этого заводить не хочется. + Немного подправь последнее решение по этому поводу

> [AI] Сделал: в промпте Плана (`prototypes/base/_directives.workflow/план.md`) новый абзац «Кусок
> говорит, где правка», в решении о директиве-треде — абзац о том же с причиной, в гайде по директивам —
> полстроки. Всё это лежит незакоммиченным рядом с планом, смотрится диффом.

> [AI] План, кусок 2 — отправка фразы: чтобы запускалась сразу и не уводила фокус.
>
> **Фича терминалов** — [features/terminals](../../../apps/vscode-extension/src/features/terminals/),
> файл `adapters/terminals.ts`. Отправка в живую сессию и переименование вкладки лежат здесь.
> - Почему не запускается: фраза уходит одним куском вместе с Enter, и агент принимает её как
>   вставку из буфера, где Enter — просто перенос строки внутри поля ввода. Отправка разделится на
>   два шага: сначала текст без Enter, после короткой паузы отдельный Enter. Так агент видит нажатие
>   клавиши, а не часть вставки. Паузу подберу на Реализации, проверив руками.
> - Фокус: терминал показывается, но фокус не забирает. Это касается и отправки, и переименования
>   вкладки на время этапа. Переименование работает только над активным терминалом, поэтому терминал
>   делается активным в панели, но курсор остаётся там, где был.
> - Сам решил: так же ведёт себя и кнопка на карте. Логика отправки у двух входов одна, и
>   фокус, уезжающий из карты в терминал, мешал бы там так же.
> - Новый терминал (когда живого нет) по-прежнему запускает агента с фразой внутри промпта. Эта
>   дорога работает, её не трогаю.

> [AI] План, кусок 3 — кнопки в самом файле директивы. Кусок 2 принял как «ок» по вашему «дальше».
>
> **Новая фича** — `features/directive-buttons/` рядом с
> [features/terminals](../../../apps/vscode-extension/src/features/terminals/). Отвечает за то, чтобы
> открытый файл директивы узнавал себя и показывал этапы.
> - Чистое правило: по пути открытого файла найти в карте объект, у которого эта директива есть, и
>   взять его этапы. Карта это уже знает: у объекта есть список директив с путями и воркфлоу по
>   порядку. Файл не из `_directives` ни одной карты — кнопок нет.
> - Строка ссылок внизу файла: по ссылке на каждый этап воркфлоу, все подряд, как договорились.
>   Показывается всегда, в том числе у выполненной директивы: перезапуск — тоже решение автора.
> - Иконка в заголовке вкладки: у редактора там нельзя поставить переменное число кнопок, поэтому это
>   одна кнопка «этап…», которая открывает список этапов.
> - Перед отправкой файл сохраняется, если в нём есть несохранённое.
>
> **Сборка расширения** — [apps/extension](../../../apps/vscode-extension/src/apps/extension/) и
> `package.json` расширения: регистрация кнопок и команды, вызов общей отправки из куска 1. Сейчас
> расширение просыпается только при открытии карты. Добавится пробуждение на открытие markdown,
> иначе у директивы, открытой раньше карты, кнопок бы не было.

> [AI] План, кусок 4 — решение и доки. Кода нет, только тексты. Кусок 3 принят словами в чате.
>
> **Решения** — [ru/docs/decisions](../../docs/decisions/). Новое `0032`: у директивы кнопки этапов
> есть и в её файле (ссылки внизу и кнопка в заголовке), получатель фразы — терминал, где эту
> директиву последний раз запускали кнопкой, а после него прежний порядок. Там же: фокус остаётся
> где был, и Enter уходит отдельно от текста. В решении о воркфлоу директив, в абзаце «Получателя
> выбирает карта…», в начале появится цитата, что его перекрыло `0032`. Текст решения — черновик от
> меня, правите вы.
>
> **Дока инструмента** — [packages/docs/ru](../../../packages/docs/ru/), раздел `directives`: сейчас там
> написано «сессий несколько — фраза уходит в активную». Правлю на новый порядок и добавляю, что
> кнопки есть и в файле директивы. Это то, что агенты получают через `read_docs`.
>
> **Реестр требований** — [ru/requirements/директивы](../../requirements/директивы/). Про кнопки этапов
> там требования нет. Сам решил: не завожу, потому что кнопка по старому правилу не делает ничего, чего
> нельзя словами, и требованием остаётся «директива выполняется этапами».
>
> **Архитектура расширения** — `ru/docs/architecture/vscode-extension.md`: строка о новой фиче
> `directive-buttons` в списке фич.
>
> Кусок последний. План собран, давай переходить к сжатию.