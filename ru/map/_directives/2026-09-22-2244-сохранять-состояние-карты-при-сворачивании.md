
## 

Сейчас при работе с двумя картами, сворачивание карты сбрасывает состояние


Давай сделаем так. Карта инициализируется при первом открытии но при сворачивании не размонтируется

+ AI такой баг вывел!

Ни одной задачи ещё не поставлено. Работать с «Mapward (ru)» через MCP сейчас не получится: подключённый сервер отдаёт другую карту.


```
- list_maps возвращает одну карту, my-space (d:\my-space\my-space-map). Карты d:\my-space\mapward\ru\map в нём нет, поэтому read_object и run_directive уходят в чужую карту: там акторы и репозитории Playerok.
- Похоже, MCP поднят из окна VS Code, где открыта карта my-space, а карта mapward ни в одном окне не открыта. Сервер живёт, пока открыта его карта.
```

> [AI] Фаза 1. Здесь две разные проблемы, и я предлагаю эту директиву оставить про одну.
> Первая — сворачивание: в окне с двумя картами (`d:\my-space`, где в `mapward.json` и my-space,
> и mapward) каждая карта — секция-аккордеон, и свёрнутая секция сейчас просто удаляется из
> дерева, а с ней уходят раскрытые узлы, выбранный объект, прокрутка. Чинится это тем, что
> свёрнутая секция прячется, а не размонтируется, — ровно как ты написал.
> Вторая — агент в терминале получил MCP чужого окна и видел только my-space. Это про то, какой
> сервер достаётся терминалу, когда открыто два окна (у mapward порт закреплён, у my-space нет),
> с аккордеоном она не связана. Предлагаю вынести её в отдельную директиву, а здесь только
> сворачивание. Так?

Да вынеси сейчас в директиву прямо, и продолжим про сворачивание

> [AI] Вынес: [терминал получает MCP чужого окна](2026-09-23-0301-терминал-получает-mcp-чужого-окна.md).
>
> Фаза 2. Если свёрнутая карта остаётся смонтированной, она продолжает жить: держит подписки на
> метрики открытого в ней объекта, и дешёвые метрики досчитываются, хотя их никто не видит.
> Предлагаю так: свёрнутая карта хранит всё, что видно глазами (дерево, выбор, прокрутку), но
> подписки на время сворачивания отпускает и при разворачивании поднимает заново. Цена — при
> разворачивании метрики на секунду показывают прошлое значение. Или пусть живёт целиком, как есть?
Пусть живёт целиком, так проще сейчас не думаю что там гига нагрузка будет

> [AI] Фаза 3. Какие карты свёрнуты, сейчас забывается при перезагрузке окна: при старте все
> развёрнуты. У карты уже есть способ помнить своё между перезагрузками — так помнятся свёрнутые
> метрики и положение схемы детей. Предлагаю хранить там же и свёрнутость карт. Тогда «при
> первом открытии» получает буквальный смысл: карта, свёрнутая к моменту перезагрузки, не
> грузится, пока её не развернут, а дальше живёт уже не размонтируясь. Берём?

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — секция аккордеона перестаёт удалять содержимое.
> Где: [`packages/abstract-react-client/src/features/maps/ui/`](../../../packages/abstract-react-client/src/features/maps/ui/)
> — внешний вид выбора карт, без знания о том, где что хранится; правка в `accordion.tsx`.
>
> - Тело секции рендерится, только если секцию хоть раз разворачивали, и после этого остаётся
>   в дереве насовсем. Свёрнутая секция прячется атрибутом `hidden`, а не условием в разметке —
>   так у скрытой карты остаются раскрытые узлы, выбранный объект, прокрутка и подписки.
> - Какие секции свёрнуты, аккордеон больше не держит у себя: получает их снаружи вместе с
>   обработчиком переключения. Хранить это — дело компоновки (кусок 2), а `ui` остаётся
>   глупым, как и остальные слои `ui` в клиенте.
> - Растягивание на высоту остаётся за развёрнутой секцией: скрытая не занимает места, поэтому
>   раскладка сайдбара не меняется.

> [AI] План, кусок 2 — свёрнутость карт помнится между перезагрузками окна.
> Где: [`packages/abstract-react-client/src/features/maps/compose/`](../../../packages/abstract-react-client/src/features/maps/compose/)
> — сборка выбора карт из данных и внешнего вида; правка в `maps.tsx`.
>
> - Список свёрнутых карт хранится там же, где свёрнутые метрики и положение схемы детей, —
>   в хранилище редактора, своём у каждого человека и у каждой папки проекта. Ключ один на
>   сайдбар, `maps:closed`, значение — пути свёрнутых карт списком (множество туда не ляжет).
> - Пока хранилище не ответило, аккордеон не рисуется, как и история карты сейчас: иначе
>   свёрнутая карта успела бы смонтироваться и начать грузиться до того, как стало известно,
>   что она свёрнута.
> - Путь карты, которой больше нет в `mapward.json`, в списке просто лежит и ничего не значит;
>   чистить его не буду — вернётся карта, вернётся и её свёрнутость.
> - Одна карта — по-прежнему без аккордеона, хранилище не трогается.
> - В доке про `mapward.json` к фразе про аккордеон допишу: свёрнутая карта не сбрасывается, а
>   свёрнутость переживает перезагрузку. Решения о сайдбаре не трогаю — это уточнение, не поворот.
>
> План собран, давай переходить к реализации.
