
## 

Связи это тоже объекты и они должны тоже обладать своими метриками

По умолчанию на связи написан короткий текст, воторый может быть просто значением одной из метрик или именем

При нажатии на этот текст, разворачивается обычное превью объекта с метриками

> [AI] **Фаза 1.** Модель уже почти готова: связь — обычный объект в `relations/` с `from`/`to`, метрики на ней завести можно хоть сейчас, а на ребре рисуется её имя. Не хватает другого: «обычного превью объекта с метриками» на карте нет вообще. Узел на холсте — просто подпись, а `preview-metrics-layout`, `preview-size` и `preview-style` доезжают до модели, но их никто не рисует (это записано в `ideas.md`). Поэтому предлагаю так: сделать одну карточку-превью по `preview-metrics-layout` и собирать её в одном месте, а связь по клику на подпись разворачивает именно её. Узлы в этой директиве оставить подписями — карточки на узлах сделать следующей директивой, на той же карточке. Или ты хочешь, чтобы превью появилось сразу у всех — и у узлов, и у связей?
