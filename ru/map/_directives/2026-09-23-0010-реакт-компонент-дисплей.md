


## Дисплей в виде react компонента

Сейчас дислпей обязательно дефлотный. Но это очень сильно ограничивает

В сложных кейсах лучше его сделать реакт компонентом. 

tailwind перевести в режим jit тогда можно писать любые его классы 

А компиляцию компонента делать через esbuild

В пропсах там приходят все необходимые данные об объекте метрика пропсы и тд

Позже ещё и экшоны

По поводу безопасности не переживаем, так как карта это сполшные нод скрипты которые нейронка генерирует)

> [AI] Фаза 1. Какой сложный случай ты видишь первым? Сейчас дисплей — одна из семи готовых
> форм (текст, список, дерево, карта и т.д.), и метрика только выбирает, какую. Я бы сделал
> компонент ещё одним видом дисплея: файл `.tsx` лежит рядом с `config.json` метрики,
> наследуется вместе с ней и получает её значение и объект. Но если тебе нужен, например, свой
> вид целого объекта или интерактив с кнопками, устройство будет другим. Конкретный пример сразу
> покажет, чего не хватает готовым формам.

Например мне нужна метрка по процессу транскрибации видосов 

Типо видеть записи, которые не транскрибированы, которые уже транскрибированы 
И ссылки на переходы в эти файлы. И списками это сделать можно но неудобно 

+ Со временем появятся возможность запускать экшоны на объекте из этих штук и получится полноценный интерактивный интерфейс

> [AI] Фаза 2. У готовых дисплеев есть описание формы данных: по нему карта ловит неверные
> данные и показывает ошибку вместо сломанной картинки, а агент-трансформ знает, в какую форму
> привести вывод, и ему не пишут это в промпте. У своего компонента формы нет. Предлагаю, чтобы
> компонент рядом с собой объявлял схему своих данных, — тогда и проверка, и подсказка агенту
> работают как у готовых. Или для таких метрик это лишнее, и данные пусть идут как есть?

В принципе не думаю что это будет лишним. Давай сделаем схему пропсов, что бы можно было подсказку выводить в ошибках метрик что схему не удовлетворили. Можно описывать json схемой прямо в frontmatter в json или yaml

> [AI] Фаза 3. Где лежит схема. У файла `.tsx` нет frontmatter, как у markdown: блок `---` в
> начале сломает TypeScript. Варианты: схема полем в `config.json` метрики, рядом с указанием
> на компонент, — тогда она наследуется по `extends`, как весь остальной конфиг. Или отдельный
> файл `display.schema.json` рядом с компонентом. Я за поле в `config.json`: одно место, где
> описана метрика. Или ты имел в виду что-то третье, например компонент в markdown-обёртке?

Давай можно указать прямо в config.json или ссылку на json файл в которой будет эта схема

> [AI] Фаза 3. Что компоненту можно импортировать. Предлагаю три источника. React берётся у
> карты, а не вкладывается в компонент: две копии React на одной странице ломают хуки. От
> mapward — набор готового: ссылка, которая открывает файл или объект так же, как везде на карте,
> и готовые дисплеи (список, дерево), чтобы собирать из них, а не переписывать. И любые пакеты
> из `node_modules` проекта — esbuild их вложит в сборку. Нужны ли тебе сторонние пакеты
> (графики, таблицы), или хватит React и набора mapward?

Да думаю c node_modules будет совсем хорошо) 

Попрос того, какой node_modules будет браться. есть вариант прицепить его к basePath есть вариант к mapward.config или просто доп строка вконфиге, что думаешь

> [AI] Фаза 3. Откуда брать `node_modules`. Предлагаю не заводить настройку, а искать так же,
> как Node и сам esbuild: от папки компонента вверх до первого `node_modules`. Карта обычно
> лежит внутри проекта, поэтому поиск сам доходит до пакетов проекта, а компонент в подпакете
> монорепы берёт свои. Запасной путь — `node_modules` в корне проекта (`baseUrl`): он нужен, когда
> карта лежит вне проекта или компонент достался от прототипа из другого места. Отдельная строка
> в конфиге понадобится, только если оба пути мимо, — такой случай у тебя есть?

Давай так, но это кратко нужно в доке прописать

> [AI] Фаза 3. Чем собирать. esbuild и быстрый режим tailwind — это нативные программы под
> каждую ОС, а установленное расширение едет одним файлом, без `node_modules`: любая внешняя
> зависимость роняет его на старте. Варианты: взять esbuild в виде WebAssembly и tailwind без
> нативного сканера (классы из исходника компонента вытащим сами) — всё едет внутри
> расширения и работает везде, сборка чуть медленнее, для маленьких компонентов незаметно.
> Или брать esbuild и tailwind из `node_modules` проекта — тогда проект обязан их поставить.
> Я за первый: дисплей работает в любом проекте без подготовки. Согласен?

Да 

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — как метрика объявляет компонент и схему.
>
> - `packages/core/src/model` — общий язык сервера и клиента. В `schema.ts` у `display` два
>   новых поля: `component` — путь к `.tsx`, `schema` — JSON-схема объектом или путь к `.json`.
>   Вид дисплея — новое имя `component`: `{ "kind": "component", "component": "./display.tsx",
>   "schema": "./schema.json" }`. В `display.ts` форма `component` пропускает данные как есть:
>   проверяет их схема, а не клиент.
> - Пути считаются от папки того `config.json`, где поле написано, а не от наследника. Решил сам:
>   компонент прототипа лежит у прототипа, и наследник с одной строкой `extends` должен его
>   находить, как находит унаследованные экшоны.
> - `packages/abstract-server` — юзкейсы. Подсказка агенту (сейчас `shapeHint` берёт форму по
>   имени дисплея) у `component` кладёт в промпт саму схему. Сбор и трансформ её уже зовут —
>   правка в одном месте.
> - Схему проверяет сервер, а не клиент: у него уже есть `typebox`, а клиенту пришлось бы тащить
>   валидатор. Не прошло — метрика красная, в ошибке путь до поля и что ожидалось.

> [AI] План, кусок 2 — сборка компонента на сервере.
>
> - `packages/abstract-server/src/ports` — всё, что сервер берёт у среды. Новый порт сборщика:
>   «собери файл, вот папки для пакетов» → код, список прочитанных файлов, ошибки. Портом, а не
>   прямым вызовом: esbuild в WebAssembly должен найти свой `.wasm`, а где он лежит, знает только
>   приложение.
> - `packages/abstract-server/src/features/map-object` — юзкейсы объекта. Новый юзкейс сборки
>   дисплея: находит `.tsx` по куску 1, собирает, кэширует, пока не изменился ни один прочитанный
>   файл, и следит за ними — правка компонента или его импорта сразу пересобирает дисплей.
>   Ошибка сборки показывается на метрике с файлом и строкой, как любая другая.
> - Пакеты ищутся от папки компонента вверх, запасной путь — `node_modules` корня проекта.
> - React и набор mapward в сборку не кладутся: их даёт клиент. Решил сам — иначе две копии
>   React на странице.
> - `packages/core/src/contracts` — договор клиента с сервером. Новый вызов «дай собранный
>   дисплей метрики»; подписка на метрики сообщает, что сборка обновилась.
> - `apps/vscode-extension`, `apps/cli` — реализуют порт через `esbuild-wasm`; `.wasm` при сборке
>   копируется в `dist`, как сейчас иконки. Расширение по-прежнему без внешних зависимостей.
> - `packages/docs/ru` — абзац о том, откуда берутся пакеты компонента.

> [AI] План, кусок 3 — стили tailwind для компонента.
>
> - `packages/abstract-server/src/features/map-object` — тот же юзкейс сборки из куска 2. После
>   esbuild он собирает и стили: вытаскивает из прочитанных файлов всё, что похоже на класс, и
>   отдаёт tailwind — тот сам отбросит не-классы и напишет css только на нужные. Ответ сервера —
>   код и css вместе.
> - Решил сам: css строит сервер, а не клиент. Компилятор tailwind — обычный JavaScript, без
>   нативной части, и лежит рядом со сборкой; клиент остаётся лёгким, ему только вставить стиль.
>   Классы ищутся в файлах проекта, а не в `node_modules` — там их тысячи чужих.
> - Решил сам: без сброса стилей (preflight) и в том же слое, что и классы карты — тогда
>   компонент не перекрасит карту, а одинаковые классы совпадут с теми, что уже есть.
> - Цвета — те же переменные темы редактора, что и у карты: `text-[var(--mw-foreground)]`.
>   Своей темы у компонента нет, он выглядит как остальная карта в любой теме редактора.
> - `packages/abstract-server/package.json` — зависимость `tailwindcss` той же версии, что у
>   расширения (общий каталог пакетов).

> [AI] План, кусок 4 — как компонент встаёт в ячейку метрики.
>
> - `packages/abstract-react-client/src/features/map-object/_metrics` — метрики объекта в клиенте.
>   Новый дисплей `component`: берёт у сервера код и css (вызов из куска 2), выполняет код и
>   рисует то, что он экспортирует по умолчанию. Css вставляется своим `<style>` на метрику и
>   убирается, когда ячейка закрывается; пришла новая сборка — компонент перерисовывается.
> - Когда код выполняется и просит `react` или набор mapward, клиент отдаёт свои — это и есть
>   «одна копия React».
> - Пропсы: `data` — значение метрики, уже проверенное схемой; `object` — адрес, имя и `props`
>   объекта; `metric` — ключ, подпись, когда собрана; `open(link)` — открыть файл, объект или
>   страницу, как любая ссылка карты. Экшоны потом добавятся сюда же полем.
> - `ui/displays.tsx` — ветка `component` рядом с готовыми дисплеями. Компонент обёрнут ловушкой
>   ошибок: упал при отрисовке — в ячейке красная строка с текстом ошибки, сайдбар живёт дальше.
> - `apps/vscode-extension/src/apps/extension` — вебвью сейчас запрещает выполнять код из строки.
>   Решил сам разрешить (`unsafe-eval` в политике безопасности вебвью): ты сказал, что код карты —
>   свой, а без этого чужой код в вебвью не запустить никак.

> [AI] План, кусок 5 — набор mapward, описание и проверки.
>
> - `packages/abstract-react-client/src/features/map-object/_metrics/ui` — готовые кусочки
>   дисплеев. Набор `@mapward/display` для компонента — те же, что рисуют карту: ссылка, markdown,
>   точка статуса, список, дерево файлов. Клиент отдаёт их по имени, отдельным npm-пакетом не
>   публикуем. Решил сам: esbuild типы не проверяет, а подсказку агенту даёт дока, не `.d.ts`.
> - `packages/docs/ru/metrics.md` — дока инструмента, её агент получает через `read_docs`. Раздел
>   о дисплее `component`: как объявить, схема, пропсы, набор, стили, откуда пакеты, пример.
> - `ru/docs/decisions` — новое решение `0035` о дисплее-компоненте: зачем, сборка на сервере,
>   WebAssembly вместо нативного, снятый запрет на код в вебвью. Решение `0027` не меняется.
> - `ru/requirements/ui/дисплеи` — требование «метрика показывается своим компонентом» со
>   сценариями: данные не прошли схему, компонент не собрался, упал при отрисовке, правка
>   файла пересобирает.
> - Тесты: в `abstract-server` — сборка и стили на фикстуре с компонентом и ошибкой схемы; в
>   `abstract-react-client` — ловушка ошибок и ветка `component` в приведении данных. Проверка,
>   что в `dist/extension.cjs` не появилось внешних `require`.
>
> План собран, давай переходить к реализации.

Типы я бы всё же публиковал + туда же можно типы пропсов метрики с дженериком под схему
А то нейронка каждый раз будет шуметь периписывать, можно даже простенький скрпиптик для проверки типов дерева для нейронки

> [AI] План, кусок 5, новая версия — типы для компонента и их проверка.
>
> - `packages/display` — новый публикуемый пакет `@mapward/display`, его ставят в проект как
>   dev-зависимость. В нём типы: `DisplayProps<Data>` — пропсы из куска 4, где `data` имеет тип
>   `Data`, — и типы кусочков набора (ссылка, markdown, статус, список, дерево). Кода в пакете нет:
>   на карте импорт подменяет клиент, вне карты он не нужен. Клиент берёт эти же типы, так что
>   типы и то, что реально приходит в пропсах, не разъедутся.
> - Дженерик под схему: JSON-файл нельзя импортировать в TypeScript с точными типами, поэтому
>   тип данных генерируется из схемы. `apps/cli` — новая команда `mapward display check <адрес
>   метрики>`: пишет рядом с компонентом `display.data.d.ts` (`export type Data = …`), прогоняет
>   TypeScript по компоненту и печатает ошибки с файлом и строкой. Заодно собирает компонент и
>   проверяет последнее значение метрики по схеме — одна команда отвечает, работает ли дисплей.
> - Решил сам: проверка типов живёт в CLI, а не в сборке на карте. TypeScript весит мегабайты и
>   медленный — тащить его в расширение и гонять на каждое сохранение незачем; агенту хватает
>   команды после правки.
> - Остальное из прошлой версии куска 5 остаётся: дока в `packages/docs/ru/metrics.md` (теперь и
>   про пакет с командой), решение `0035`, требование в реестре, тесты. К тестам добавляется
>   генерация типа из схемы.
>
> План собран, давай переходить к реализации.