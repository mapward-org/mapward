
## 

Сейчас метрика при выводе в объекте показывается своей, даже если она была получена из родительского объекта

+ Давай сразу поправим следующее. Сейчас разные свойства объекта (метрики, сам конфиг объекта, этапы директив)
Это разные не очень заметные кнопки. 

Давай вместо этого сделаем отображение, которое открывается по клику на кнопку. В этом отображении вся мета информацию об объекте + кнопка назад

Ещё момент. 

Сразу давай переработаем директив интерфейс. Сейчас это список при нажатии на кнопку

Но работа с не завершёнными директивами происходит постоянно. 

Давай сделаем под названием объекта до метрик список активнх директив

Кнопки этапов, пусть будут при наведении, но не должны прагать (менять лейаут)

Доп кнопка открывает меня уже готовых директив как раньше

название директивы чуть устарело, но не беда

Не забудь поправить требования 

## Вопросы

### Стоит учесть

- Модель уже знает, чья метрика: у `MapMetric` есть `owner` — адрес объекта, который метрику
  завёл, и он ставится только унаследованной (`packages/core/src/model/model.ts`,
  `read-map.ts:inherit`). В клиенте это поле не используется **нигде**: единственные два
  места с `owner` — крестик у директивы и подпись этапа. То есть данные для «не своя» есть,
  а до экрана они не доезжают.
- Происхождение метрики сейчас показывается одной подсказкой в меню «Метрики» и считается
  не по владельцу, а по слоям конфига: `originHint` в
  `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:38` —
  «из прототипа», если среди слоёв есть прототипов, иначе «из общей». В самой сетке метрик
  признака нет вовсе: `MetricCell` рисует название, свежесть, точку статуса и две кнопки.
  Поэтому на экране объекта унаследованная метрика неотличима от своей.
- Слои и владелец отвечают на разные вопросы, а сейчас склеены в один. У
  `abstract-react-client` метрики `lint`, `tests`, `version`, `public-api` имеют свой
  `config.json` поверх прототипова: владельца у них нет — заведены здесь, — но по слоям они
  помечаются «из прототипа». Требование `ui/метрики/конфигурация-метрики-видна-на-объекте.md`
  просит обратного: «у своей метрики — ничего». Так что расхождение двустороннее: чужая
  выглядит своей в сетке, своя выглядит чужой в меню.
- То, что унаследованная метрика адресуется наследником и кэш пишет в его папку, — не баг, а
  решение `0002`: конфиг заимствован, состояние нет (`adoptMetric`). Значит «своей» по адресу
  она и останется, и отличать её можно только подписью.
- Кнопок в шапке шесть: терминалы, директивы, этапы, экшоны, метрики, конфигурация объекта.
  Пять из них — один и тот же `MenuButton` с формой «пункт, подсказка справа, кнопки под
  пунктом на наведении». Мета-экран, который их собирает, убирает из шапки ряд целиком —
  останется одна кнопка (плюс терминалы, если они туда не идут).
- Форма «меню в шапке» и «слои кнопками под пунктом» прописаны в решении `0019` прямым
  текстом («Меню объекта, а не вторая панель»), а кнопки этапов у директивы — в `0017`.
  Мета-экран и список активных директив их переопределяют, то есть по правилам карты нужно
  новое решение, перекрывающее эти куски, а не правка старых.
- Требований, которые придётся переписать, семь, и все `status: implemented`:
  `ui/метрики/конфигурация-метрики-видна-на-объекте.md`,
  `ui/объект/конфигурация-объекта-видна-на-объекте.md`,
  `ui/директивы/директивы-списком-со-статусами.md`, `воркфлоу-объекта-видно.md`,
  `экшоны-списком.md`, `директиву-можно-удалить.md`,
  `кнопка-просит-агента-запустить-этап.md`. Плюс новое — про признак владельца в сетке.
  Сетка метрик (`ui/метрики/сетка-метрик.md`) не трогается.
- Мета-экран упирается в `0014`: пункты «метрики» и «конфигурация объекта» показываются
  только при `virtualDocs`, крестик — при `ask`, этапы — при `terminals`. Сейчас хост,
  ничего этого не умеющий, просто не видит кнопок. За одной кнопкой это придётся решать
  заново: кнопка, открывающая пустой экран, — ровно то, что решение запрещает.
- Про объём списка директив: на этом объекте их 34, из них выполненных 31 и незакрытых 3.
  То есть «активные» — это действительно три строки под названием, а архив за кнопкой.
  Статус (`new`/`changed`/`done`), последний прогон (`run.stage`, закончен или идёт) и
  счётчик кругов (`runs`) приходят с картой готовыми — считать в интерфейсе ничего не надо.
- Про «не должны прыгать»: сейчас кнопки этапов добавляются строкой внутри пункта меню
  (`group-hover/item:flex` в `ui/menu-button.tsx`) — именно поэтому список и дёргается.
  Этапов у этого объекта шесть, и шесть кнопок в одну строку в сайдбаре не встанут:
  либо строка под кнопки резервируется всегда, либо кнопки ложатся поверх строки.

### Уточни

> [AI] Про саму багу: чинить её по владельцу или по слоям? Это разные факты, и сейчас они
> смешаны в одну подсказку. Я бы развёл: в сетке у названия метрики — короткий признак
> «не заведена здесь» с именем объекта-владельца (из `owner`), потому что это единственное,
> что человеку нужно на бегу; а «из прототипа / из общей» по слоям оставил бы мета-экрану,
> где есть место на всю цепочку. Тогда метрика со своим `config.json` поверх прототипова
> перестанет называться чужой. Что тебе нужно видеть в сетке: имя владельца, или хватит
> значка/приглушённого названия, а имя — тултипом?

Главное починить кнопки при наведении. Что бы там были все слои. признак "Унаследована" сейчас работает номр 

> [AI] Что такое мета-экран по форме? Я вижу его как второй режим того же объекта: сетка
> метрик уезжает, на её месте — метрики со слоями, конфигурация объекта, этапы, экшоны;
> сверху та же шапка с названием и кнопка назад. Альтернатива — панель поверх сетки, тогда
> «назад» это закрыть. И второй вопрос того же места: конфиг по клику по-прежнему открывается
> json-документом в редакторе (`0019`), или внутри мета-экрана он показывается прямо, а
> редактор остаётся только для слоёв-файлов?

Да это второй режим отображения того же объекта

Мета экран не удобен для показа json, так что да открывай его в редакторе

По сути json полный текст, мета экран версия без текста

> [AI] Что считать активной директивой в списке под названием? Прямой критерий — «не
> выполнена», то есть `new` + `changed`; тогда на этом объекте в списке три строки, а
> компактной сводки ждать неоткуда. Можно жёстче: только те, у которых есть начатый прогон
> (`run`), — тогда свежесозданная директива в список не попадёт, пока её не запустили. Что
> в строке кроме имени: статус цветом, идущий этап, номер круга? И нужен ли потолок по
> количеству, если незакрытых накопится десяток.

Да new + changed. Потолка не нужно. Остальное можно как сейчас отображается

> [AI] Кнопки этапов: место под них резервировать или класть поверх? Резерв честнее — ничего
> никогда не дёргается, но список директив становится вдвое выше, а этапов шесть. Поверх —
> список компактный, но кнопки перекроют имя соседней директивы. Третий вариант: в строке
> постоянно живёт один следующий этап (по последнему прогону видно, какой), а остальные
> под «…». Как тебе удобнее в узком сайдбаре, и нужны ли этапы прямо в этом списке всем
> директивам сразу?

Давай при наведении все кнопки + открыть, прямо поверх

## План

### Решение `0024` — мета-экран объекта и активные директивы под названием

Перекрывает форму, заданную `0019` («меню объекта, а не вторая панель») и часть `0017`
(этапы кнопками внутри меню директив); что показывается — остаётся прежним.

- `ru/docs/decisions/0024-object-meta.md` — новое решение: мета-информация объекта уезжает во
  второй режим того же объекта, а не в ряд меню; незакрытые директивы живут на первом экране,
  архив остаётся меню; ряд кнопок на наведении ложится поверх списка и его не двигает.
- `ru/docs/decisions/0019-metric-config.md:16` — раздел «Меню объекта, а не вторая панель»:
  строкой пометить, что форму переопределяет `0024`, текст не переписывать.
- `ru/docs/decisions/0017-directive-workflow.md` — там же, про кнопки этапов у директивы.
- Номер `0024`: последнее решение `0023`, `0021` пропущен и переиспользоваться не должен.

### Блок 1 — ряд кнопок на наведении не двигает список и показывает все слои

Общий компонент вместо `group-hover/item:flex` внутри строки: ряд ложится поверх соседних
строк, переносится по ширине и начинается кнопкой «открыть».

- `packages/abstract-react-client/src/lib/ui/hover-actions.tsx` — новый: ряд кнопок,
  `absolute left-0 top-full z-50 w-max max-w-[22rem] flex-wrap`, фон `--mw-menu-background`,
  рамка `--mw-menu-border`; показывается на `group-hover/item`. Место — `lib/ui`, рядом с
  `loading.tsx`: им пользуются и фича, и подмодуль, а `ui` родителя подмодулю недоступен
  (`ru/docs/architecture/abstract-react-client.md`, правило импортов).
- `packages/abstract-react-client/src/features/map-object/ui/menu-button.tsx:100` — ряд `runs`
  заменить на `HoverActions`; строка остаётся кликабельной, ряд висит под ней.
- `packages/abstract-react-client/src/features/map-object/ui/menu-button.tsx:17` — в тип
  `runs` добавить, что первым элементом идёт «открыть» (`item.onSelect`), дальше слои: ряд
  накрывает соседей, и дублирующая кнопка нужна, когда мышь ушла со строки.
- `packages/abstract-react-client/src/features/map-object/ui/menu-button.tsx:49` — у `<ul>`
  снять то, что режет вылезающий ряд: ширина `min-w-56` остаётся, `overflow` не добавлять.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:55` —
  `layerRuns` не трогать: все слои уже передаются, резало их отображение.

### Блок 2 — мета-экран: второй режим того же объекта

Метрики, конфигурация объекта, этапы и экшоны уезжают с ряда кнопок шапки на отдельный экран
с «назад»; json по клику по-прежнему открывается в редакторе.

- `packages/abstract-react-client/src/features/map-object/_meta/pure-model/meta.ts` — новый:
  переезжают `originHint` (`compose/map-object.tsx:38`), `layerOwner` (:50), `layerLabel`
  (:52), `kinds` (:62), `howCollected` (:66); плюс сборка секций из `MapObject`.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx` — новый:
  секции «Объект» (имя, прототип, путь, адрес, `props`, слои), «Метрики» (название, откуда,
  чем собирается, слои), «Этапы» (имя, порядок, чей), «Экшоны» (имя, чей). Строка и ряд
  слоёв — тот же `HoverActions` из блока 1.
- `packages/abstract-react-client/src/features/map-object/_meta/compose/meta-screen.tsx` —
  новый: связывает `meta-view` с `useCapabilities` и действиями моста (`open`, `openVirtual`).
- `packages/abstract-react-client/src/features/map-object/ui/icons.tsx:39` — добавить
  `MetaIcon`; `IndexIcon`, `WorkflowIcon`, `ActionsIcon`, `MetricsIcon` остаются — их берёт
  мета-экран под заголовки секций.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:170` — убрать
  `MenuButton` «Этапы директив».
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:187` — убрать
  `MenuButton` «Экшоны».
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:204` — убрать
  `MenuButton` «Метрики».
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:232` — убрать
  `MenuButton` «Конфигурация объекта» вместе с веткой `HeaderButton` для хоста без
  `virtualDocs`: `_index.json` открывается из мета-экрана кнопкой слоя.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:88` — состояние
  `mode: "map" | "meta"`, `useState` рядом с `address`; смена объекта возвращает в `"map"`.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:114` — в
  `ObjectHeader` остаются три кнопки: терминалы, директивы (архив), мета-экран.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:266` — вместо
  `MetricGrid` рисуется `MetaScreen`, когда `mode === "meta"`; сетка не размонтируется зря —
  условный рендер одного из двух.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx` — «назад»
  строкой над секциями, как в `ui/breadcrumbs.tsx:16`: стрелка влево, тот же размер.

### Блок 3 — активные директивы под названием объекта

Незакрытые директивы всегда на виду, до сетки метрик; кнопки этапов — на наведении, поверх.

- `packages/abstract-react-client/src/features/map-object/_directives/pure-model/active.ts` —
  новый: отбор `status !== "done"` (`new` + `changed`), порядок от новых к старым; переезжают
  `statusHint` (`compose/map-object.tsx:20`), `runHint` (:26), `statusColor` (:78) — меню
  архива забирает их отсюда же.
- `packages/abstract-react-client/src/features/map-object/_directives/ui/active-list.tsx` —
  новый: строка — имя, подсказка справа (идущий этап или статус) цветом, как в меню сейчас;
  на наведении `HoverActions`: «открыть», затем этапы объекта по кнопке на этап.
- `packages/abstract-react-client/src/features/map-object/_directives/compose/active-directives.tsx`
  — новый: `useTerminals().runStage`, `actions.open`, `useCapabilities`; без `terminals` ряд
  этапов не рисуется, строка остаётся кликабельной.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:263` —
  вставить список между `ObjectHeader` и сеткой; пусто — не рисуется вовсе, чтобы у объектов
  без директив ничего не занимало места.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:129` — меню
  директив остаётся «как раньше»: весь список, `+ новая директива`, крестик, этапы кнопками.
- Потолка по количеству нет — так решено в обсуждении; при двух десятках незакрытых список
  займёт экран, и это видно сразу, а не прячется.

### Блок 4 — требования и доки

Семь требований описывают старую форму; плюс одно новое на мета-экран.

- `ru/requirements/ui/объект/мета-экран-объекта.md` — новое: второй режим объекта, что в нём
  показано, «назад», json в редакторе. `object: mapward://packages/abstract-react-client`.
- `ru/requirements/ui/метрики/конфигурация-метрики-видна-на-объекте.md` — место переезжает
  в мета-экран; слои по-прежнему кнопками, но ряд не двигает список и начинается «открыть».
- `ru/requirements/ui/объект/конфигурация-объекта-видна-на-объекте.md` — то же место; ветка
  «хост без `virtualDocs` открывает свой `_index.json`» переписывается на кнопку слоя.
- `ru/requirements/ui/директивы/директивы-списком-со-статусами.md` — два места: незакрытые
  под названием, полный список в меню.
- `ru/requirements/ui/директивы/кнопка-просит-агента-запустить-этап.md` — кнопки этапов есть
  и в активном списке, и в меню; появляются на наведении и не двигают список.
- `ru/requirements/ui/директивы/воркфлоу-объекта-видно.md` — этапы видно в мета-экране.
- `ru/requirements/ui/директивы/экшоны-списком.md` — там же.
- `ru/requirements/ui/директивы/директиву-можно-удалить.md` — крестик остаётся в меню архива;
  дописать, что в активном списке его нет (см. «Уточни»).
- `packages/docs/ru/index-format.md:77` — «меню конфигурации на объекте» переписать на
  мета-экран: это дока инструмента, она едет с ним.
- Статусы: на Реализации у тронутых требований ставим `implementing`, на Тестировании —
  `implemented`. Метрика «Расхождения с кодом» сверяет реестр с кодом, а не наоборот.

### Порядок

1. Решение `0024` — на него ссылаются комментарии в коде и тексты требований.
2. Блок 1 — общий ряд кнопок; на нём стоят и блок 2, и блок 3.
3. Блок 2 — мета-экран; из шапки уезжают четыре меню, освобождая место.
4. Блок 3 — активные директивы; шапка к этому моменту уже разгружена.
5. Блок 4 — требования и доки, по сделанному.
6. `pnpm build`, `pnpm lint`, `pnpm typecheck`, `pnpm test` — с корня, иначе тесты молчат.

### Не делаем

- Признак владельца в ячейке сетки метрик — «унаследована» работает и так, сказано в ответах.
- Перевод `originHint` со слоёв на `owner` — по тем же ответам; расхождение из «Стоит учесть»
  остаётся как есть.
- Сетку метрик, раскладки, сворачивание — не трогаем совсем.
- Сервер, MCP и модель — всё нужное (`layers`, `owner`, `status`, `run`, `runs`) уже приходит;
  правок в `abstract-server` и `core` в этой директиве нет.
- Терминалы из шапки в мета-экран не переносим: это действие, а не мета-информация.
- Капабилити (`0014`) не расширяем: новых умений хоста мета-экран не требует.

### Стоит учесть

- Ряд кнопок ложится под строкой и накрывает соседние: у последней строки длинного меню он
  уедет за нижний край. Разворот вверх делать не планирую — в активном списке строк три,
  а в меню архива наведение на последнюю строку редкое.
- Кнопка мета-экрана показывается всегда, даже когда хост не умеет ни `virtualDocs`, ни
  `openFile`: за ней остаются имена метрик, этапов, экшонов, `props` и прототип — текст,
  а не пустота. Это не спорит с `0014`, но в решении `0024` должно быть сказано прямо.
- Режим — состояние React, не `useViewState`: переживать перезапуск ему незачем, а возврат
  на первый экран при смене объекта как раз нужен.
- Шапка после правок: три кнопки вместо шести. Иконку мета-экрана надо придумать — в клиенте
  иконки свои, `ports/icons.tsx` отдаёт только хостовые.
- `_meta` и `_directives` — подмодули фичи `map-object`, как `_metrics` и `_children-map`:
  свои `compose`/`ui`/`pure-model`, наружу только через `compose` родителя.
- Метрика `architecture-drift` на `abstract-react-client` сверяет структуру с
  `ru/docs/architecture/abstract-react-client.md`; новые подмодули в правило укладываются,
  править док не придётся — но метрику после реализации стоит прогнать.

### Уточни

> [AI] Крестик удаления в активном списке: предлагаю его туда не тащить — в ряду на наведении
> и так «открыть» плюс шесть этапов, а удаление необратимо и промахнуться по нему в узкой
> строке легко. Тогда удаляют из меню архива, как сейчас, — незакрытые директивы там тоже
> есть. Годится или крестик нужен и в активном списке?

Да

> [AI] Что из объекта показывать в мета-экране кроме метрик, этапов и экшонов: предлагаю
> имя с прототипом, адрес, путь на диске, `props` после подстановок и слои `_index.json`
> кнопками. `props` бывают длинными (пути с подстановками), поэтому думаю показывать их
> таблицей «ключ — значение» с обрезкой по ширине и полным текстом в тултипе. Что-то лишнее?

Всё отлично

> [AI] Как назвать кнопку и экран. «Мета» в интерфейсе выглядит техническим словом; предлагаю
> тултип «Об объекте» и заголовок экрана — имя объекта с пометкой, что это его устройство.
> Либо оставить «Мета» — оно короткое и в разговоре уже прижилось. Как назовём?

Ок

> [AI] Директивы в мета-экране: сейчас планирую их туда не класть — активные под названием,
> архив в меню шапки. Но мета-экран это «всё об объекте», и отсутствие в нём директив может
> раздражать. Вариант — секция «Директивы» в мета-экране со всем списком, и тогда меню из
> шапки уходит совсем, а кнопок там остаётся две. Оставляем меню или сводим в мета-экран?

Да экшони и архив директив туда выносим тоже (там все директивы и крестик)! 
В основном меню только кнопка новая директива и терминал и об объекте

## Реализация

### Сделано

- `ru/docs/decisions/0024-object-meta.md` — новое решение: мета-экран вместо ряда меню,
  незакрытые директивы на первом экране, ряд кнопок слоем поверх списка. Инвариант — три
  кнопки в шапке и неизменная высота списка при наведении.
- `ru/docs/decisions/0019-metric-config.md` — в разделе «Меню объекта, а не вторая панель»
  строка о том, что форму переопределило `0024`; сам текст не тронут.
- `ru/docs/decisions/0017-directive-workflow.md` — то же в разделе «Кнопка вернулась, но
  другая»: меняется место кнопок, не то, что они делают.
- `packages/abstract-react-client/src/lib/ui/list-row.tsx` — новый общий компонент строки
  списка: название, подсказка справа, крестик и ряд кнопок на наведении. Ряд лежит под
  строкой и поверх соседних, первая кнопка — «открыть».
- `packages/abstract-react-client/src/features/map-object/pure-model/directives.ts` — новый:
  статусы, подсказка строки, порядок от новых к старым, отбор незакрытых.
- `packages/abstract-react-client/src/features/map-object/_directives/` — новый подмодуль:
  `ui/directive-row.tsx` (строка с этапами) и `compose/directive-list.tsx` (список, один и
  тот же для незакрытых и для полного).
- `packages/abstract-react-client/src/features/map-object/_meta/` — новый подмодуль:
  `pure-model/meta.ts` (происхождение конфига, подписи слоёв, чем собирается метрика, `props`
  таблицей), `ui/meta-view.tsx` (разделы «Объект», «Метрики», «Директивы», «Этапы директив»,
  «Экшоны», кнопка «назад»), `compose/meta-screen.tsx` (мерджи и файлы через мост).
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx` — шапка
  сведена к трём кнопкам: терминалы, новая директива, «об объекте». Под шапкой — список
  незакрытых директив, в теле — сетка метрик или мета-экран.
- `packages/abstract-react-client/src/features/map-object/ui/menu-button.tsx` — удалён:
  после переезда списков его не зовёт никто.
- `packages/abstract-react-client/src/features/map-object/ui/icons.tsx` — добавлены иконки
  «новая директива» и «об объекте»; крестик уехал в общую строку списка.
- `ru/requirements/ui/объект/мета-экран-объекта.md` — новое требование.
- `ru/requirements/ui/метрики/конфигурация-метрики-видна-на-объекте.md`,
  `ru/requirements/ui/объект/конфигурация-объекта-видна-на-объекте.md`,
  `ru/requirements/ui/директивы/директивы-списком-со-статусами.md`,
  `кнопка-просит-агента-запустить-этап.md`, `воркфлоу-объекта-видно.md`,
  `экшоны-списком.md`, `директиву-можно-удалить.md` — переписаны под новую форму.
- `packages/docs/ru/index-format.md`, `ru/docs/development-guide/directives.md` — строки про
  «меню конфигурации» и «меню директив» приведены к тому, что теперь на экране.
- Проверки с корня: `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm build`,
  `pnpm test` (136 тестов) — зелёные. В живом редакторе не смотрел: это Тестирование.

### Решил за тебя

- Кнопка «новая директива» стала прямой кнопкой шапки, а не пунктом внутри меню: меню
  директив больше нет, а из ответа следует, что кнопка в шапке остаётся.
- `ui/menu-button.tsx` удалён целиком, вместо него общий `lib/ui/list-row.tsx`: после переезда
  всех списков компонент оказался без единого использования, а строка нужна и подмодулям —
  в `lib`, потому что `ui` родителя подмодулю по правилам импортов недоступен.
- Ряд кнопок кладётся **под** строкой, а не поверх неё: так клик по самой строке остаётся
  живым. «Открыть» в ряду всё равно есть — как ты и просил.
- Вход в мета-экран — кнопка в шапке, выход — «назад» строкой в самом экране. Дублировать
  выход кнопкой шапки не стал: в режиме мета-экрана она прячется.
- Статус требований — `implementing`. `implemented` поставит Тестирование, когда проверит
  в живом редакторе.
- Пометки в `0019` и `0017` сделаны цитатой в начале переопределённого раздела: старое решение
  не переписывается, новое его перекрывает.
- В разделе «Объект» `props` печатаются как есть, а не-строки — через `JSON.stringify`:
  в карте они строковые, но схема этого не обещает.
- У экшона и этапа, доставшихся от прототипа, владелец показан адресом без схемы
  (`prototypes/system`) — тем же видом, что подписи общих слоёв.

## Правки

Сейчас директивы от метрик не отделены, хотя бы небольшие подписи сделай и горизонтальную линию

Кнопка добавить директиву плюсик рядом с "Директивы"

Если больше 3х директив то там скролл пусть будет

Кнопки при наведении не удобные при движении мыши в низ. 

Давай сделаем кнопку запуска на директиве, которая открывает меню с элементами воркфлоу 
Открыть просто клик по директиве

2. Иконки визуально не выравнены терминала и новой директивы

3. Этапы директив над архивом диретив + Каждую секцию можно скрывать и раскрывать

4. Отображение объекта это не на наведение, а прям кнопка с текстом. Так же и для метрик
Клик уже отрывает меню.  Собранный вид так и называется а не просто открыть

5. Отображай директивы без шума (убери даты, и расширение md). Так же новые отмечай не синим а серым

6. Кнопку возвращения обратно сделай прямо там где была кнопка перейти в описание, что бы такой некий тогл получился, а не стрелка назад. Кнопка называется (к метрикам)
## План правок

### Решение `0028` — действие по клику, а не по наведению

Перекрывает форму из `0024`: ряд кнопок на наведении заменяется меню по клику на видимой
кнопке, вход в мета-экран становится тоглом, разделы сворачиваются.

- `ru/docs/decisions/0028-click-not-hover.md` — новое решение: у строки списка есть кнопка
  с подписью, меню открывается кликом; «собранный вид» называется собой; вход и выход из
  мета-экрана — одна кнопка шапки; разделы сворачиваются.
- `ru/docs/decisions/0024-object-meta.md:50` — раздел «Кнопки этапов ложатся поверх списка»:
  строкой пометить, что форму переопределило `0028`, текст не переписывать.
- `ru/docs/decisions/0024-object-meta.md:63` — инвариант «кнопок в шапке три»: там же
  пометка. Состав другой — терминал, таб, тогл, — а «новая директива» уехала к заголовку
  списка директив.
- Номер `0028`: последнее решение `0027`, `0021` пропущен и переиспользоваться не должен.

### Блок 1 — общие детали: раздел со сворачиванием и меню строки

Две формы, которые дальше нужны во всех блоках: заголовок раздела и меню по клику.

- `packages/abstract-react-client/src/lib/ui/section.tsx` — новый: заголовок (иконка,
  название, кнопки справа), сворачивание по клику на заголовок, тело под ним. Состояние —
  `useState` внутри, `defaultOpen` пропом.
- `packages/abstract-react-client/src/lib/ui/row-menu.tsx` — новый: кнопка с текстовой
  подписью и выпадающее меню; закрытие по клику вне — тем же `useEffect`, что в
  `features/map-object/ui/terminal-menu.tsx:21`.
- `packages/abstract-react-client/src/lib/ui/list-row.tsx:68` — ряд на наведении убрать
  целиком: `group-hover/row:flex` и слой `absolute top-full` уходят.
- `packages/abstract-react-client/src/lib/ui/list-row.tsx:32` — подстановку «открыть» первой
  кнопкой убрать: подпись пункта задаёт вызывающий, и в мета-экране это «собранный вид».
- `packages/abstract-react-client/src/lib/ui/list-row.tsx:9` — `RowAction` остаётся; вместо
  `actions` строка принимает `menu?: { label: string; actions: RowAction[] }` — `RowMenu`
  рисуется в строке справа, до подсказки.
- `packages/abstract-react-client/src/lib/ui/list-row.tsx:57` — крестик остаётся на
  наведении и справа: движение мыши к нему горизонтальное, а не вниз, и на него жалобы нет.
- `packages/abstract-react-client/src/lib/ui/list-row.tsx:38` — `group/row` оставить: на нём
  держится крестик.

### Блок 2 — директивы отделены от метрик и запускаются меню

Список незакрытых становится разделом со своим заголовком, плюсиком и границей; этапы
переезжают с наведения в меню на кнопке.

- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:218` —
  список обернуть в `Section` с заголовком «Директивы», иконкой `DirectivesIcon` и плюсиком
  справа; `border-b` под разделом отделяет его от вкладок и сетки.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:189` —
  `HeaderButton` «Новая директива» из шапки убрать: кнопка переезжает в заголовок раздела.
- `packages/abstract-react-client/src/features/map-object/_directives/compose/directive-list.tsx:16`
  — пустой список больше не возвращает `null` на первом экране: раздел с плюсиком нужен и
  объекту без директив. Пустоту показывает сам раздел, строкой-заглушкой.
- `packages/abstract-react-client/src/features/map-object/_directives/compose/directive-list.tsx:23`
  — контейнеру `max-h` под три строки и `overflow-y-auto`; ориентир `max-h-[4.5rem]`,
  точное значение подобрать по строке (`py-0.5` плюс интерлиньяж).
- `packages/abstract-react-client/src/features/map-object/_directives/ui/directive-row.tsx:28`
  — `actions` заменить на `menu`: подпись кнопки одна на все строки, в меню — этапы объекта.
- `packages/abstract-react-client/src/features/map-object/_directives/ui/directive-row.tsx:35`
  — многоточие у идущего этапа остаётся: в меню оно так же читается.
- `packages/abstract-react-client/src/features/map-object/_directives/ui/directive-row.tsx:26`
  — `onSelect` остаётся открытием файла: клик по строке открывает директиву, пункта
  «открыть» в меню больше нет.

### Блок 3 — директива в строке без шума

Имя файла несёт дату, время и расширение; в строке от них пользы нет, а ширину они съедают.

- `packages/abstract-react-client/src/features/map-object/pure-model/directives.ts:30` —
  рядом с `newestFirst` новый `directiveLabel`: снять `.md` и префикс `ГГГГ-ММ-ДД-ЧЧММ-`;
  имя без префикса (`create-base.md`) остаётся собой. Порядок по-прежнему считается по
  имени файла — чистка касается только подписи.
- `packages/abstract-react-client/src/features/map-object/pure-model/directives.ts:6` —
  `statusColor.new`: `--mw-charts-blue` заменить на `--mw-descriptionForeground` (переменная
  есть в `apps/vscode-extension/src/apps/webview/theme.css:15`). `changed` и `done` не
  трогаем.
- `packages/abstract-react-client/src/features/map-object/_directives/ui/directive-row.tsx:23`
  — `label={directiveLabel(props.file)}`, `title={props.file.name}`: полное имя тултипом,
  иначе две директивы одного дня в списке неразличимы.
- `packages/abstract-react-client/src/features/map-object/pure-model/directives.test.ts` —
  новый: `directiveLabel` на имени с датой, без даты и без расширения. В пакете тесты уже
  есть (`_metrics/pure-model/markdown.test.ts`), места для нового не заводится.

### Блок 4 — мета-экран: порядок, сворачивание, кнопки с текстом

Разделы сворачиваются, этапы встают перед архивом директив, слои открываются кнопкой,
а мердж называется собранным видом.

- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx:7` —
  локальный `Section` заменить на общий из блока 1.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx:119` —
  раздел «Директивы» перенести **под** «Этапы директив»: порядок станет объект, метрики,
  этапы, директивы, экшоны.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx:52` —
  `layers` отдаёт не `actions`, а `menu`: кнопка с подписью в строке, слои пунктами.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx:75` —
  строка объекта: «собранный вид» становится первым пунктом того же меню вместо
  `onSelect`; клик по строке открывает меню.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx:110` —
  то же у строки метрики.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx:63` — строку
  «← Об объекте» убрать: возврат переезжает в шапку (блок 5), а заголовок экрана лишний —
  объект назван в шапке, разделы подписаны сами.
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx:46` — проп
  `onBack` убрать из `MetaView`.
- `packages/abstract-react-client/src/features/map-object/_meta/compose/meta-screen.tsx:22` —
  `onBack` убрать и отсюда; `onOpenObjectConfig` и `onOpenMetricConfig` остаются как есть
  (`meta-screen.tsx:39`, `:46`) — меняется только место, откуда их зовут.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:256` —
  `onBack` из вызова `MetaScreen` убрать.

### Блок 5 — шапка: тогл вместо входа и ровные иконки

Кнопка «об объекте» перестаёт прятаться и становится переключателем; иконки шапки сводятся
к общему боксу.

- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx:205` —
  условие `!meta` снять: кнопка есть всегда. В режиме метрик — «Об объекте», `MetaIcon`;
  в мета-экране — «К метрикам», `MetricsIcon`, клик `setMeta(false)`.
- `packages/abstract-react-client/src/features/map-object/ui/icons.tsx:50` — `TabIcon`
  перевести с `size-3.5` на общий `box`: сейчас он мельче соседей.
- `packages/abstract-react-client/src/features/map-object/ui/icons.tsx:34` —
  `NewDirectiveIcon` и `DirectivesIcon` (`icons.tsx:3`) нарисованы в боксе `x 4…12.5`,
  `y 2.5…13.5`, а терминал и `MetaIcon` — в `x 2…14`, `y 3…13`: лист сдвинут вправо и выше.
  Привести все четыре к одному боксу `x 2.5…13.5`, `y 3…13`.
- `packages/abstract-react-client/src/features/map-object/ui/terminal-menu.tsx:3` —
  `TerminalIcon` туда же: `rect x=2.5 width=11`.
- `packages/abstract-react-client/src/features/map-object/ui/object-header.tsx:17` —
  `HeaderButton` не трогаем: паддинги у всех кнопок общие, расходятся сами рисунки.

### Блок 6 — требования и доки

Форму описывают пять требований и две доки; все говорят «на наведении».

- `ru/requirements/ui/директивы/кнопка-просит-агента-запустить-этап.md:51` — абзац про
  наведение и «открыть» первой кнопкой переписать на меню по клику; клик по строке
  открывает файл.
- `ru/requirements/ui/директивы/директивы-списком-со-статусами.md:8` — раздел с заголовком
  и границей, скролл после трёх строк, плюсик у заголовка вместо кнопки в шапке; там же
  `:20` — про кнопку новой директивы.
- `ru/requirements/ui/директивы/директивы-списком-со-статусами.md:13` — подпись строки:
  дата и расширение не показываются, полное имя тултипом; `new` помечается серым.
- `ru/requirements/ui/метрики/конфигурация-метрики-видна-на-объекте.md:99` — слои кнопкой
  с меню, первый пункт «собранный вид»; абзац про ряд на наведении уходит.
- `ru/requirements/ui/объект/конфигурация-объекта-видна-на-объекте.md:130` — то же для
  строки объекта.
- `ru/requirements/ui/объект/мета-экран-объекта.md:14` — вход и выход одной кнопкой шапки,
  «назад» строкой в экране нет; `:16` — порядок разделов с этапами перед директивами;
  разделы сворачиваются.
- `ru/requirements/ui/директивы/воркфлоу-объекта-видно.md:161` — место раздела в порядке.
- `ru/docs/development-guide/directives.md:65` — «появляются на наведении» переписать на
  меню на кнопке.
- `packages/docs/ru/index-format.md:126` — «строка объекта открывает мердж» переписать на
  пункт «собранный вид».
- Статусы требований остаются `implementing`: `implemented` ставит Тестирование.

### Порядок

1. Решение `0028` — на него ссылаются комментарии в коде и тексты требований.
2. Блок 1 — общие детали; на них стоят блоки 2 и 4.
3. Блок 3 — чистая функция и цвет: ни от чего не зависит, ломаться нечему.
4. Блок 2 — раздел директив на первом экране.
5. Блок 4 — мета-экран.
6. Блок 5 — шапка: к этому моменту видно, что в ней осталось.
7. Блок 6 — требования и доки, по сделанному.
8. `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm build`, `pnpm test` — с корня.

### Не делаем

- Сетку метрик, вкладки и раскладки не трогаем: жалоба про списки, а не про сетку.
- `TerminalMenu` на общий `row-menu` не переводим: это меню шапки, а не строки, и форма
  у него своя (кнопка-иконка, первый клик заводит терминал).
- Потолок по количеству директив не вводим: скролл прячет третью строку от глаз, но не от
  списка, а `0024` запрещал именно «и ещё 7».
- Крестик удаления в активный список не тащим — как решено кругом выше.
- Сервер, MCP и модель: `status`, `run`, `runs`, `layers`, `owner` уже приходят, правок в
  `abstract-server` и `core` нет.
- Статусы требований в `implemented` не переводим.

### Стоит учесть

- Инвариант `0024` — «кнопок в шапке три» — сломан не этими правками: таб-кнопка приехала
  решением `0026` (`compose/map-object.tsx:197`) и пометки о перекрытии не оставила. `0028`
  закрывает это заодно, иначе инвариант остаётся красным без причины.
- Порядок директив держится на дате **в имени файла** (`pure-model/directives.ts:30`).
  Чистка подписи его не трогает, но две директивы одного дня в списке станут похожи —
  за это отвечает тултип с полным именем.
- Раздел директив рисуется всегда, даже когда их нет: иначе плюсику негде жить. Пустой
  раздел — это одна строка заголовка, а не пустой блок на пол-экрана.
- Сворачивание разделов — `useState` внутри `Section`: переживать перезапуск ему незачем,
  как и режиму мета-экрана (`0024`). Свёрнутое состояние сбросится при переходе на другой
  объект — это то же поведение, что у вкладок.
- Скролл в разделе директив и меню, открывающееся из строки, спорят: меню внутри
  `overflow-y-auto` обрежется по краю списка. Поэтому меню строки должно рисоваться слоем
  с `z-50`, как ряд сейчас (`lib/ui/list-row.tsx:69`), и выходить за контейнер — проверить
  это на четвёртой строке списка.
- Живой MCP обслуживает `dist` открытого расширения: правки интерфейса видно только после
  `pnpm build` и перезапуска окна.
- Тесты гоняются с корня (`pnpm test`), типы — `pnpm typecheck` через turbo.

### Уточни

> [AI] Как подписать кнопку меню в строке. Предлагаю в мета-экране «слои» (и первым пунктом
> «собранный вид»), а у директивы — «этапы». Подпись одна на все строки списка, чтобы глаз
> не читал её каждый раз. Альтернатива для мета-экрана — сделать кнопкой саму подсказку
> «из прототипа» / «из общей»: тогда в строке не прибавится текста, но у своей метрики
> подсказки нет, и кнопке будет не на чем стоять. Что берём?

> [AI] Что делает клик по самой строке в мета-экране. Сейчас он открывает мердж; если
> «собранный вид» становится пунктом меню, у клика по строке остаётся два варианта: открыть
> то же меню (тогда кнопка — просто видимая подсказка, что меню есть) или не делать ничего.
> Я за первое: промахнуться мимо узкой кнопки легко. У директивы клик по строке по-прежнему
> открывает файл, как ты и написал.

> [AI] Плюсик у заголовка «Директивы» — вместо кнопки в шапке или вдобавок? Предлагаю
> вместо: в шапке останутся терминал, таб и тогл «об объекте», и это ровно то, что `0024`
> называло тремя кнопками. Если плюсик нужен и там и там — скажи, тогда `0028` разрешит
> четвёртую.

> [AI] Подписи над списками: заголовок «Директивы» с границей снизу я ставлю только над
> директивами. Над метриками подписью работают вкладки (`ui/group-tabs.tsx`), а у объекта
> без вкладок над сеткой не будет ничего. Добавлять ли заголовок «Метрики» такому объекту,
> или граница под директивами достаточно их разделяет?

## Реализация правок

### Сделано

- `ru/docs/decisions/0028-click-not-hover.md` — новое решение: действия строки берут кликом
  на видимой кнопке, а не наведением; разделы сворачиваются; вход и выход из мета-экрана —
  один тогл; директива в строке зовётся собой. Инвариант — ни одно меню в списках не
  открывается наведением, и кнопок в шапке три.
- `ru/docs/decisions/0024-object-meta.md` — пометки о перекрытии в разделе «Кнопки этапов
  ложатся поверх списка» и в инварианте: состав трёх кнопок пересчитан.
- `ru/docs/decisions/0017-directive-workflow.md` — там же, вторым абзацем к прежней пометке.
- `packages/abstract-react-client/src/lib/ui/row-menu.tsx` — новый: кнопка с подписью и меню
  под ней, плюс общий `useDismiss`. Меню рисуется `fixed` по месту кнопки — иначе его резал
  бы край прокручиваемого списка; прокрутка и ресайз его закрывают.
- `packages/abstract-react-client/src/lib/ui/section.tsx` — новый: раздел со сворачиваемым
  заголовком и кнопками рядом с названием (`SectionButton`).
- `packages/abstract-react-client/src/lib/ui/list-row.tsx` — ряд на наведении убран, вместо
  него `menu`; строка стала флексом с отдельной кнопкой меню, подсветка переехала на всю
  строку. Крестик остался на наведении: к нему мышь едет поперёк, а не вниз.
- `packages/abstract-react-client/src/features/map-object/pure-model/directives.ts` —
  `directiveLabel` (без даты, времени и `.md`), `statusColor.new` серым.
- `packages/abstract-react-client/src/features/map-object/pure-model/directives.test.ts` —
  новый: чистка имени, отбор незакрытых, порядок.
- `packages/abstract-react-client/src/features/map-object/_directives/ui/directive-row.tsx` —
  подпись без шума, полное имя тултипом, этапы в меню «этапы»; клик по строке открывает файл.
- `packages/abstract-react-client/src/features/map-object/_directives/compose/directive-list.tsx`
  — `compact` (три строки и прокрутка) и `empty` для первого экрана; в мета-экране высота
  не режется.
- `packages/abstract-react-client/src/features/map-object/compose/map-object.tsx` — список
  незакрытых обёрнут разделом «Директивы» с плюсиком и границей снизу; кнопка новой директивы
  ушла из шапки туда; «об объекте» стала тоглом «к метрикам».
- `packages/abstract-react-client/src/features/map-object/_meta/ui/meta-view.tsx` — общий
  `Section` вместо локального, «Этапы директив» перед «Директивами», слои и мердж в меню
  «слои» с пунктом «собранный вид», строка «← Об объекте» убрана.
- `packages/abstract-react-client/src/features/map-object/_meta/compose/meta-screen.tsx` —
  `onBack` больше не нужен.
- `packages/abstract-react-client/src/features/map-object/ui/icons.tsx` и `ui/terminal-menu.tsx`
  — все иконки шапки и заголовков сведены к одному боксу `x 3…13`, `y 3…13`, центр `8,8`.
- `ru/requirements/ui/директивы/директивы-списком-со-статусами.md`,
  `кнопка-просит-агента-запустить-этап.md`, `воркфлоу-объекта-видно.md`,
  `директиву-можно-удалить.md`, `ru/requirements/ui/метрики/конфигурация-метрики-видна-на-объекте.md`,
  `ru/requirements/ui/объект/конфигурация-объекта-видна-на-объекте.md`,
  `мета-экран-объекта.md` — переписаны под новую форму.
- `ru/docs/development-guide/directives.md`, `packages/docs/ru/index-format.md` — строки про
  «на наведении» и «клик открывает мердж» приведены к тому, что теперь на экране.
- Проверки с корня: `pnpm typecheck`, `pnpm test` (163 теста), `pnpm lint`, `pnpm format:check`,
  `pnpm build` — зелёные. В живом редакторе не смотрел: это Тестирование.

### Решил за тебя

- **Подпись кнопки меню:** «слои» в мета-экране и «этапы» у директивы. Подпись одна на весь
  список — глаз читает её один раз. Подсказку «из прототипа» кнопкой не делал: у своей
  метрики её нет, и кнопке было бы не на чем стоять.
- **Клик по строке в мета-экране** открывает то же меню: промахнуться мимо узкой кнопки
  легко. У директивы клик по строке по-прежнему открывает файл.
- **Плюсик — вместо кнопки в шапке**, не вдобавок. В шапке остались терминал, «открыть
  табом» и тогл: три, как в `0024`, но состав другой — это записано в `0028`.
- **Заголовка «Метрики» на первом экране нет.** Границы под разделом директив хватает,
  а лишняя строка съедает место у сетки. Если объекту без вкладок его всё же не хватает —
  скажи, это одна строка.
- **Кнопка меню видна всегда, не только на наведении** — ты просил «прям кнопка с текстом».
  Приглушена, пока меню закрыто.
- **Прокрутка списка закрывает меню.** Меню лежит `fixed` и о прокрутке не знает — иначе
  уехало бы от своей строки.
- **`empty` под заголовком:** «незакрытых нет». Без него раздел с плюсиком выглядел бы
  сломанным.
- **Крестик оставлен на наведении.** Жалоба была про движение вниз, а крестик справа: мышь
  идёт к нему поперёк списка.
- **`TerminalMenu` не переписан** на общий `RowMenu` — у него своя форма (кнопка-иконка,
  первый клик заводит терминал); общим стал только `useDismiss`.
- **Метрику `architecture-drift` не гонял** — дорогая и агентская; новые файлы лежат
  в `lib/ui`, что архитектурный док прямо разрешает.

### Правки по живому экрану

Пять правок после того, как ты посмотрел собранное.

- `packages/abstract-react-client/src/features/map-object/ui/terminal-menu.tsx` — кнопке
  терминала вертикальный паддинг, а её обёртке `flex`: без него кнопка лежала внутри
  `div.relative` и не тянулась до высоты соседей — те у шапки прямые дети флекса и
  растягиваются сами. Теперь все три кнопки шапки одной высоты.
- `packages/abstract-react-client/src/features/map-object/ui/icons.tsx` — `RunIcon`: плей
  вместо слова «этапы» в строке директивы. Меньше кнопок шапки (`size-3.5`): это кнопка
  внутри списка. Тултип «Запустить этап».
- `packages/abstract-react-client/src/lib/ui/row-menu.tsx` — подпись кнопки стала `ReactNode`
  и принимает `title`: слово там, где есть ширина, значок там, где её нет. Пункты меню
  переехали с ряда на вертикальный список — по пункту на строку, как в меню терминалов;
  длинный список прокручивается внутри себя.
- `packages/abstract-react-client/src/features/map-object/_directives/compose/directive-list.tsx`
  — прокрутка после пяти строк вместо трёх.
- `packages/abstract-react-client/src/features/map-object/pure-model/directives.ts` —
  `directiveHintClass`: серым помечается только «новая», то есть не запускавшаяся. Был прогон
  — в строке стоит этап, и он синий. Прежде цвет брался по статусу, и директива с прогоном
  оставалась серой, потому что статус у неё всё ещё `new`.
- `packages/abstract-react-client/src/features/map-object/pure-model/directives.test.ts` —
  три теста на этот цвет.
- `ru/docs/decisions/0028-click-not-hover.md` — дописаны абзацы про значок вместо слова,
  вертикальный список пунктов, пять строк и цвет подсказки.
- `ru/requirements/ui/директивы/директивы-списком-со-статусами.md`,
  `кнопка-просит-агента-запустить-этап.md`, `ru/docs/development-guide/directives.md` —
  приведены к тому же.
- Проверки с корня после правок: `pnpm typecheck`, `pnpm test` (166 тестов), `pnpm lint`,
  `pnpm format:check`, `pnpm build` — зелёные.

#### Решил за тебя

- **Кнопка «слои» в мета-экране осталась словом.** Плей поставлен только директиве: там
  строка узкая и действие у всех строк одно. В мета-экране ширины хватает, и слово честнее
  значка, за которым прячется разное.
- **Вертикальный список — у обоих меню**, а не только у этапов: форма меню одна на клиент,
  и слои теперь читаются так же.
- **Высота кнопки терминала задаётся её паддингом**, соседи подтягиваются под неё. Так строка
  шапки выше на пару пикселей, зато по кнопкам легче попасть.
