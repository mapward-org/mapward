
## 

Сейчас экшоны запускаются из директив. Но на практике это не удобно 

Некоторые действия удобно запускать по кнопке. 

Сбор же метрик - это read а не write поэтому это нельзя делать через метрики


Сразу делаем тут так же как с коллектором 

1. Скрипт 
2. Промпт
3. Кастомные типы которые могут потом появится
4. У экшонов есть логи, вывод и статус точно так же как и у метрик


Так же для экшонов нужен будет небольшой форм билдер потому что почти всегда нужно какие то данные собрать

Схемы этой формы будет храниться в экшоне


## Новый экран отслеживания метрик и экшонов

Сейчас не видно ни логов ни этапов работы экшонов 

Прелагаю сделать отдельный экран для этого. 

Чем то по смыслу похожий на github экшоны 

Видно настройки этапы логи, результаты каждого этапа


Ну и не будем показывать на каждой метрике зелёный круг, они шумят только красный если надо

Кстати прогоны тоже есть смысл как то хранить. Возможно стоит добавить git ignore файлы или в стейте редактора хранить

## Запуск экшонов из дисплеев

Некоторые экшоны удобно запускать из дисплеев. Нужно сделать возможность вместо ссылок из дисплеев открывать нормальноХ

> [AI] Фаза 1. Сейчас экшон — это текст-инструкция для агента, который подключает директива
> («как запустить dev», «как выпустить релиз»), и в решении про требования прямо записано, что
> кнопкой он не становится: кнопка — воздействие без документа. Ты предлагаешь исполняемый экшон
> со скриптом, промптом и формой. Это тот же файл в `_actions/`, который получает исполнитель и
> остаётся текстом для директив, или отдельная сущность рядом, а текстовые экшоны живут как есть?
> Предлагаю одну сущность: у файла появляется блок «как запускать», а документом воздействия
> вместо директивы становится сохранённый прогон — кто, когда, с какими данными формы, лог и итог.
Думаю что тут так. 

Промпт экшон прям запускается в отдельном хедлесс клоде. Если нужно в сессии своей то есть директива
Запуск экшона можно сделать через мост из ui или через mcp  (оба отображаются в ui как запуски)

Экшон теперь не маркдаун файл, а json такой же как и у метрики. Так как по сути они очень близки просто sqrs так сказать. Но только тут трансформов нету


> [AI] Фаза 1. Если экшон теперь json с исполнителем, как у метрики, что делать с шестью текущими
> markdown-экшонами — «как выпустить релиз», «как запустить dev», «как опубликовать версию» и
> другими? Это знание для агента, которое директивы подключают по ссылке, а не то, что жмут кнопкой.
> Предлагаю: каждый становится json-экшоном с исполнителем «промпт», текст переезжает в его поле
> `prompt`. Кнопкой он запускается в headless-агенте, а директива по-прежнему на него ссылается:
> агент в сессии читает этот промпт через MCP и выполняет сам. Отдельной «текстовой» разновидности
> экшона тогда не остаётся.

Да всё верно, если нужна инструкция. Она будет просто выноситься в док, и ссылаться метрикой. Без отдельной сущности экшона

> [AI] Фаза 1. Где живёт запись о прогоне экшона. Раньше экшон не становился кнопкой потому, что
> воздействие должно оставлять документ, и этим документом была директива. Теперь документ —
> сам прогон: кто и когда запустил, данные формы, лог, итог. Если прогоны лежат в gitignore или
> в состоянии редактора, запись видна только на этой машине и теряется при чистке — «почему
> это стало так» снова исчезает, как с промптом в чат. Предлагаю два слоя: полный лог и вывод
> этапов — локально и с ограничением по числу (они жирные), а короткая строка «кто, когда,
> с какими данными, чем кончилось» — в git рядом с экшоном, как состояние прогона директивы.
> Или прогон экшона считаешь операционкой, которой в истории проекта не место?
Да как раз для операционки они и создаются. То есть состояние прогона чисто локальноая штука

> [AI] Фаза 1. Раздел про запуск экшонов из дисплеев оборвался на «вместо ссылок из дисплеев
> открывать нормально…». Понимаю так: у элемента списка или дерева, кроме ссылки, может стоять
> экшон — например, «перезапустить этот тест» на строке упавшего теста. Предлагаю: элемент несёт
> адрес экшона и значения полей его формы, взятые из самого элемента; клик открывает форму с уже
> подставленными значениями, а если все поля заполнены и форма не требует подтверждения — запускает
> сразу. Так ли, или ты имел в виду другое?
Да всё так

> [AI] Фаза 1. Экран прогонов. Сейчас лог метрики — один файл на последний прогон, истории нет.
> Предлагаю одну вкладку на карту, как список запусков в GitHub Actions: все прогоны метрик и
> экшонов, свежие сверху, с фильтром по объекту и статусу. Клик открывает прогон: конфиг после
> подстановок, данные формы, шаги по порядку (у метрики — коллекторы и трансформы, у экшона — его
> шаги), у каждого шага лог, вывод и время. История лежит в папке карты под gitignore, по
> несколько последних прогонов на метрику или экшон, и переживает перезапуск окна. Годится, или
> тебе хватит экрана одного объекта без общего списка?
Общий список бесмысленный, так как карта так строится что бы работа была зонами
Можем эту вкладку сделать прям воъекте по типу того как метаданные сделали

> [AI] Фаза 1. Что остаётся после экшона, кроме лога и статуса. Экшон чаще всего меняет то, что
> показывают метрики того же объекта: прогнал тесты — устарела метрика тестов, выпустил версию —
> устарела метрика версии. Предлагаю две вещи. Вывод экшона показывается тем же дисплеем, что у
> метрик, по умолчанию как markdown-текст. И у экшона есть список метрик объекта, которые после
> успешного прогона пересобираются сами, чтобы на карте не висело старое значение рядом со
> свежим запуском. Нужно ли второе, или пересборку оставим человеку?
Второе нужно, первое наврядли, по сути все действия экшона должны отображаться в метриках. Этакий sqrs

> [AI] Фаза 2. Права агента у экшона-промпта. Сейчас метрика с промптом запускает `claude` просто
> с текстом на входе, без флагов разрешений: в таком режиме агент читает, а правку файла или
> команду ему некому одобрить, и он её пропускает. Для чтения этого хватало, экшону же нужно
> именно менять. Предлагаю, чтобы экшон сам объявлял, что агенту разрешено: список инструментов
> и команд, а не «разрешить всё». Полный обход разрешений тогда включается явно, отдельным
> полем в конфиге экшона, и виден в файле при ревью. Или экшонам карты ты доверяешь целиком?
Нууу, давай добавим 2 режима, но вообще я на байпасе сижу пожизни) 

Но так как это инструмент публичный, там точно нужен режим с доступами, которые ai будет заполнять

> [AI] Фаза 2. Одновременные прогоны. Экшон идёт минутами, и за это время его легко нажать ещё раз
> или запустить соседний экшон на том же объекте, который трогает те же файлы. Предлагаю: у одного
> экшона на объекте идёт не больше одного прогона. Повторное нажатие не запускает второй, а
> открывает идущий во вкладке прогонов, и там же есть «остановить», который снимает процесс. Разные
> экшоны идут параллельно, а за конфликты по файлам отвечает тот, кто их нажал. Или нужна очередь
> на объект, чтобы экшоны одного объекта шли строго по одному?
Тут возможна паралельность, так как параметризация есть и может захотеться запустить несколько экшоноа сразу 

Так что каждый запуск свой док лог, и тд

> [AI] Фаза 3. Формат схемы формы. Есть два пути: стандартная JSON Schema, которую любой агент
> заполняет без подсказок, но по которой форму рисовать сложно — она описывает данные, а не поля;
> или свой короткий список полей: имя, подпись, вид (строка, многострочный текст, число, флажок,
> выбор из вариантов), умолчание и обязательность. Предлагаю свой список: умолчание в нём
> подставляется так же, как в метриках (`${{ … }}` из свойств объекта), а значения доезжают до
> скрипта одним JSON во входе и переменными окружения, до промпта — подстановкой в текст.
> Годится, или хочешь JSON Schema ради совместимости?

Давай свой формат, но тоже более менее стандартный

> [AI] Фаза 3. Где кнопки экшонов на объекте. Метрики у объекта разложены по вкладкам, и метрику,
> не названную ни в одной вкладке, не показывают вовсе. Для экшонов предлагаю то же: вкладка
> перечисляет свои экшоны рядом со своими метриками, они стоят рядом кнопками над метриками вкладки,
> а у объекта без вкладок все экшоны — одним рядом в шапке. Тогда «тесты» и «перезапустить тесты»
> оказываются на одном экране. Или экшоны должны быть одним рядом в шапке всегда, вне вкладок?
Список всех экшонов точно удобная штука. Это скорее всего кнопка которая открывает селект с поиском (Думаю из прототипа их может много прилетать)

Но конкретные экшоны можно выводить в gird-ares в виде кнопок

> [AI] Фаза 4. Директива спорит с решением о требованиях: там записано, что экшон остаётся текстом
> и кнопкой не становится, потому что воздействие без документа ломает главное правило модели —
> каждое изменение оставляет запись о причине. Прогоны же у нас теперь локальные и в git не
> попадают. Предлагаю новое решение, заменяющее тот пункт, с границей: экшон — операционка
> (запустить, прогнать, выпустить), а изменение самого устройства проекта по-прежнему идёт только
> директивой. Тогда «Добавить требование» и «Реализовать требование» кнопками не становятся, а
> уезжают в доки, как ты и сказал про инструкции. Граница верная?
Да верная граница + Директивы это один из вариантов работы, но кто то может только через кэшоны выстроить работу карты

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — экшон в модели карты.
>
> `packages/core/src/model` — типы карты и схемы файлов. В `schema.ts` появляется `ActionConfig`,
> рядом с конфигом метрики: `label`, `extends`, `timeout`, `runners` (как коллекторы: `script`,
> `prompt` и открытый `kind` для будущих видов), `inputs` — поля формы, `refreshes` — ключи метрик
> объекта, которые пересобираются после успеха. У промпт-исполнителя — `permissions`: либо
> `bypass`, либо список разрешённых инструментов и команд. В `model.ts` `actions: MapFile[]`
> меняется на `MapAction[]` — ключ, адрес, путь конфига, слои и `owner`, как у `MapMetric`.
>
> `packages/abstract-server/src/features/map-object` — сборка объекта. В `read-map.ts` экшоны
> читаются папками `_actions/<ключ>/config.json`, как метрики, и наследуются тем же путём: `extends`
> на другой экшон и по прототипу, своё выигрывает.
>
> Решил сам: экшон — папка, а не одиночный json, ради одинакового адреса с метриками и места под
> скрипт рядом. Поля формы называются как в ручном запуске GitHub Actions: `type` (`string`,
> `boolean`, `number`, `choice`), `description`, `required`, `default`, `options`; умолчание проходит
> подстановку `${{ … }}`. Markdown в `_actions/` больше не читается.

> [AI] План, кусок 2 — прогон экшона на сервере.
>
> `packages/abstract-server/src/features/action-runs/` — новая фича рядом с `directive-turns`.
> В `domain` — запись прогона: номер, адрес экшона и объекта, данные формы, статус (идёт, успех,
> ошибка, остановлен), шаги по порядку со своим логом, выводом и временем. В `application` —
> хранилище прогонов по образцу списка «ждут ответа»: запуск отдаёт номер сразу, прогоны одного
> экшона идут параллельно, остановка снимает процесс по номеру, подписчик видит изменения живьём.
> После успеха хранилище просит хранилище метрик пересобрать ключи из `refreshes`.
>
> `packages/abstract-server/src/features/map-object/application/use-cases/collect.ts` — исполнители
> `script` и `prompt` выносятся отсюда в общий кусок, чтобы экшон и коллектор запускали команду и
> агента одним кодом. Данные формы доезжают до скрипта JSON-ом во входе и переменными
> `MAPWARD_INPUT_<ИМЯ>`, до промпта — подстановкой.
>
> `packages/abstract-server/src/ports` — `AgentPort.run` получает разрешения; как превратить их во
> флаги `claude`, решает адаптер в расширении и CLI.
>
> Решил сам: прогоны лежат в `.mapward/runs/` в корне карты, по двадцать последних на экшон, а
> в `.mapward/` карта сама кладёт `.gitignore` со звёздочкой — править корневой `.gitignore`
> человеку не нужно. Прогоны метрик пишутся туда же: экрану прогонов нужна история и у них.

> [AI] План, кусок 3 — как экшон запускают: интерфейс, агент и терминал.
>
> `packages/core/src/contracts` — контракт между сервером и видами. Рядом с `turns.ts` появляется
> `runs.ts`: вызовы `runAction` (адрес экшона и данные формы → номер прогона) и `stopRun`
> (номер), подписка `watchRuns` на прогоны одного объекта — ей питается вкладка прогонов.
>
> `packages/abstract-server/src/entry/mcp.ts` — инструменты агента. `run_action` с адресом экшона
> и данными формы, по образцу `run_metric`: по умолчанию ждёт конца, с `wait: false` сразу отдаёт
> номер. `read_object` показывает экшоны объекта с их формой, чтобы агент знал, что заполнять.
>
> `apps/cli/src/cli.ts` — команда `mapward action <адрес> [--input имя=значение]`, печатает лог
> по ходу и выходит с ненулевым кодом при ошибке.
>
> Решил сам: запуск через MCP не требует нового правила — документация уже приравнивает вызов
> метрики к нажатию кнопки, а экшон встаёт туда же. Данные формы проверяются на сервере, а не в
> виде: иначе MCP и CLI обошли бы обязательные поля.

> [AI] План, кусок 4 — запуск экшона из интерфейса: шапка, сетка, форма.
>
> `packages/abstract-react-client/src/features/map-object/ui/object-header.tsx` — шапка объекта.
> Рядом с меню терминалов встаёт кнопка «экшоны»: выпадающий список с поиском по всем экшонам
> объекта, включая доставшиеся от прототипа. Шапка держит то, что делают, — экшон туда и просится.
>
> `packages/abstract-react-client/src/features/map-object/_metrics/` — сетка метрик. Раскладка уже
> называет клетки ключами метрик; ключ экшона в той же раскладке рисует кнопку экшона в этой клетке.
>
> `packages/abstract-react-client/src/features/map-object/_actions/` — новая папка по образцу
> `_metrics`: форма экшона, собранная из его полей. Всё заполнено и в конфиге нет `confirm: true`
> (это поле добавляется к куску 1) — запуск сразу, без формы; иначе форма, и ошибки сервера о полях показываются у самих полей.
>
> Решил сам: ключи метрик и экшонов одного объекта не должны совпадать — совпадение показывается
> ошибкой объекта, как неверная раскладка, а не угадывается. Список с поиском сделан внутри вида,
> а не окном редактора: так он работает и там, где редактора нет.
Тут ок но вопрос. Мы делаем поддержку переиспользуемых экшонов как это делаем с метриками?

> [AI] Да, так же, как метрики, и двумя путями — это заложено в куске 1, скажу явно. Первый путь —
> прототип: экшон, заведённый у прототипа «Пакет», достаётся каждому пакету, свой с тем же ключом
> его перекрывает. Второй — `extends`: у объекта в `_actions/<ключ>/config.json` одна строка
> `{ "extends": "mapward://shared-actions/release" }`, как сейчас метрики пакетов ссылаются на
> `shared-metrics`. Общий экшон ничего не знает о том, где его запустят: пути и имена он берёт
> подстановкой `${{ mapward://~#props.… }}`, и она разрешается по объекту, на котором нажали, а
> не там, где лежит файл. Прогоны при этом у каждого объекта свои, как кэш у метрик. Папку
> `shared-actions` в нашей карте заведём, когда появится первый общий экшон, а не заранее.

> [AI] План, кусок 5 — экшон на строке дисплея.
>
> `packages/core/src/model/display.ts` — формы данных дисплеев. У элемента списка и узла дерева,
> рядом с `link`, появляется `action`: `{ "run": "<ключ или адрес экшона>", "inputs": { … } }`.
> Ключ ищется у объекта метрики, адрес — где угодно на карте. Подсказка агенту о форме ответа
> (она уезжает в промпт трансформа) дополняется этим полем, чтобы промпт-метрика умела его ставить.
>
> `packages/abstract-server/src/features/map-object/application/use-cases/display-schema.ts` —
> проверка формы. Экшон, которого нет, — ошибка строки, а не всей метрики: остальные строки видны.
>
> `packages/abstract-react-client/src/features/map-object/_metrics/ui/` — рисование дисплеев. У
> строки с экшоном появляется кнопка запуска справа, клик по самой строке по-прежнему ведёт по
> `link`. Нажатие идёт тем же путём, что кнопка в шапке: форма с подставленными `inputs` или запуск
> сразу. `display-kit.tsx` получает `runAction` рядом с `onOpen` — им пользуются и свои компоненты.
>
> Решил сам: у строки и ссылка, и экшон сразу, а не одно вместо другого — у упавшего теста нужно
> и открыть файл, и перезапустить.

> [AI] План, кусок 6 — экран прогонов в объекте и тихие метрики.
>
> `packages/abstract-react-client/src/features/map-object/_runs/` — новая папка по образцу `_meta`.
> Экран прогонов открывается кнопкой в шапке, так же как «Об объекте», и тем же способом попадает
> в историю «назад-вперёд». Слева — прогоны объекта, свежие сверху: экшон или метрика, кто запустил
> (кнопка, агент, терминал), время, статус; идущий обновляется живьём по подписке из куска 3.
> Справа — выбранный прогон: конфиг после подстановок, данные формы, шаги по порядку, у каждого
> лог и вывод, и «остановить», пока он идёт.
>
> `packages/abstract-react-client/src/features/map-object/_metrics/ui/metric-cell.tsx` — клетка
> метрики. Зелёная точка удачного прогона уходит, остаётся только красная; по клику на неё
> открывается экран прогонов на последнем прогоне этой метрики — туда, где объяснено, почему.
>
> Решил сам: «кто запустил» — это откуда пришёл вызов (мост, MCP, CLI), а не имя человека: имени
> карта не знает, а локальным прогонам оно и не нужно.

> [AI] План, кусок 7 — решение и документация инструмента.
>
> `ru/docs/decisions/` — новое решение `0038` (номер `0037` уже занят незакрытой директивой про
> компонент-дисплей). Оно заменяет пункт «экшон остаётся текстом» в решении о требованиях: экшон —
> исполняемая операционка с формой и локальными прогонами; изменение устройства проекта у нас
> по-прежнему идёт директивой, но инструмент эту границу не навязывает. Старому пункту — пометка
> о замене цитатой в начале.
>
> `packages/docs/ru/` — дока, которую агент получает через `read_docs`. `directives.md`: раздел
> «Экшоны» переписан — экшон больше не текст и становится кнопкой. Новый раздел `actions.md` рядом
> с `metrics.md`: конфиг, исполнители, форма, разрешения, прогоны, экшон на строке дисплея, общие
> экшоны. `mcp.md`: `run_action` в таблице инструментов и уточнение «читать — всё, писать —
> ничего», что запуск экшона, как и метрики, — то же нажатие кнопки. `metrics.md`: только красная
> точка и история прогонов. `index-format.md`: ключ экшона в раскладке.
>
> Решил сам: дока собирается в `read_docs` через `bundle-docs.mjs`, поэтому после правки её
> пересобираю, иначе агент будет читать старый текст.

> [AI] План, кусок 8 — что становится с нашими шестью экшонами и требованиями.
>
> `ru/map/apps/vscode-application/_actions/` — экшоны расширения. «Выпустить версию» становится
> скриптом: собрать `.vsix` и поставить его в свой редактор, с `confirm: true`. «Запустить dev»
> уезжает в доку: он открывает окно отладки редактора, headless его не запустить.
>
> `ru/map/prototypes/package/_actions/` — экшоны пакетов. «Опубликовать версию» становится
> промптом с формой (какой пакет, какой уровень версии) и `confirm: true`. «Создать пакет» меняет
> устройство проекта — уезжает в доку.
>
> `ru/map/prototypes/system/_actions/` — «Добавить требование» и «Реализовать требование» меняют
> устройство проекта и уезжают в доки `ru/docs/development-guide/`. Прототип «Система» ссылается
> на них метрикой-ссылкой, как ты предложил, — директива находит их там же, где раньше экшоны.
> `ru/docs/development-guide/actions.md` и строка «статус ставит экшон» в `requirements.md`
> переписываются под это.
>
> `ru/requirements/` — реестр требований. Требования про экшоны в `mcp/` и `ui/директивы/`
> правятся под новый экшон, новые — в `ru/requirements/экшоны/`, по одному на кусок плана.

> [AI] План собран, давай переходить к реализации.

Вопрос, что ещё нужно доработать, если появился дисплей в виде react компонента, туда тоже удобно прокидывать экшоны

> [AI] План, кусок 9 — экшоны в дисплее-компоненте. В куске 5 это было одной строкой про
> `runAction` в наборе, здесь целиком.
>
> `packages/display/src/index.ts` — типы для автора компонента. В `DisplayProps` рядом с `open`
> появляются `actions` — экшоны объекта: ключ, подпись, поля формы, сколько прогонов идёт сейчас —
> и `run(action, inputs?)`. Он ведёт тем же путём, что кнопка в шапке: форма, если полей не хватает
> или стоит `confirm`, иначе сразу запуск. `LinkItem` получает то же поле `action`, что строки
> списка, поэтому `List` и `FileTree` из набора рисуют кнопку сами. В наборе появляется
> `ActionButton` — та же кнопка, что в сетке, для своей разметки.
>
> `packages/abstract-react-client/src/features/map-object/_metrics/ui/` — `component-display.tsx`
> и `display-kit.tsx` передают экшоны и запуск компоненту так же, как сейчас ссылки.
>
> `apps/cli/src/display-check.ts` — `mapward display check`: в `display.data.d.ts` попадает тип
> поля `action`, чтобы схема данных компонента могла его описать.
>
> Решил сам: компонент зовёт экшон по ключу своего объекта или по полному адресу, как строка
> списка, а в экране прогонов такой запуск помечен «из дисплея».

> [AI] План собран, давай переходить к реализации.
