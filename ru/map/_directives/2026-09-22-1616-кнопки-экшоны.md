## Задача

Экшоны сейчас — текстовые инструкции, которые подключает директива; по кнопке их не запустить, а
метрика для воздействий не годится — она читает, а не пишет. Нужен исполняемый экшон по образцу
метрики: скрипт, промпт, будущие виды, форма для данных, логи и статус. Прогоны метрик и экшонов
должны быть видны — этапы, логи, результаты, как в GitHub Actions. Зелёные точки на метриках
шумят — оставить только красную. Экшоны нужно запускать и со строк дисплеев.

## Решено

- **сущность** — экшон — json в `_actions/<ключ>/config.json`, как метрика, но без трансформов;
  отдельной «текстовой» разновидности нет. Инструкция для агента выносится в доку, на неё
  ссылается метрика
- **где идёт промпт** — в отдельном headless-агенте; нужен в своей сессии — это директива
- **кто запускает** — интерфейс через мост, агент через MCP, терминал через CLI; все запуски видны
  в интерфейсе как прогоны
- **прогоны** — операционка, живут только локально, в git не попадают
- **результат** — своего вывода у экшона нет, всё, что он сделал, видно в метриках (CQRS); у
  экшона список метрик, которые пересобираются после успеха
- **экран прогонов** — вкладка в самом объекте, как экран «Об объекте»; общего списка на карту
  нет — карта строится зонами
- **экшон на строке дисплея** — строка несёт экшон и значения формы; клик открывает форму с
  подставленными значениями, а если всё заполнено и подтверждение не нужно — запускает сразу
- **права агента** — два режима: полный обход разрешений и список разрешённых инструментов и
  команд, который заполняет AI; второй нужен, потому что инструмент публичный
- **параллельность** — прогоны одного экшона идут параллельно (параметризация), у каждого свой
  лог и запись
- **форма** — свой формат полей, но близкий к стандартному
- **размещение** — у объекта кнопка со списком всех экшонов с поиском (от прототипа их много);
  отдельные экшоны ставятся кнопками в раскладку сетки
- **граница** — экшон — операционка (запустить, прогнать, выпустить); изменение устройства
  проекта у нас идёт директивой. Инструмент эту границу не навязывает: карту можно вести и
  только экшонами
- **переиспользование** — как у метрик: по прототипу и через `extends` на общий экшон; подстановки
  разрешаются по объекту, на котором нажали; прогоны у каждого объекта свои

## План

1. **Модель.** `packages/core/src/model`: в `schema.ts` — `ActionConfig`: `label`, `extends`,
   `timeout`, `confirm`, `runners` (`script`, `prompt`, открытый `kind`), `inputs`, `refreshes`; у
   `prompt` — `permissions`: `"bypass"` или список разрешённых инструментов. Поля формы — как в
   ручном запуске GitHub Actions: `type` (`string`, `boolean`, `number`, `choice`),
   `description`, `required`, `default`, `options`; `default` проходит подстановку. В `model.ts`
   `actions: MapFile[]` → `MapAction[]` по образцу `MapMetric`. `abstract-server/.../read-map.ts`:
   экшоны читаются папками `_actions/<ключ>/config.json`, наследуются через `extends` и прототип,
   своё выигрывает; markdown в `_actions/` больше не читается.
2. **Прогон на сервере.** `abstract-server/src/features/action-runs/`: запись прогона (номер,
   экшон, объект, данные формы, источник, статус, шаги с логом, выводом и временем) и хранилище:
   запуск сразу отдаёт номер, прогоны параллельны, остановка по номеру, подписка живьём, после
   успеха — пересборка `refreshes`. Исполнители `script` и `prompt` выносятся из `collect.ts` в
   общий код. Данные формы — JSON во вход скрипта и `MAPWARD_INPUT_<ИМЯ>`, в промпт —
   подстановкой. `AgentPort.run` получает разрешения, во флаги `claude` их превращают адаптеры.
   Прогоны лежат в `.mapward/runs/` в корне карты, по двадцать последних на экшон; в `.mapward/`
   карта кладёт `.gitignore` со звёздочкой. Прогоны метрик пишутся туда же.
3. **Контракт, MCP, CLI.** `core/src/contracts/runs.ts`: `runAction`, `stopRun`, подписка
   `watchRuns` по объекту. `entry/mcp.ts`: `run_action` по образцу `run_metric` (с `wait: false`
   отдаёт номер сразу); `read_object` показывает экшоны с формой. `apps/cli`: `mapward action
   <адрес> [--input имя=значение]`, лог по ходу, ненулевой код при ошибке. Данные формы
   проверяются на сервере. Запуск через MCP приравнен к нажатию кнопки, как у метрик.
4. **Интерфейс запуска.** `abstract-react-client/.../map-object`: в шапке кнопка «экшоны» со
   списком и поиском внутри вида; ключ экшона в раскладке сетки рисует кнопку в клетке; новая
   папка `_actions/` — форма из полей экшона, ошибки сервера у полей. Всё заполнено и нет
   `confirm: true` — запуск без формы. Совпадение ключей метрики и экшона на объекте — ошибка
   объекта.
5. **Экшон на строке дисплея.** `core/src/model/display.ts`: у элемента списка и узла дерева поле
   `action: { run, inputs }` (ключ экшона объекта или адрес), подсказка агенту о форме знает это
   поле. `display-schema.ts`: неизвестный экшон — ошибка строки, а не метрики. Клиент: кнопка
   запуска справа у строки, ссылка при этом остаётся.
6. **Экран прогонов.** Клиент, папка `_runs/` по образцу `_meta`: кнопка в шапке, история
   «назад-вперёд»; слева прогоны объекта (экшон или метрика, источник, время, статус), справа
   выбранный: конфиг после подстановок, данные формы, шаги с логами, «остановить». В
   `metric-cell.tsx` зелёная точка уходит, красная ведёт на последний прогон метрики. Источник —
   откуда пришёл вызов (мост, MCP, CLI, дисплей), не имя человека.
7. **Решение и дока.** `ru/docs/decisions/0038` заменяет пункт «экшон остаётся текстом» решения о
   требованиях (у старого — цитата о замене). `packages/docs/ru/`: `directives.md` — раздел
   «Экшоны» переписан; новый `actions.md`; `mcp.md` — `run_action` и уточнение про запись;
   `metrics.md` — красная точка и история; `index-format.md` — ключ экшона в раскладке. Дока
   пересобирается `bundle-docs.mjs`.
8. **Наши экшоны и реестр.** «Выпустить версию» расширения — скрипт с `confirm`. «Опубликовать
   версию» пакета — промпт с формой и `confirm`. «Запустить dev», «Создать пакет», «Добавить
   требование», «Реализовать требование» — в доки `ru/docs/development-guide/`, прототип
   ссылается на них метрикой-ссылкой; `actions.md` и строка «статус ставит экшон» в
   `requirements.md` переписываются. Требования про экшоны в `ru/requirements/mcp/` и
   `ru/requirements/ui/директивы/` правятся, новые — в `ru/requirements/экшоны/`.
9. **Экшоны в компоненте-дисплее.** `packages/display/src/index.ts`: в `DisplayProps` — `actions`
   (ключ, подпись, поля, сколько идёт) и `run(action, inputs?)`; `LinkItem.action`, кнопку рисуют
   `List` и `FileTree`; в наборе `ActionButton`. `component-display.tsx`, `display-kit.tsx`
   передают экшоны и запуск. `display-check.ts`: тип `action` в `display.data.d.ts`.

## Развороты

- Сперва предлагалось оставить экшон markdown-файлом с блоком «как запускать» — автор развернул
  в json по образцу метрики, а инструкции — в доки.
- Запись о прогоне предлагалось держать в git как документ воздействия — автор счёл прогоны
  операционкой, отсюда граница «операционка — экшон, устройство — директива» и новое решение.
- Один прогон на экшон и общий экран прогонов на карту — оба отклонены: параллельно и вкладкой
  в объекте.

Сжато до коммита `531a993`.

> [AI] Сжал до коммита `531a993`. Сделаны все девять кусков: модель и сборка экшонов, хранилище
> прогонов на сервере (параллельные прогоны, остановка, таймаут, `.mapward/runs/` со своим
> `.gitignore`, прогоны метрик туда же), `run_action` и `read_run` в MCP, `mapward action` в
> терминале, кнопка экшонов в шапке и в сетке, форма, экшон на строке дисплея и в компоненте,
> экран прогонов, только красная точка. Решение 0038, дока инструмента, доки нашей карты, два
> наших экшона («Собрать и поставить», «Опубликовать версию») и метрика «Как у нас делается»
> вместо шести markdown-экшонов, требования в реестре.
>
> Где взял умолчание: метрика «Собрать и поставить» была записью под видом метрики — стала
> экшоном на вкладке «Публикация»; новые требования заведены `draft`, как положено тексту агента,
> хотя сделаны; проверка полей формы переехала в `core`, чтобы клиент решал «нужна ли форма» той
> же функцией, что сервер; поле `action` в схеме компонента описывается `{ "$ref": "mapward:action" }`;
> `run()` из компонента с неизвестным экшоном пока молчит. В живом окне интерфейс не проверен —
> только тестами; проверю после установки.
>
> Сделал, давай переходить к тестированию.

Правки: 
Давай добавим фильтр экшонов и метрик

+ Метрики собираются супер часто, у нас есть лемит хранимых запусков? 

Кнопка остановить на промпте не работает

Не видно логов прогона промптов метрик

Добавь кнопки в метрики на react дисплее

+ На странице раннера выводить бы в json результат коллекта
> [AI] Фаза 1. Фильтр и частые метрики — про одно: экран прогонов тонет в сборах метрик. Лимит
> есть — двадцать последних прогонов на каждую метрику и каждый экшон, старые вытесняются, но
> при двадцати метриках на объекте это четыреста строк, и экшоны в них теряются. Предлагаю две
> вещи: фильтр над списком «всё / экшоны / метрики» с экшонами по умолчанию, и не писать в
> историю прогоны, которые метрика сделала сама — при открытии объекта и по интервалу, — а только
> запущенные кнопкой, агентом или после экшона. Упавший автоматический прогон при этом писать:
> красной точке нужно, куда вести. Так, или хочешь видеть и все автоматические?
Давай оставим автоматические запуски, но просто выделим в отдельную вкладку 

все экшоны метрики автоматические метрики 

Можно не 20 но 10
> [AI] Фаза 1. Кнопки на React-дисплеях. Компонентом на карте рисуются три метрики корня: решения,
> директивы и витрина набора. Экшонов у корня нет, поэтому и кнопкам сейчас нечего запускать —
> сама возможность в компоненте уже есть. Понимаю так: нужны настоящие экшоны на корне и кнопки
> к ним в этих компонентах. Предлагаю у решений — «проверить решение» на строке (агент ищет
> противоречия с остальными решениями и пишет итог в прогон), в витрине — кнопку-пример. У
> директив этап по кнопке уже запускается в терминале, а экшон идёт без сессии — что там должно
> быть кнопкой, скажи ты. Или ты имел в виду другие метрики и кнопки?
Давай сделаем какую нибудь тестову кнопку типо. Открыть папку файл в проводнике
> [AI] Понял всё, давай переходить к плану.

> [AI] Правки, кусок 1 — вкладки на экране прогонов и лимит.
>
> `packages/abstract-react-client/src/features/map-object/_runs/` — экран прогонов. Над списком
> вкладки «все», «экшоны», «метрики», «автоматические метрики»; открыта «экшоны». Какой прогон на
> какой вкладке, решает чистая функция в `pure-model/runs.ts` по виду прогона и тому, откуда он
> запущен, — с тестом рядом.
>
> `packages/core/src/contracts/runs.ts` — откуда прогон. Сейчас сбор метрики после экшона
> помечен так же, как сбор при открытии объекта и по интервалу («сам»). Добавляю отдельную
> пометку «после экшона»: такой сбор идёт на вкладку «метрики», а на «автоматические» — только то,
> что метрика запустила сама.
>
> `packages/abstract-server/src/features/action-runs/domain/history.ts` — лимит: десять последних
> прогонов на метрику или экшон вместо двадцати.
>
> Решил сам: выбранная вкладка помнится у каждого человека, как свёрнутые метрики, а не в истории
> «назад-вперёд» — это удобство, а не переход.

> [AI] Правки, кусок 2 — «остановить» снимает прогон на самом деле. Причин две.
>
> `packages/abstract-server/src/features/action-runs/` и
> `features/map-object/application/services/metric-store.ts` — прогон метрики записывается в
> историю, но остановить его хранилищу прогонов нечем: кнопка знает только прогоны экшонов.
> Стор метрик теперь отдаёт вместе с записью свою отмену, и «остановить» у прогона метрики снимает
> её сбор так же, как уход с объекта снимает `cancelOnLeave`. Прежнее значение метрики остаётся.
>
> `apps/vscode-extension/src/apps/extension/ports/index.ts` и `apps/cli/src/ports.ts` — запуск
> процессов. На Windows агент идёт через оболочку, и отмена убивала оболочку, а `claude` под ней
> продолжал работать. Отмена теперь снимает дерево процессов (`taskkill /T /F` на Windows, группу
> процессов на остальных) — и у агента, и у скриптов.
>
> Решил сам: остановленный прогон на экране — «остановлен», не «ошибка»; метрика от остановки не
> краснеет, как и сейчас при уходе с объекта.

> [AI] Правки, кусок 3 — лог промпт-метрик и JSON сбора на странице прогона.
>
> `packages/abstract-server/src/features/map-object/application/use-cases/collect.ts` и
> `transform.ts` — стадии метрики. Сейчас в прогон уходит только stderr, а агент пишет всё в
> stdout: его ответ разбирается в данные, и сам текст теряется. Стадия теперь сообщает прогону
> и лог, и вывод: в лог — stderr и, у промпта, ответ агента как он есть; в вывод — результат
> стадии JSON-ом: собранное у сбора, итог у трансформа.
>
> `packages/abstract-server/src/features/map-object/application/services/metric-store.ts` —
> передаёт это в шаг прогона. `_runs/ui/runs-view.tsx` в клиенте уже рисует у шага и лог, и вывод
> моноширинно — там правка не нужна, кроме подписей «лог» и «результат».
>
> Решил сам: вывод шага режется на двухстах килобайтах с пометкой, сколько отрезано — дерево
> файлов иначе раздует историю, которая хранится целиком. Живого лога, пока агент думает, не
> будет: `claude` без потокового режима отдаёт ответ только в конце, и это отдельная правка.

> [AI] Правки, кусок 4 — тестовая кнопка «показать в проводнике» на React-дисплеях.
>
> `ru/map/_actions/reveal/` — первый экшон корня карты. Поле формы `path` (обязательное), шаг —
> скрипт `reveal.mjs` рядом с конфигом: папку открывает в проводнике, файл — выделенным в папке.
> На Windows это `explorer`, на macOS `open -R`, на Linux `xdg-open` родительской папки.
>
> `ru/map/_metrics/demo-decisions/` и `demo-directives/` — у каждой строки кнопка «показать в
> проводнике» с путём файла решения или директивы: сбор кладёт в строку `action`, компонент
> рисует её штатной кнопкой набора, схема данных описывает поле. `demo-kit/` — витрина набора:
> отдельная `ActionButton`, показывающая корень карты.
>
> Решил сам: скрипт не доверяет коду выхода `explorer` — тот возвращает единицу и при успехе, —
> а сам проверяет, что путь существует, и падает понятной ошибкой, если нет. Экшон без
> `confirm`: он ничего не меняет, спрашивать нечего.

> [AI] План собран, давай переходить к реализации.
