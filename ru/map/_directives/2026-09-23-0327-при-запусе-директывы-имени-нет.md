
## 
Сейчас есть баг, что до финала первого прогона имя вкладки - объект а не дериктива что не удобно (это про терминал)

> [AI] Фаза 1. Похоже на гонку. Новый терминал заводится с именем «mapward: объект», и сразу
> после этого кнопка переименовывает его под этап командой редактора, которая работает над
> *активным* терминалом. А новый терминал ещё не успел стать активным — переименование либо
> проходит мимо, либо достаётся соседней вкладке. К концу этапа терминал уже активный, поэтому
> «✓» ставится правильно. Предлагаю не переименовывать новый терминал, а сразу создавать его с
> именем «объект · директива · этап». Вопрос, чтобы сверить диагноз: видел ли ты, что в этот
> момент чужая вкладка (другой терминал, открытый раньше) получала имя этой директивы?

Я на них мало смотрю, поготовь фикс предположительный, я проверю

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 из 1. Новый терминал этапа сразу рождается с именем этапа.
>
> - `apps/vscode-extension/src/apps/extension/` — сборка кнопки этапа (сервер карты +
>   терминалы). В `run-stage.ts` имя «объект · директива · этап» считается до того, как
>   терминал заведён, и новый терминал открывается уже с ним вместо «mapward: объект».
>   Переименование командой редактора остаётся только для живого терминала директивы:
>   он уже существует, и гонки с активной вкладкой там нет.
> - `apps/vscode-extension/src/features/terminals/` — терминалы-сессии. Наружу из фичи
>   дополнительно отдаётся `stageName` (сейчас он внутренний), чтобы имя собиралось одной
>   функцией и при рождении, и при переименовании, и при отметке «✓» в конце.
>
> Решил сам: префикс «mapward:» пропадает и у нового терминала — после переименования его и
> сейчас нет, имена станут одинаковыми с самого начала. Номер «2», «3» для совпавшего имени
> по-прежнему даёт `freeName`. Имя, записанное для восстановления после перезагрузки окна,
> берётся из сессии и будет верным сразу. Тест: на `stageName` тест уже есть, на само
> рождение с именем тест не пишу — это обёртка над редактором, проверишь руками.
>
> План собран, давай переходить к реализации.
