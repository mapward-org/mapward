
## Задача

Раскладка метрик неудобна: развёрнутое дерево файлов сжимает все метрики, потому что сетка
прибита к высоте окна, а прокрутки вкладки нет. Агенты, подбирая высоты рядов
(`fit-content`, `minmax`), путаются и мучают автора. Нужно, чтобы простые «метрики друг под
другом» получались сразу, а раскладка работала и в узком сайдбаре, и в широком табе.

## Решено

- **прокрутка** — сетка растёт по содержимому, область метрик прокручивается целиком; шапка,
  директивы и вкладки остаются на месте
- **дефолты** — код ничего не подставляет: ни высот рядов, ни потолков у клеток, ни
  складывания колонок в сайдбаре; всё растёт. Единственное умолчание — ряды прижаты кверху
  (`align-content: start`), лишнее место остаётся внизу
- **язык раскладки** — «как в css», своего языка нет: агенты знают css и путаются в кастоме
- **режим «во всё окно»** (хедер, футер, растянутая середина) — обычный css в `style`:
  `"height": "100%"` плюс ряд `1fr`
- **потолок у длинной метрики** — ставит раскладка сама, рядом `fit-content(…)` в `gridTemplateRows`
- **сайдбар и таб** — словарь раскладок с ключами-условиями `@container`, порядок ключей —
  порядок правил, побеждает последний подошедший
- **дока** — меньше рассказа, больше примеров и схем: агенты теряют повествование на
  больших контекстах
- **полоса прокрутки** — одно правило на весь вид: 4px, без стрелок, без тёмной дорожки,
  ползунок серый из темы редактора, при наведении ярче

## План

1. **Сетка растёт, вкладка прокручивается.** `packages/abstract-react-client/src/features/map-object/`:
   в `compose/map-object.tsx` обёртка под сетку — `overflow-auto`; в
   `_metrics/compose/metric-grid.tsx` у сетки уходит `h-full`, ставится `align-content: start`.
   Таб одной метрики остаётся во всю высоту. Превью-раскладки клиент не рисует — не трогаем.
2. **Раскладки под ширину — настоящие `@container`.** `_metrics/pure-model/grid.ts` собирает
   текст css: правило на вариант (`grid-template-areas`, колонки, `style`), одиночная раскладка —
   правило без условия; тесты рядом. `metric-grid.tsx` кладёт css тегом `<style>` под
   уникальным классом. `_metrics/ui/metric-cell.tsx` не ставит `gridArea` инлайном — клетка
   несёт метку ключа, место назначает css. Обёртке в `map-object.tsx` — `container-type:
   inline-size`. Показываются метрики, названные хотя бы в одном варианте; в варианте без
   метрики клетка скрыта; не подошло ни одно условие — стопка. `style` вставляется как есть.
3. **Решение и дока.** Новое решение `0033` в `ru/docs/decisions/`; в `0029` первый раздел
   (высоты рядов пишутся всегда, вкладка в окне) и инвариант о числе рядов помечаются
   перекрытыми цитатой, второй (метрика вне раскладки не показывается) остаётся. Требование
   `ru/requirements/ui/метрики/сетка-метрик.md` перестаёт повторять старое правило о высотах.
   `packages/docs/ru/index-format.md`: «Высота рядов» и «Раскладка под ширину» — четыре примера
   со схемой и строкой «когда брать»: стопка из одних `areas`; колонки в табе и колонка в
   сайдбаре; потолок у дерева; хедер/футер с растянутой серединой; строкой — прижатие кверху.
   `content.generated.ts` пересобирает сборка.
4. **Раскладки карты.** В `_index.json` корня, прототипов `system` и `package`,
   `apps/vscode-application`, `apps/cli-application`, `packages/abstract-react-client`,
   `packages/abstract-server`: потолки `fit-content(…)` и `max-content` убираются; минимум
   высоты остаётся только у карты детей, остальные ряды просто растут. Многоколоночные —
   словарь `min-width: 0px` (одна колонка) и `min-width: 600px` (колонки как были). Превью не
   трогаем.
5. **Полоса прокрутки.** `apps/vscode-extension/src/apps/webview/`: в `theme.css` — переменные
   ползунка `--mw-scrollbarSlider-*` из темы редактора; в `index.css` — правило на все
   прокрутки вида: 4px, стрелок нет, дорожка и угол прозрачные.

## Развороты

- Сначала предлагался потолок у клеток по умолчанию и складывание в колонку в сайдбаре — автор
  развернул на «никаких дефолтов, всё растёт».
- Для режима «во всё окно» сначала предлагалось отдельное слово в раскладке, для сайдбара —
  своё поле `narrow`; развернулись на «как в css»: `height: 100%` и `@container`.
- После реализации метрики расползались по высоте окна — добавили прижатие кверху; часть
  расползания давала старая сборка установленного расширения.

Сжато до коммита `a3cd964`

> [AI] сжал до коммита `a3cd964`
> Полоса прокрутки теперь 4px, без стрелок, дорожка прозрачная — под ней фон сайдбара, ползунок
> серый из темы редактора и светлеет при наведении. Правило одно на весь вид, лежит в стилях
> вебвью. Чёрная дорожка и стрелки были не от нас: сам VS Code красит полосы вебвью в цвет
> редактора, и пока это его правило действует, тонкую полосу нарисовать нельзя — я его снял.
> Сборка прошла; в редакторе увидишь после `install-local` и перезагрузки окна, как и прошлую
> правку.
>
> сделал, давай переходить к тестированию

> [AI] Проверил по плану — все пять пунктов сделаны: сетка растёт и прокручивается область,
> раскладки под ширину стали настоящими `@container`, решение `0033` и дока с четырьмя
> примерами на месте, потолки из раскладок карты убраны, полоса прокрутки тонкая. Тесты,
> линтер, форматирование, типы и сборка прошли. Глазами в редакторе я не смотрел — это за тобой.
>
> Я закончил, коммитить?
Комить