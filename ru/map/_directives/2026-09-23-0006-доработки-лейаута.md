
## 

Текущий лейаут оказался неудобным

Сейчас при развороте больших деревьев файлов всё начинает сжиматься. Видемо нету где то оверфлоу скролл 

+ Из за базового совета с скроллом в файловых дереявьях нейронка часто странные штуки делает

Нужно нам доработать лейаут что бы пользователям не приходилось постоянно нейронку мучать чо бы получить просто метрики друг над дружкой)

## Тред

> [AI] **Фаза 1. Кто прокручивается: страница целиком или каждая метрика в своей клетке.**
> Сейчас сетка ровно в высоту окна, и высоту каждого ряда пишет карта строкой вроде
> `fit-content(300px) … minmax(200px, 1fr)` — умолчания в коде нет, так решили в 0029, чтобы
> длинная нижняя метрика не утаскивала верхние. Отсюда обе боли: развернул дерево — ряды делят
> одну высоту окна и сжимаются, а нейронка, подбирая эти строки, путается в `fit-content` и
> `minmax`. Я предлагаю перевернуть: метрики стоят друг под другом своей высотой, прокручивается
> вкладка целиком, а у длинных метрик (дерево, списки) есть потолок по умолчанию в коде — скажем,
> полэкрана — после которого они прокручиваются внутри себя. Тогда `gridTemplateRows` карте писать
> не нужно вовсе, раскладка задаёт только кто где стоит. Цена — верхние метрики уезжают вверх при
> прокрутке, от чего 0029 и уходила. Так подходит, или тебе важно, чтобы вкладка помещалась в окно
> без прокрутки?

Как раз сейчас как будто прокрутка в принципе всего окна не работает посмотри в акордион

То есть возможность сделать грид хорошая. Например когда нужно бахнуть футер и хедер с прокруткой в середине, но часто нужно просто сетку сделать что бы всё что вылезло прокручивалось

> [AI] **Фаза 1. Как карта выбирает между двумя режимами.** Почему окно не прокручивается,
> нашёл: прокрутка в аккордеоне есть, но вид объекта прибит к высоте окна, и сетка ужимает все
> ряды так, чтобы влезть, — вылезать нечему, прокручивать нечего. Значит, режимов два: обычный —
> ряды по содержимому, вкладка растёт и прокручивается целиком; и «во всё окно» — для твоего
> случая с хедером и футером, где середина тянется и прокручивается внутри. Предлагаю не заводить
> для этого отдельного поля: обычный режим по умолчанию, а «во всё окно» включается сам, если в
> высотах рядов раскладки есть `1fr` — тянуть ряд до низа окна имеет смысл только в нём. Тогда
> простая раскладка пишет только `areas`, а сложная — то же, что сейчас. Или тебе понятнее явный
> переключатель в раскладке, чтобы режим было видно по одному слову?

Вообще агенты будут писать раскладку, подумай как агенту будет лучше и надёжнее что бы он спервого раза сделал норм раскладку а немучался

+ Важно учитывать что уже появились открытия объектов в отдельной вкладке. И нужно бы сразу лейауты под это тоже делать

> [AI] **Фаза 1. Одна раскладка на узкий сайдбар и широкий таб — или две.** Режим решил сам
> в пользу агента: явное слово в раскладке, а не угадывание по `1fr` — агенту проще написать
> слово, чем помнить, что `1fr` меняет поведение всей вкладки. Теперь про табы. Сейчас раскладка
> одна на оба места, а обещанные в доке варианты под ширину (css-условия ключами) в коде не
> работают вовсе — берётся последний, и никто на карте их не пишет. Для агента надёжнее всего,
> по-моему, так: он пишет одну раскладку под широкий таб, с колонками, а в узком сайдбаре она
> сама складывается в одну колонку в порядке чтения — слева направо, сверху вниз. Условий под
> ширину агент тогда не пишет никогда. Подходит, или бывает, что в сайдбаре ты хочешь видеть
> другой набор или порядок метрик, чем в табе?

Я дума что должен быть нормальный дефолт, но и возможность оверрайда

> [AI] **Фаза 3. Как выглядит переопределение для сайдбара.** Дефолт принят: широкая раскладка
> в узком месте сама складывается в одну колонку. Для переопределения вижу два пути. Первый —
> починить то, что уже обещано в доке: словарь раскладок с css-условиями по ширине ключами
> (`"max-width: 500px": {…}`). Гибко, но агенту надо придумать пороги в пикселях и помнить,
> что порядок ключей значим, — ровно то, на чём он сейчас и путается. Второй — одно именованное
> поле рядом с основной раскладкой, условно `narrow`: «вот так в сайдбаре», а где проходит граница
> узкого, решает код, одна на всю карту. Я за второй, а словарь с условиями убрать, раз им
> никто не пользуется. Или тебе нужны пороги под конкретные ширины, больше двух вариантов?
Думаю тут важно, что css агенты хорошо знают а наш кастом могут путать. Я бы просто сказал "Туть как в css"

> [AI] **Фаза 3. «Как в css» и для режима «во всё окно».** Раз правило — никакого своего языка,
> то и отдельное слово для режима с хедером и футером, которое я обещал две реплики назад, лишнее.
> Сейчас сетка метрик жёстко растянута кодом на всю высоту, и поэтому всё сжимается. Предлагаю:
> код высоту не задаёт, сетка растёт по содержимому и вкладка прокручивается целиком — это дефолт.
> Нужна середина, которая тянется до низа окна, — агент пишет в `style` обычный css:
> `"height": "100%"` и ряд `1fr` в `gridTemplateRows`. Ровно так он сделал бы это на любой
> странице. Цена — агент должен знать, что без `height` ряд `1fr` ничего не тянет; это тоже
> обычное поведение css, и в доке встанет одним примером. Так подходит?
Да думаю так лучше всего, 3-4 базовых примера на основные случаи. И проблем не будет

> [AI] **Фаза 3. Потолок у длинной метрики по умолчанию.** Когда сетка растёт по содержимому,
> развёрнутое дерево в тысячу строк честно вытянет вкладку на тысячу строк, и метрики под ним
> окажутся далеко внизу. Можно оставить так — чистый css, потолок агент ставит сам, если нужно.
> Я предлагаю другое: у каждой клетки метрики в коде стоит потолок по высоте, скажем, две трети
> окна, дальше она прокручивается внутри себя, а агент перебивает его обычным css в раскладке,
> когда хочет иначе. Тогда простая раскладка из одних `areas` сразу выглядит прилично, и агенту
> не надо помнить про деревья. Ставим потолок по умолчанию или пусть растёт без предела?

Все такие дефолты на практике не удобны. Просто всё растёт это просто и понятно. Если надо, то поставят в gridTemplateColumns максимальную высоту 

> [AI] **Фаза 4. Остаётся ли хоть один дефолт — складывание в одну колонку в сайдбаре.**
> Раньше мы договорились, что широкая раскладка в узком сайдбаре сама складывается в колонку,
> а переопределяется обычными css-условиями по ширине. Твой последний ответ — «такие дефолты
> неудобны, всё просто растёт» — говорит против любых подстановок кода, и эта из них последняя.
> Без неё раскладка ведёт себя ровно как css-сетка на странице: три колонки в сайдбаре так и
> остаются тремя узкими, а под сайдбар агент пишет условие `max-width` сам, по примеру из доки.
> Я бы и её убрал ради одного правила «код ничего не подставляет». Убираем, или колонки — особый
> случай, где дефолт всё-таки нужен?

Да ты прав дефлот не нужен. а просто примеры в доке. Вообще доку старайся писать меньше наративом больше примерами и схемами
ТАк как агенты на больших контекстах эти наративы быстро теряют

> [AI] понял всё, давай переходить к плану.

> [AI] **План, кусок 1. Сетка растёт, вкладка прокручивается целиком.**
> Где правка — клиент редактора, вид объекта:
> - `packages/abstract-react-client/src/features/map-object/compose/` — собирает экран объекта:
>   крошки, шапка, директивы, вкладки, сетка. В `map-object.tsx` обёртка под сетку сейчас
>   `min-h-0 flex-1` без прокрутки — станет `min-h-0 flex-1 overflow-auto`. Шапка, директивы и
>   вкладки остаются сверху на месте, прокручивается только область метрик.
> - `.../map-object/_metrics/compose/` — сетка метрик. В `metric-grid.tsx` у сетки уходит
>   `h-full`: высоту код больше не задаёт, сетка растёт по содержимому. Пишет раскладка в `style`
>   `"height": "100%"` — растягивается на высоту области, и ряд `1fr` снова тянется до низа.
> - `.../_metrics/ui/` — клетка метрики. `metric-cell.tsx` не трогаю: прокрутка внутри клетки
>   уже есть и включается сама, когда ряду задан потолок.
>
> Решил сам: таб одной метрики остаётся во всю высоту, как сейчас, — там раскладки нет, и метрика
> занимает экран, ради этого таб и открыт. Превью-раскладки карточек клиент сейчас не рисует
> вовсе, поэтому этот кусок их не касается.

> [AI] **План, кусок 2. Раскладки под ширину работают как css-условия.** Сейчас из словаря
> раскладок берётся последняя, а условие выбрасывается. Станет так: каждый ключ словаря уходит
> в настоящее `@container (…)`, порядок ключей — порядок правил, побеждает последний подошедший.
> - `.../_metrics/pure-model/` — чистая логика сетки. `grid.ts` вместо одного выбранного варианта
>   собирает текст css: правило на вариант, внутри — `grid-template-areas`, колонки и `style`
>   раскладки. Одиночная раскладка — то же правило без условия. Проверяется тестами рядом.
> - `.../_metrics/compose/` — `metric-grid.tsx` кладёт этот css тегом `<style>` рядом с сеткой,
>   под своим уникальным классом, чтобы две сетки на экране не задевали друг друга.
> - `.../_metrics/ui/` — `metric-cell.tsx` перестаёт ставить `gridArea` инлайном, клетка несёт
>   только метку с ключом метрики, а место ей назначает css.
> - `.../map-object/compose/` — обёртке под сетку в `map-object.tsx` ставится
>   `container-type: inline-size`: без этого ни одно `@container` не срабатывает.
>
> Решил сам: показываются метрики, названные хотя бы в одном варианте; в варианте, где метрики
> нет, её клетка скрыта. Не подошло ни одно условие — метрики стоят стопкой, как в css без
> правил; в доке пример покажет ключ-«всегда» первым (`min-width: 0px`). Текст `style`
> вставляется как есть, без проверки — так же, как сейчас.

> [AI] **План, кусок 3. Решение и дока: «раскладка — это css».**
> - `ru/docs/decisions/` — база решений. Новое `0033`: сетка растёт по содержимому, код ничего
>   не подставляет, условия по ширине — настоящие css-условия. В старом решении о высоте рядов
>   (там требовалось, чтобы вкладка влезала в окно, а высоты рядов писались всегда) первый раздел
>   помечается перекрытым — цитатой в начале. Его второй раздел остаётся в силе: метрика, которой
>   нет в раскладке, не показывается.
> - `packages/docs/ru/index-format.md` — дока инструмента, её агент получает через `read_docs`.
>   Разделы «Высота рядов» и «Раскладка под ширину» переписываются на примеры вместо рассказа,
>   четыре штуки: только `areas`, стопкой; колонки в табе и одна колонка в сайдбаре; потолок
>   у дерева через `fit-content`; хедер и футер с растянутой серединой через `height: 100%`
>   и `1fr`. У каждого — схема клеток и одна строка «когда брать».
> - Решил сам: копию доки в `content.generated.ts` пересобирает сборка — руками её не трогаю.

> [AI] **План, кусок 4. Раскладки самой карты — по новым правилам.** Высоты рядов сейчас пишут
> 12 раскладок в 8 файлах `_index.json` под `ru/map/`: корень (4 вкладки), прототипы `system` и
> `package`, `apps/vscode-application` (3 вкладки), `apps/cli-application`,
> `packages/abstract-react-client`, `packages/abstract-server`.
> - Потолки `fit-content(…)` и `max-content` убираю — всё растёт. Где стоит `1fr` с минимумом,
>   остаётся только минимум: `minmax(320px, auto)`. Карте детей и другим «резиновым» метрикам
>   своей высоты нет, без минимума они схлопнутся в ноль.
> - Раскладки с двумя-тремя колонками (архитектура у vscode, cli, оба пакета, прототип `package`)
>   становятся словарём из двух вариантов: `min-width: 0px` — одна колонка в том же порядке,
>   `min-width: 600px` — колонки, как сейчас. Иначе в сайдбаре они станут узкими полосками.
> - Превью-раскладки не трогаю: клиент их не рисует.
>
> Решил сам: порог 600px — примерно где кончается сайдбар и начинается таб; в доке он встанет
> тем же числом в примере.
>
> план собран, давай переходить к реализации
