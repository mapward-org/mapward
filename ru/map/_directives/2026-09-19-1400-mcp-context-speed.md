##

Ускорь сбор контекста агентом через MCP. Сейчас `read_object` отдаёт лишнее там, где агент
читает быстро, и не отдаёт нужного там, где он потом всё равно идёт в файлы.

Разобрано на прогоне проверки директивы `prototypes/package/_directives/create-package.md`:
агент собрал контекст тремя вызовами MCP и полутора десятками обращений к диску.

Требования — по экшону `mapward://prototypes/system` → «Добавить требование», реализация — по
«Реализовать требование». Объект — `mapward://packages/abstract-server`, группы
`ru/requirements/архитектура/` и `ru/requirements/директивы/`.

## Что выяснилось

Решение `0016` сработало: `refresh: "on-display"` действительно досчитал дешёвые метрики на
прототипе `Пакет`, и лучшая находка ревью пришла из MCP, а не из файлов — `read_object` по
`mapward://packages/core` показал `directives: [{ name: "create-package.md", status: "new" }]`,
хотя папки `_directives` у этого объекта нет вовсе. Директива наследуется от прототипа
(`read-map.ts`, `byName(prototype.directives, object.directives)`) и висит непрогнанной на всех
трёх пакетах разом. По файлам это не видно никак.

Дальше агент ушёл в диск и не вернулся. Причины разные, и все чинятся.

**Ответ дорогой на объект и потому не годится для обхода.** В ответе по `packages/core` —
двенадцать метрик, и у каждой полный `config` после подстановок плюс `configPath`. Только
исходники этих конфигов весят 5540 байт, из них 2.2 КБ — тексты промптов, которые агенту при
сборе контекста не нужны ни разу. Полезного в ответе — двенадцать строк `key` / `label` /
`display` / `value`.

**Обзора нет, и он заменяется `find`.** Вопрос был простой: у кого из объектов заведены метрики
`architecture` и `architecture-drift`. В карте 14 объектов; через `children` это 14 вызовов по
10 КБ, поэтому агент сделал `find ru/map -name _metrics -exec ls`. Решение `0016` закрыло обход
вверх и вниз, но обход поштучный, а вопрос был про срез.

**Директивы и экшоны отдаются без путей.** `mcp.ts:63` отдаёт у директивы `name` и `status`,
`mcp.ts:64` у экшона — одно имя строкой. Путь в модели есть (`MapFile.path`), в ответ не
попадает. Агент, которому директива говорит «перенеси файл в `_actions/`» или «загляни в
экшон», вынужден угадывать путь — и не может отличить свой файл объекта от унаследованного
от прототипа. Именно это пришлось выяснять чтением `read-map.ts`.

**Агент сам экономит на `on-display` и слепнет.** Первый вызов был с досчётом, второй, по
соседнему объекту, — с `none`, из соображения «это же просто сосед». В ответе пришли пустые
`files`, `code`, `version` — то есть ровно та слепота, от которой уходило `0016`. Причина
экономии реальная: `collectorsStaleTime` не задан ни у одной метрики карты, а без него
`stale()` в `metric-store.ts:114` всегда истина, и каждый досчёт полный.

## Что сделать

1. **Агент говорит, что читать.** У `read_object` — проекция полей, как `select` в sql:
   умолчание «всё», явный список — только перечисленное. Читается и объект, и метрики: тяжёлое
   в ответе — это `config` метрики после подстановок, а для сбора контекста нужны `key`,
   `label`, `display`, `value`. Конфиг просят, когда метрику правят.

   Записывается одним списком точечных путей — `metrics.value`, `children.name`, — а не
   отдельными параметрами на объект и на метрики. Ближе к `select`, и не придётся заводить
   новый параметр всякий раз, когда в ответе появится очередная ветка.

   Проекция — не только про вес. Она говорит серверу, чего от него хотят, и на ней держится
   пункт 3: без неё глубина упирается в тот же тяжёлый ответ, умноженный на число детей.

2. **Путь и происхождение у директив и экшонов.** В ответе у каждой — `path` и адрес объекта,
   которому файл принадлежит. На себя владелец не показывает: у своего файла поля нет, и оно
   означает ровно одно — «файл достался от прототипа, править надо там». Экшоны при этом
   перестают быть массивом строк. Новое требование в `ru/requirements/директивы/`; существующие
   `директивы-списком-со-статусами` и `экшоны-списком` не трогать — они про интерфейс.

3. **Глубина у `read_object`.** Параметр вида `depth: n`: дети приходят не именами, а так же,
   как сам объект, — в том виде, который задан проекцией из пункта 1. Это не инструмент обхода,
   против которого возражает `0016`, а глубина того же ответа: второго способа читать не
   появляется. При 14 объектах вызов по корню с достаточной глубиной заменяет всю карту целиком.
   Правка требования `агент-обходит-карту-по-ссылкам`.

4. **Глобальная настройка свежести.** В `mapward.json` — умолчание для всех метрик карты,
   которое метрика при желании перебивает своими `collectorsStaleTime` и
   `transformsStaleTime`. Стадии разведены и здесь, двумя настройками: сбор и трансформ
   протухают по-разному, и на метрике они разделены ровно поэтому — одно число заставило бы
   пересчитывать дешёвый трансформ вместе с дорогим сбором. Механизм уже есть и описан
   требованием
   `метрика-не-пересобирается-пока-свежая`, но ни одна метрика его не ставит, и потому каждое
   чтение с досчётом пересобирает всё заново.

   Колея готова: `metricsConcurrency` живёт ровно так — `RawConfig` и `parseSettings` в
   `features/maps/domain/config.ts`, дальше через `server.ts` в `metric-store.ts`. Новая
   настройка едет тем же путём.

   Критерий размещения при этом не ломается. В коде он записан комментарием у
   `metricsConcurrency`: «свойство машины и карты, а не метрики». Свежесть — свойство
   смотрящего: сколько не жалко показывать старое, решает тот, кто читает, и у человека с
   открытым экраном ответ тот же, что у агента в середине сессии.

5. **Ошибка «карта не найдена» называет карты.** `map: "mapward://"` даёт `Карта mapward:// не
   найдена` и ничего больше, хотя карта в списке одна и параметр вообще необязателен. Пусть
   текст ошибки перечисляет имена — это экономит вызов `list_maps` и одну ошибку на сессию.

6. **Промпт директивы показывает быстрый вызов.** Проекция и глубина ускоряют только того, кто
   про них знает, а промпт — весь интерфейс к агенту. Значит он называет типовой вызов для
   сбора контекста целиком: проекция под чтение, глубина, `refresh: "on-display"`. По той же
   причине, по которой `0016` вписал туда сам `on-display` со словами «запускать не бойся».
   Правка требования `промпт-ведёт-агента-в-карту`.

7. **Решение `0016` и доки.** В `0016` дописываются проекция, глубина и глобальная свежесть.
   Два места требуют осторожности:

   - «Отдельного инструмента для обхода не заводим. Навигация — свойство ответа, а не
     действие» — остаётся в силе, и `depth` ей не противоречит: это по-прежнему один ответ.
     Сказать это прямо, иначе следующий читатель увидит противоречие;
   - «Глобальной настройки нет» — сказано про таймаут и остаётся про таймаут. Рядом появляется
     глобальная свежесть, и разницу надо назвать: длительность метрики знает тот, кто её
     написал, а свежесть — тот, кто смотрит.

   Инвариант `0016` при этом переписывается. Было: всё, что видит человек, агент получает
   одним вызовом. Стало: агент может запросить всё, что видит человек, и получает ровно
   запрошенное. Глазами тоже видно то, на что смотришь, — обещание про доступность, а не
   про объём. Нарушен он теперь, когда чего-то нельзя запросить вовсе.

   Доки-проекции следом: `ru/docs/mapward/mcp.md` — таблица инструментов и параметры,
   `ru/docs/mapward/metrics.md` — свежесть, `ru/docs/mapward/config.md` — новая настройка
   рядом с адресом сервера.

8. **MCP отдаёт документацию инструмента.** Чтобы понять, как устроен `_index.json`, как
   читается `mapward://` и из чего собирается метрика, агент сейчас идёт в исходники. Это
   работает только потому, что карта ведётся в репозитории самого mapward: у пользователя
   после релиза не будет ни `packages/`, ни `ru/docs/`, а вопросы у агента останутся те же.

   Делить адресатов заново не надо, это сделало решение `0012`: `ru/docs/mapward/` — как
   работает инструмент, «отсюда вырастет публичная документация»; `development-guide/` — как
   работаем мы со своей картой.

   Значит содержимое `ru/docs/mapward/` уезжает в отдельный пакет документации. Сервер зависит
   от него и отдаёт инструментом — оглавление и раздел по имени, а не одной простынёй:
   вываливать всю документацию в контекст — ровно то, против чего эта директива целиком.
   Параметр языка закладывается сразу, значение пока одно и оно же умолчание — `ru`; внутри
   пакета доки лежат по языкам, чтобы английское зеркало из `0012` легло рядом, а не поверх.

   В карте остаётся то, что про нашу работу, а не про инструмент: `decisions/`,
   `development-guide/`, `architecture/`. Их и показывает метрика «Доки» на корне — её
   `read-dir` по `${{ mapward://@ }}/ru` переписывается под то, что осталось.

   Переезд тянет хвосты:

   - **ссылки на решения снимаются.** Из доков наружу ведут около двадцати ссылок, почти все
     вида `../decisions/NNNN-*.md`. `decisions/` публичным не становится, поэтому при переезде
     ссылка убирается, а сказанное остаётся текстом: док должен читаться сам по себе.
     Проекцией решения он быть не перестаёт — это по-прежнему `0012`, просто источник истины
     виден теперь только нам;
   - **`architecture.md` в пакет не едет.** Он описывает границы между нашими пакетами, а не
     инструмент, и переезжает в `ru/docs/architecture/` — туда же, где доки по каждому пакету.
     Имя выбрать так, чтобы не спорить с ними (`core.md`, `cli.md` и прочими); README той
     папки поправить — сейчас он ссылается на этот файл как на внешний;
   - **четыре места в карте** — `docsPath` у `core`, `abstract-server` и
     `abstract-react-client` и метрика `docs` у CLI смотрят на
     `ru/docs/mapward/architecture.md` и переезжают вместе с ним;
   - **ссылки в доки извне** — девять штук из `architecture/` и `development-guide/`
     (`../mapward/directives.md`, `../mapward/metrics.md`, `../mapward/index-format.md`).
     Чинятся путями: обе стороны остаются в репозитории;
   - **новый пакет заводится как обычный**, со строкой в `ALLOWED` в
     `dependency-direction.test.ts`, иначе тест упадёт с «нет правила». Отдельного вида пакета
     под документацию не вводить: слои в `0015` описаны для кода, у этого их просто нет, и то,
     что `architecture-drift` на нём ничего не найдёт, — не повод для новой сущности.

## Что при этом не делать

Не заводить отдельный инструмент обхода карты — `0016` отказался от него сознательно, и
пункт 3 этот отказ не отменяет.

Не менять умолчание `refresh` на `on-display`: оно выбрано в директиве от 19.09 1300, и
чтение должно оставаться чтением.

Не делать умолчанием проекцию «только дешёвое». Умолчание — «всё», как `select *`: кто не
просил, тот получает прежний ответ, и ничьи ожидания не ломаются.

Не отнимать у метрики собственные `collectorsStaleTime` и `transformsStaleTime`: глобальная
настройка — умолчание, а не замена.

Не увозить в пакет документации `development-guide/`, `decisions/` и `architecture/`. Граница
проведена решением `0012` и проходит по тому, что ломается: код поменялся — это `mapward/`,
мы переставили что-то у себя в карте — это `development-guide/`.

Не трогать формат `_directives.state` в промпте: он уедет вместе с требованием
`состояние-прогона-пишет-карта`, у которого свой статус `ready-to-implement`.

## Проверено по коду

- `read-map.ts` наследует директивы наравне с экшонами, а статус считает `readDirectives` по
  папке владельца — значит после первого прогона `create-package` станет `done` сразу у трёх
  пакетов;
- `mcp.ts:63-64` — место, где теряются пути;
- `metric-store.ts:112-116` — `stale()` без `staleTime` всегда истина;
- `collectorsStaleTime` и `transformsStaleTime` есть в схеме (`core/src/model/schema.ts:30,36`)
  и в сторе, в карте не встречаются ни разу;
- глобальная настройка ложится в готовую колею: `RawConfig` и `parseSettings`
  (`features/maps/domain/config.ts:13,43`) → `server.ts:27` → `metric-store.ts:70,75`;
- 14 объектов в карте, у 6 есть `extends`; конфигов метрик в одном ответе по `core` — 5540
  байт, промптов в карте — 5 штук на 2201 символ;
- доки инструмента агенту сейчас недоступны иначе как файлами: у корня карты есть метрика
  `Доки`, но её `read-dir` смотрит в `${{ mapward://@ }}/ru` — то есть в наш репозиторий;
- переезд `ru/docs/mapward/`: наружу из него ведут около двадцати ссылок (почти все в
  `../decisions/`), внутрь — девять из `architecture/` и `development-guide/`, плюс четыре
  места в карте через `docsPath` и метрику `docs` у CLI.

## Вопросы

> [AI] Пункт 1 меняет форму ответа для всех, кто уже его читает. Делать параметром с
> умолчанием «без конфига» или умолчание оставить прежним, а экономию включать явно? Первое
> быстрее для агента, второе никому не ломает ожиданий.

А может просто сделать как в sql "select *" или конкретные поля. То есть настройку что реально чтитаь

> [AI] Пункт 3: `depth` числом или список ключей, которые нужны у детей? Число проще, но на
> глубине 3 ответ снова становится тяжёлым — и тогда экономия из пункта 1 и есть то, что
> делает `depth` пригодным. Может, `depth` вообще имеет смысл только вместе с ним.

Грубина ок! Только нужно явно задавать что читать

> [AI] Пункт 4 — это правка карты или умолчание в коде? Можно проставить `collectorsStaleTime`
> руками десятку метрик, а можно дать `on-display`-метрикам умолчание в сервере. Первое видно
> в конфиге и честно, второе не забудется на новых метриках.

Давай сделаем глобальную настройку staleTime

> [AI] Нужно ли под это решение в `ru/docs/decisions/`? Контракт MCP из `0009` и `0016` не
> меняется по смыслу — «агент видит то же, что человек» остаётся, — но форма ответа меняется
> заметно, и появляется `depth`, про который `0016` говорил «отдельного инструмента не
> заводим». Или хватает правки требований?

Промодифицируем давай 16 если нужно + доки не забудь поправить

## Осталось решить

> [AI] Как записывается проекция? Один список точечных путей (`metrics.value`, `children.name`)
> или отдельные параметры на объект и на метрики? Первое гибче и ближе к `select`, второе
> проще проверить схемой и подсказать в описании инструмента.

Давай 1

> [AI] Инвариант `0016` говорит: всё, что человек видит на объекте, агент получает одним
> вызовом с `on-display`. С проекцией это становится «получает, если попросил». Инвариант
> переписывается под «просит всё — получает всё», или у проекции есть набор по умолчанию,
> который и держит обещание?

Мы глазами тоже не всё видем, на что смотрим то и видем)

> [AI] Глобальный `staleTime` — одно число на обе стадии или отдельно сбор и трансформ? На
> метрике они разведены, и логика та же: сбор дорогой, трансформ дешёвый.

Раздели

> [AI] Пункт 2: признак наследования — флаг `inherited` или адрес владельца? Адрес полезнее —
> по нему сразу видно, к какому прототипу идти, — но у своих файлов он равен адресу самого
> объекта, то есть шумит в каждом ответе.

Адрес владельца, но на себя не показывает


# + Важно

Нам бы добавить в принципе что бы mcp отдавало документацию по карте. Сейчас что бы узнать как карта работает модели приходится ходить в исходники. Такой роскоши не будет после релиза

> [AI] Записал пунктом 8. Осталось три развилки.

> [AI] Где живут доки инструмента в собранном виде? Markdown рядом с кодом в
> `abstract-server` и копирование при сборке, или отдельный пакет, который тянут и сервер, и
> сайт документации, когда он появится. Второе дороже сейчас и дешевле потом.

Давай отдельный пакет с документацией + обновить нужно метрику и ссылки доков

> [AI] Язык. Доки русские и лежат в `ru/`, английского зеркала по `0012` пока нет — «переводим,
> когда содержимое устоится». До тех пор MCP отдаёт русский всем, или инструмент сразу
> заводится с параметром языка и одним доступным значением?

Давай заложим параметр языка но сейчас он русский по умолчанию и доки только на русском

> [AI] Гайд конкретной карты — тем же инструментом или остаётся метрикой `Доки` на корне? Через
> метрику он уже работает и настраивается картой; через инструмент агент найдёт его, не зная,
> что такая метрика у этой карты заведена.

В метрике на карте остаются доки не по инструменту, а по тому как мы его разарбатываем и решения

По факту в отдельный пакет уезжает содержимое mapward

> [AI] Записал в пункт 8 вместе с хвостами переезда. Три развилки, и первая меняет объём.

> [AI] Чем публичная дока ссылается на решения? Из `ru/docs/mapward/` наружу ведут около
> двадцати ссылок, почти все в `../decisions/`, а по `0012` это намеренно: доки — проекции
> решений, источник истины там. У пользователя решений нет. Снять ссылки и оставить текст,
> оставить их битыми до публикации решений, или решения тоже едут — и тогда `decisions/`
> становится публичным, со всеми последствиями для того, как они пишутся.

Decisions не для публичного просмотра, поэтому при переезде убери ссылки

> [AI] `architecture.md` едет или остаётся? Он лежит в `mapward/`, но описывает границы между
> нашими пакетами — `core`, `abstract-server`, `abstract-react-client`. Пользователю это не
> нужно, а вот `docsPath` трёх пакетов и метрика `docs` у CLI смотрят именно на него. Похоже,
> его место в `ru/docs/architecture/`, и тогда же решается судьба метрики «Дока» на прототипе
> `Пакет` — она у всех трёх пакетов одинаковая и про пакет ничего не говорит.

Да пусть он переедет в arhitecture

> [AI] Пакет без кода — что с ним делают `0014` и `0015`? Строка в `ALLOWED` нужна, иначе
> `dependency-direction.test.ts` упадёт. А раскладки слоёв для пакета из markdown в `0015`
> нет, и `architecture-drift` на нём проверять нечего. Считаем его исключением и говорим это
> в решении, или заводим ему свой вид с пустой раскладкой?

Не понял, но концептуально новые сущности заводить не нужно без надобности

## Из второго прогона той же проверки

> [AI] Пункты 1–3 уже в коде: проекция полей, `depth`, `path` с владельцем у директив. Второй
> прогон проверки `create-package` шёл на них — один вызов MCP и около десяти обращений к
> диску. Что видно на реализованном.

**Проекция полей этот вызов не спасла: вес сидит в `metrics.value`.**

> [AI] Вызов по корню со списком полей из промпта — 36 КБ данных, 57 КБ в ответе с отступами,
> и `config` в нём не было ни у одной метрики. Разбивка: `requirements` 14047 байт,
> `prototypes` 6656, `docs` 5383, `children` 1852, `requirements-todo` 1521, `decisions-drift`
> 1316, `shared-metrics` 1077, `requirements-links` 188; директивы 3755, из них пути 1819;
> дети на `depth: 2` — 275 байт на всех. То есть 30 КБ из 36 — это `value` восьми метрик
> корня, а нужна была одна.
>
> Проекция режет поперёк: какие поля у каждой метрики. Нужен отбор и вдоль —
> `metrics: ["children"]` рядом с `fields`. Без него самое дорогое поле в ответе это ровно то,
> ради которого агент и звал, и отказаться от него нельзя, можно только целиком.

**Стартовый вызов всегда приходится на самый тяжёлый объект карты.**

> [AI] Директивы верхнего уровня висят на корне, а промпт говорит «начни с объекта директивы» —
> значит типовой первый вызов сессии по корню и есть. На корне 24 директивы и восемь
> метрик-обзоров. Пункт 2 добавил туда же `path` каждой директиве: 1819 байт путей, из них
> двадцать один путь — к прогнанным давно.
>
> Предложение: список директив сводкой — `total`, `new`, `changed` и имена только непрогнанных
> и изменившихся; полный список по явному запросу через проекцию. Выполненная директива это
> археология, и на старте сессии она не нужна.

**Ответ, не влезший в бюджет, пропадает целиком.**

> [AI] 36 КБ приехали одним куском, и дешёвые поля я потерял вместе с дорогими: ответ ушёл в
> файл мимо контекста, и разбирать его пришлось скриптом.
>
> Предложение: сервер сам режет самые жирные `value` и оставляет на их месте маркер вида
> `{ "truncated": true, "bytes": 14047 }`. Усечь одну метрику дешевле, чем потерять ответ, и
> какая из них жирная, знает только сервер — агент об этом узнаёт постфактум.

**Решения не адресуемы, а проверка директивы упирается именно в них.**

> [AI] Промпт режима `check` спрашивает «нет ли противоречий с решениями карты». В карте
> решения есть ровно в одном месте — путём `ru/docs/decisions` внутри промпта метрики
> `decisions-drift` на корне, и та `manual`. На объекте нет ничего: чтобы проверить
> `create-package` против `0014` и `0015`, я читал их файлами, и это половина всего похода на
> диск.
>
> Предложение: на объекте метрика «Решения» — список тех, что его держат, ссылками. Развилка:
> по пункту 8 решения не публичны, значит это метрика нашей карты, как `decisions-drift`, а не
> часть контракта MCP. Так и оставить?

**Промпт даёт готовый список полей, и его копируют не думая.**

> [AI] Я это и сделал: взял список из промпта дословно и позвал по корню. Значит типовой вызов
> из пункта 6 должен быть безопасен на худшем объекте карты, а не на среднем, — то есть нести
> отбор метрик, а не только проекцию полей.

**Обзор закрывается пунктами 1 и 3 вместе.**

> [AI] Вопрос из «Что выяснилось» — у кого заведены `architecture` и `architecture-drift` — это
> `fields: ["address", "metrics.key"]` с большой глубиной. Дети стоили 275 байт, то есть
> глубина дешёвая, дорог только `value`. Отдельный инструмент обзора и правда не нужен, но
> обещание пункта 3 «заменяет всю карту целиком» верно только вместе с отбором — стоит
> оговорить, иначе следующий читатель проверит его на корне и не поверит.
 В директиву пришёл отзыв от другого агента, доработай требования на основе этого.

## Из третьего прогона: та же проверка на доработанном MCP

> [AI] Третий прогон `create-package` шёл уже с отбором метрик, сводкой директив и бюджетом.
> Два вызова `read_object`, два `read_docs` и около двадцати обращений к диску. Обращений
> больше, чем во втором прогоне, но выросли они за счёт репозитория — сверки `package.json`,
> `tsconfig`, `vitest.config.ts` и корневых тестов. Это и есть заявленный сценарий «в код по
> ссылкам», он отработал.

**Главное: чем нет в базовом промпте, тем я не пользовался — хотя оно есть в доке и в коде.**

> [AI] Разбор промахов дал ровную корреляцию, без единого исключения. Что названо в промпте —
> `metrics`, `fields`, `depth`, `refresh`, `run_metric`, `directives: "all"`, маркер
> `truncated` — я знал и применял. Чего в промпте нет — `metrics.config`, `metrics.key`,
> `budget` — не применял ни разу, хотя `ru/mcp.md` описывает всё это прямым текстом, а
> `metrics.config` даже с оговоркой «нужен, только когда метрику правят», то есть ровно про мой
> случай.
>
> Причина не в том, что доку трудно найти. Причина в том, что **промпт пересказывает MCP и
> выглядит исчерпывающим**: там двухшаговый рецепт, готовый список полей, пример вызова и
> предупреждение про `truncated`. Прочитав его, агент не имеет повода заподозрить, что список
> неполон, — и в доку за инструментом не идёт. Я позвал `read_docs` за предметом разбора
> (`ru/directives.md`), но про сам инструмент, которым работаю, доку не открыл ни разу за весь
> прогон. Открыл только на разборе задним числом — и нашёл там всё, чего мне не хватало.
>
> Развилка: либо промпт перестаёт пересказывать и начинает обязывать — «перед первым вызовом
> прочти `read_docs ru/mcp.md`», а рецепт живёт в доке в одном экземпляре; либо пересказ
> остаётся, но тогда он часть контракта и обязан совпадать с докой — и расходиться будет при
> каждой правке. Первое дешевле: дока и так едет с инструментом, а промпт дублирует её без
> обязательства быть полным.
>
> Следствие шире этой директивы: «агент не нашёл» — это измерение промпта и доки, а не свойство
> агента. Каждый такой промах стоит читать как «здесь пересказ разошёлся с контрактом».

**Отбор метрик, сводка директив и бюджет стартовый вызов закрыли.**

> [AI] `metrics: []`, `depth: 2`, список полей из промпта — весь обзор карты одним вызовом:
> четыре пакета, два приложения, семь связей, прототипы, props. Ответ уместился в контекст и
> читался глазами, а не скриптом из файла, как 36 КБ второго прогона. Директивы приехали
> счётчиками и тремя непрогнанными из двадцати четырёх. Пункты 1–3 и оба предложения второго
> прогона работают.
>
> `owner` у унаследованной директивы дал главный факт разбора сразу: `create-package.md`
> числится непрогнанным в пяти местах — на прототипе и на всех четырёх пакетах. Отдельно
> показательно, что про `owner` в промпте тоже ничего нет, а сработал он: поле приехало в
> ответе само и объяснило себя именем. То, что приезжает без спроса, промптом не отсекается, —
> аргумент отдавать полезное по умолчанию, а не по запросу.

**`metrics.config` адресуем и лучше файла, а я пошёл в файл.**

> [AI] Претензия проверяемой директивы — «метрика Тесты сломана», проверяется она командой из
> конфига. Я прочитал `_metrics/tests/config.json` с диска, хотя `fields: ["metrics.config"]`
> отдаёт то же самое после подстановок: `--project @mapward/core` вместо
> `${{ mapward://~#props.packageName }}`. Для разбора это строго лучше файла — видно, во что
> подстановка превратилась, а спор был именно про неё.
>
> В доке это есть. В промпте `metrics` читается как «значения метрик», и мысль, что у метрики
> запрашивается ещё и устройство, взяться неоткуда.

**Списка метрик объекта я тоже не спросил, и поправка к выводу второго прогона.**

> [AI] «У кого заведены `architecture` и `architecture-drift`» я выяснял `find`-ом по
> `_metrics/`, хотя `fields: ["address", "metrics.key"]` с глубиной отвечает одним вызовом и
> дёшево. Снова: в доке правило точечных путей есть, в промпте примеры кончаются на `props`.
>
> Но и записанный во втором прогоне вывод «обзор закрывается пунктами 1 и 3» неточен. Вызов
> отдаёт **эффективный** набор: у всех четырёх пакетов список одинаковый, двенадцать ключей,
> вместе с приехавшими от прототипа. Что заведено на самом объекте — не видно. Инвариант
> `0015` сформулирован именно так, «метрика на своём объекте», и через MCP он не проверяется.
> Директивам признак происхождения пункт 2 уже дал (`owner`), метрикам — нет, а нужен он по той
> же причине и теми же словами.

**Логов метрики нет ни в контракте, ни в доке.**

> [AI] У красной метрики видно `ok: false`, и всё. Почему она красная — только в
> `collect.logs.json` на диске: `No projects matched the filter "@mapward/core"`. `metrics.logs`
> в проекции пусто, в `ru/mcp.md` слова `logs` нет. Это единственная из моих претензий, которая
> не лечится промптом.
>
> Для режима `check` существенно: «метрика сломана» проверяется текстом ошибки, а не флагом.
> Предложение: `metrics.logs` в проекцию, по умолчанию не отдавать — они жирные, и это ровно
> тот случай, ради которого делались отбор и бюджет.

**Текст директивы и экшона не отдаётся — тут дока честна, вопрос к контракту.**

> [AI] `ru/mcp.md` прямо говорит: «Экшоны и файлы директив приходят с путями». Приезжают имя,
> путь, статус и владелец; сам markdown читается файлом — то есть первое действие режима
> `check` всегда мимо карты. Предложение: текст по явному запросу и по одному файлу, не списком.

**Решения — по-прежнему половина похода на диск.**

> [AI] Подтверждаю записанное выше, третий прогон ничего не изменил: `0014`, `0015` и `0016`
> читал файлами. Разложение двадцати обращений по причинам: около шести — решения и внутренние
> доки, около пяти — устройство карты, которое в MCP есть и которое я не догадался спросить,
> около семи — репозиторий, два — текст самой директивы и экшона. Вторая группа целиком
> лечится промптом; первая — контрактом или отдельной метрикой.

**Что сработало молча и стоит не сломать.**

> [AI] `read_docs` без раздела дал оглавление, с `section` — раздел целиком; определение экшона
> я взял из доки, в исходники не ходил. Инвариант `0016` формально держится, но с оговоркой:
> держался он на промпте, а не на доке, — за устройством самого инструмента я не обратился,
> и заметно это стало только на разборе. Инвариант стоит читать строже: агент не ходил в
> исходники — не то же самое, что агент узнал устройство из `read_docs`.
>
> И маркер `«не разрешилось: mapward://~#props.codePath»` в props прототипа честнее пустой
> строки — по нему сразу видно, что у прототипа `codePath` быть и не может.

## Из четвёртого прогона: промпт отправил в доку, доку прочитал

> [AI] Четвёртый прогон проверки `create-package` шёл на промпте, где первая развилка третьего
> прогона уже выбрана: пересказа параметров нет, вместо него «первым делом позови
> `read_docs { section: "mcp" }`… считай, что здесь сказано меньше, чем умеет MCP». Шесть
> вызовов MCP (`list_maps`, `read_docs` ×3, `read_object`, `read_index`) и около двадцати пяти
> обращений к диску на полсотни файлов. Обращений больше, чем в третьем прогоне, и весь прирост
> — вне карты: исходники самого инструмента, `git log`, решения.

**Строка «MCP не подключён» приезжает всегда и сбивает ровно тот первый шаг, ради которого всё делалось.**

> [AI] `prompts.ts:47` лежит в `head` безусловно, а единственный вызывающий
> (`bridge-handler.ts:73`) признака доступности не передаёт. Мне её прислали при живом MCP —
> и первым делом я пошёл читать директиву `cat`'ом, то есть промпт сам отправил меня туда, куда
> двумя абзацами выше запретил. `read_docs` я позвал третьим действием, а не первым.
>
> Это самая дешёвая правка во всём списке и самая обидная: новая формулировка промпта отработала
> бы с первого шага, если бы её не отменяла строка ниже. Либо параметр `mcp: boolean` в
> `directivePrompt`, либо строку убрать совсем — агент без MCP увидит это по отсутствию
> инструментов, а не по тексту.

**Промпт перестал пересказывать — и это сработало.**

> [AI] Подтверждаю выбор первой развилки. Из доки я взял и применил то, чего в промпте нет
> вообще: `metrics: []`, `fields`, `depth`, сводку директив, `owner`, `read_index { file }`.
> Третий прогон не открыл доку инструмента ни разу; четвёртый открыл её первым же вызовом и
> работал по ней. Инвариант `0016` в строгом чтении («агент узнал устройство из `read_docs`»),
> которое третий прогон предложил, здесь держится.

**Но три промаха третьего прогона повторились дословно — при прочитанной доке.**

> [AI] `metrics.config`, `metrics.key`, `metrics.logs` — не применил ни одного. Конфиги метрик
> читал `cat`'ом (`_metrics/tests/config.json`, `code`, `files`, `version`, `lint`,
> `public-api`, оба `shared-metrics`), список заведённых метрик выяснял `ls _metrics/` по шести
> объектам, текст ошибки метрики «Тесты» — из `collect.logs.json` на диске.
>
> Все три описаны в `ru/mcp.md`, в разделе «Что ещё есть у метрики», который я прочитал в том же
> вызове. Значит диагноз третьего прогона — «причина в том, что промпт выглядит исчерпывающим» —
> неполон: доку прочитали целиком, и это ничего не изменило.

**Механизм другой: исполняется пример, а не текст.**

> [AI] В доке рецепт оформлен двумя блоками кода — обзор (`depth`, `metrics: []`, `fields`) и
> «потом нужные метрики нужного объекта» (`metrics`, `refresh`). Я выполнил ровно их, в том же
> порядке, и дальше действовал так, будто список параметров этим и исчерпан. Проза ниже
> прочитана и не применена — в точности как в третьем прогоне была прочитана проза промпта.
>
> То есть переезд рецепта из промпта в доку саму болезнь не лечит: пересказ стал каноническим,
> но остался пересказом. Работает не «где написано», а «что показано вызовом».
>
> Предложение: то, что должно применяться, обязано стоять в примере. Конкретно — во втором блоке
> рецепта показать `fields: ["metrics.key", "metrics.config", "metrics.logs"]` как отдельный
> пример «когда метрику разбирают, а не читают», рядом со словами «для режима `check` это и есть
> обычный вызов». Абзац про них уже есть; не хватает строки кода.

**Текст экшона приехал через `read_index { file }` — претензия третьего прогона закрыта.**

> [AI] `create-package.md` лежит у прототипа, а числится на четырёх пакетах; достать его по
> имени, не склеивая чужой путь, — ровно то, чего третий прогон просил. Работает. `owner` у
> директивы снова дал главный факт разбора сразу, без единого похода на диск.
>
> Отбор метрик, сводка директив и бюджет тоже держатся: обзор всей карты одним вызовом, ответ
> читался глазами, маркер `truncated` не понадобился ни разу.

**Решения — четвёртый прогон подряд мимо карты.**

> [AI] Режим `check` спрашивает «нет ли противоречий с решениями карты», и вся проверка прошла
> через `cat ru/docs/decisions/*.md`: `0002` (дважды — экшоны и состояние), `0012`, `0014`,
> `0015`, `0016`. Плюс `ru/docs/architecture/README.md`. Около семи обращений из двадцати пяти.
> Записанное во втором и третьем прогонах предложение — метрика «Решения» на объекте — с тех пор
> ничем не заменилось, и цифра не двигается.

**Новое: карта показывает «сейчас», а проверка директивы упирается в «когда разошлось».**

> [AI] Две самые весомые находки разбора — что `docs/mapward/directives.md` и
> `ru/docs/mapward/architecture.md` из текста директивы больше не существуют — добыты `git log`,
> а не картой. Ценность в них не в том, что ссылка битая, а в том, что обе умерли в `bd43210`
> через пять часов после того, как директива написана: это объясняет, почему автор так написал,
> и отделяет протухшее от ошибочного.
>
> Карта этого не знает в принципе: `_index.json` отдаёт текущее состояние, копия директивы в
> `_directives.state/` снимается на момент прогона, а прогона не было. Не предлагаю тащить
> историю в контракт — но стоит записать границу: **проверка директивы на протухание живёт в
> git, и промпт `check` вправе про git сказать**, как он говорит про MCP. Сейчас он не говорит,
> и я пошёл туда сам.

**«В код по ссылкам из метрик» не работает, когда проверяешь сам инструмент.**

> [AI] Центральное утверждение директивы — «состояние одно на файл» — проверяется функцией
> `readDirectives` (`read-map.ts:49`) и промптом `run` (`prompts.ts:70`). Метрика `code` даёт
> ссылку на папку пакета; найти в ней эти две точки она не помогает, и я искал `grep`'ом —
> шесть обращений. Это не дефект: карта отвечает «где», а не «что внутри». Но обещание промпта
> «в код иди по ссылкам из метрик, сканировать проект целиком не нужно» на задачах про
> устройство самого mapward не выполняется, и честнее это оговорить, чем считать промахом агента.

**`files` не спросил — тот самый промах, который описан в `0016` как повод его написать.**

> [AI] «Пустой ли `packages/core`» я выяснял через `find packages/core`, при живой метрике
> `files` и доступном `refresh: "on-display"`. Смягчающее: ответ я уже знал из текста директивы
> и лишь сверял. Но рефлекс за четыре прогона не изменился, хотя именно этот случай вынесен в
> `0016` заглавным примером. Значение метрики в режиме `check` не запрашивалось ни разу за весь
> прогон — ни одного `refresh`, ни одного `run_metric`.
>
> Похоже, для `check` это отчасти верно по существу: проверка читает замысел и устройство, а не
> числа. Но тогда стоит это признать в промпте `check` явно и назвать исключения — `files` и
> `metrics.logs`, — иначе «метрика сказала бы одним вызовом» останется правдой, которой никто
> не пользуется.

**Разложение двадцати пяти обращений по причинам.**

> [AI] Около семи — решения и внутренние доки (лечится контрактом или метрикой «Решения»).
> Около десяти — устройство карты, которое в MCP есть и которое я не спросил: конфиги метрик,
> списки ключей, логи. **Это больше, чем в третьем прогоне, при прочитанной доке** — см. блок
> про пример. Около одиннадцати — репозиторий: `package.json`, `tsconfig`, `vitest.config.ts`,
> `turbo.json`, `.oxlintrc.json`, `pnpm-workspace.yaml`, корневые тесты; заявленный сценарий,
> отработал. Шесть — исходники самого mapward, новая группа, третий прогон туда не ходил. Три —
> `git log`.
