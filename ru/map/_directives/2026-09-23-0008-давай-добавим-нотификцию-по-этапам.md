
## 

Новый флоу работы требует частых дописок пользователей в директивы

Нам нужно сделать систему нотификаций с возможностью быстро сфокусироваться на нужную директиву с терминалом по этой нотификашке

ДА так что бы эти нотификашки не пропадали сразу! Это важно так как это стек задач для человека при паралельной мультиагентной разработки

> [AI] Фаза 1. С чего начинается и чем кончается одна нотификация. Предлагаю так: появляется,
> когда агент закончил этап (`finish_directive`), то есть ход перешёл к человеку. Исчезает не
> от клика и не по таймеру, а когда человек взял ход: дописал ответ в директиву или запустил
> следующий этап. Тогда стек — это не журнал событий, который надо хранить и чистить, а просто
> список директив «ждут меня», который карта вычисляет из состояния прогона. Он переживает
> перезапуск редактора и сам не врёт. Клик по пункту — открыть директиву и терминал, где шёл
> этап. Так или тебе нужны нотификации и о чём-то кроме конца этапа (упал агент, агент ждёт
> разрешения в терминале, покраснела метрика)?

Сейчас нужны нотификации именно о finish_directive

Просто директивы которые ждут прогон часто долго его ждут, у меня в брейншторме их уже штук 6

Тут это императивно должно быть, и не должно переживать перезапуск

Что бы они не копились

Может быть просто нотификашки но которые живут подольше минут 10 например

> [AI] Фаза 1. Где их показывать, от этого зависит почти всё остальное. Первый вариант —
> обычные уведомления VS Code в правом нижнем углу, с кнопкой «Открыть» (директива и её
> терминал). Писать почти ничего не надо, но время на экране задаёт сам VS Code: через несколько
> секунд уведомление уходит в колокольчик и лежит там, пока его не закроют. Ровно десять минут на
> экране получаются только обходом: уведомление в виде «прогресса», которое мы сами закрываем по
> таймеру. Второй вариант — свой маленький стек в панели карты (или счётчик в статус-баре со
> списком по клику): десять минут жизни, клик фокусирует, но уведомление видно, только когда
> панель карты открыта. Я бы взял первый вариант с обходом через «прогресс». Какой тебе ближе?
ДАвай пока простой вариант попробуем на vscode уведах, если не ок будет то придумаем что

> [AI] Фаза 3. Какой терминал открывать по кнопке «Открыть». Сейчас расширение знает терминал
> этапа, только если этап запускали кнопкой. Этап, запущенный словами, приходит к серверу без
> указания, из какого он терминала. Можно это связать: терминалы открывает само расширение и
> вписывает в них адрес MCP, так что туда же можно дописать метку терминала (`?terminal=…`), и
> тогда любой вызов из этого терминала, хоть кнопкой, хоть словами, знает, откуда пришёл.
> Предлагаю так и сделать. Для агентов, запущенных мимо карты, кнопка откроет только директиву.
> Или пока хватит терминала только для запусков кнопкой?

Пока хватит запущеного кнопкой

> [AI] Фаза 2. Показывать ли уведомление, когда ты и так смотришь на этот этап. Если этап
> кончился в терминале, который сейчас в фокусе, или у тебя открыта сама директива, всплывашка
> на десять минут про то, что ты видишь своими глазами, — шум, и при шести параллельных
> директивах он забьёт угол экрана. Предлагаю так: уведомление не показывать, только если
> активен именно терминал этого этапа, а во всех остальных случаях (включая открытый файл
> директивы и свёрнутое окно редактора) показывать. Или проще — показывать всегда, без исключений?
Давай всплывашку всё же показывать всегда. Изменение текста директивы часто не так заметно

Кстати клик по всплывашке показывает именно директиву, (фокус на терминал тоже можно, но не обязательно)
> [AI] Фаза 4. Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — сама всплывашка.
> Новая фича `apps/vscode-extension/src/features/stage-notifications/`: показывает уведомление
> «этап кончился» и держит его на экране. Внутри одна функция: принимает объект, директиву, этап
> и путь файла директивы. Показывает уведомление «прогрессом» (`withProgress` в области
> уведомлений) с текстом «<объект> · <директива> · <этап> — твой ход». Закрывается само через
> десять минут или по крестику: у «прогресса» кнопка отмены работает как крестик. Обычное
> уведомление с кнопкой «Открыть» так не сделать: VS Code прячет его в колокольчик через
> несколько секунд, а таймер закрытия ему не задать.
> «Открыть» — это клик по ссылке в тексте уведомления: у «прогресса» своих кнопок нет, а ссылка
> на команду есть. Команда открывает файл директивы и ставит курсор в конец, на тред.
> Что ссылки в тексте «прогресса» кликаются, я проверю первым делом на реализации. Если не
> кликаются, запасной вариант — обычное уведомление с кнопкой «Открыть» без таймера: оно само
> уходит в колокольчик и лежит там, пока его не закроют.
> Если этап запускали кнопкой и его терминал ещё жив, рядом вторая ссылка — «терминал». Её
> можно выкинуть, ты говорил, что это необязательно. Десять минут — константа в коде, без
> настройки.
> Каждое уведомление живёт само по себе, поэтому шесть параллельных этапов дадут шесть отдельных
> всплывашек. Сколько из них видно сразу, решает VS Code, остальные ждут в колокольчике.

Давай обычный vscode нотификашкой пофиг про 10 минут

> [AI] План, кусок 1, вторая версия — сама всплывашка, обычным уведомлением.
> Новая фича `apps/vscode-extension/src/features/stage-notifications/`: показывает уведомление
> «этап кончился» и открывает директиву по кнопке. Внутри одна функция: принимает объект,
> директиву, этап, путь файла директивы и, если он известен, терминал этапа.
> Уведомление обычное, информационное, с текстом «<объект> · <директива> · <этап> — твой ход».
> Сколько оно видно, решает VS Code: через несколько секунд уходит в колокольчик и лежит там,
> пока его не закроют. Таймеров и своего списка в коде нет.
> Кнопка «Открыть» открывает файл директивы и ставит курсор в конец, на тред. Если этап
> запускали кнопкой и его терминал ещё жив, есть вторая кнопка — «Терминал», она показывает его
> вкладку. Терминал ищется в момент клика, а не показа: за время в колокольчике его могли
> закрыть, и тогда «Терминал» открывает директиву, как «Открыть».
> Шесть параллельных этапов дадут шесть отдельных уведомлений, одно на этап.

> [AI] Кусок 1 считаю принятым: ты запустил «План» дважды, не возразив. Если это не так,
> поправь его ответом здесь, я вернусь.
>
> План, кусок 2 — где ловится конец этапа.
> `apps/vscode-extension/src/apps/extension/` — слой сборки, где сводятся сервер карты и
> терминалы. В нём уже есть обёртка вокруг «этап кончился» (`stage-tabs.ts`), которая ставит
> отметку на вкладку терминала. Она срабатывает на любой конец этапа, и когда агента запустили
> кнопкой, и когда словами. В неё и добавляется вызов уведомления из куска 1, после того как
> сервер записал состояние. Если запись упала, уведомления нет.
> Имя объекта и путь файла директивы обёртка берёт у той же карты, из которой их берёт кнопка
> запуска: находит объект по адресу, в нём директиву по имени. Сервер карты и ответ
> `finish_directive` не меняются.
> Терминал отдельно не запоминается. Расширение и так знает, какой из открытых им терминалов
> принадлежит директиве, — по нему кнопка запуска решает, куда отправить фразу. Кнопка
> «Терминал» в момент клика спрашивает то же самое. Поэтому она есть у директив, которые хоть раз
> запускали кнопкой, пока жив их терминал.
> Если найти директиву не удалось (например, файл удалили, пока шёл этап), уведомление всё равно
> показывается, без кнопок. Ошибка уведомления не роняет ответ агенту: он получает свой ответ
> в любом случае.

> [AI] Кусок 2 считаю принятым так же, как кусок 1: ответа под ним нет.
>
> План, кусок 3 — доки и решение.
> `packages/docs/ru/directives.md` — дока инструмента, её отдаёт `read_docs`. В абзац про
> вкладку этапа дописывается: конец любого этапа, запущенного и кнопкой, и словами, в окне,
> где открыта карта, показывает уведомление с кнопкой «Открыть» и, если есть терминал
> директивы, «Терминал». Уведомление живёт, пока его не закроют, и перезапуск редактора не
> переживает.
> `ru/docs/architecture/vscode-extension.md` — устройство расширения. Рядом с абзацем про фичу
> кнопок в файле директивы — абзац про фичу `features/stage-notifications`: что она делает и
> что вызывается она из слоя сборки, из той же обёртки, что метит вкладку.
> `ru/docs/decisions/` — новое решение `0034`, «уведомление о конце этапа». Суть: появляется по
> концу этапа, обычным уведомлением VS Code, без своего хранилища; клик ведёт в директиву; связь
> прогона с терминалом только для запусков кнопкой. В решении про воркфлоу директив (0017) было
> сказано: «связывать сессию с прогоном придётся, когда понадобятся уведомления». Новое решение
> это ожидание снимает: уведомлению терминал не обязателен. Поэтому в 0017 в начало этого
> раздела ставится цитата-ссылка на 0034, а сам текст 0017 не переписывается.
>
> План собран, давай переходить к реализации.
