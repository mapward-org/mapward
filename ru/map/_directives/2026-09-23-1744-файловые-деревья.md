
## 

Сейчас не сохраняется персистентное открытие закрытие папок в файловом дереве метрики

Это очень мешает

> [AI] Фаза 1. Сейчас «открыта ли папка» живёт только в памяти строки дерева, поэтому любое
> пересоздание дерева — переход на другой объект и обратно, открытие метрики отдельным табом,
> перезагрузка окна — сворачивает всё обратно. Предлагаю запоминать так же, как уже запоминается
> свёрнутость метрик: у человека в редакторе, не в репозитории; отдельно на каждый объект, метрику
> и путь папки; для всех деревьев, а не только файлов (требования, доки); в сетке и в отдельном
> табе метрики — одно и то же состояние. Этого достаточно, или нужно что-то сверх — например,
> чтобы метрика сама задавала, какие папки открыты по умолчанию, или чтобы раскрытие было общим
> для всех, кто открывает карту?

Да этого достаточно

> [AI] Фаза 3. Дерево есть не только у встроенных метрик: тот же компонент дерева из набора
> `@mapward/display` отдаётся метрикам с собственным компонентом-отображением, и их автор
> вставляет его сам. Предлагаю, чтобы запоминание работало там само: ячейка метрики сообщает
> дереву, чей это объект и чья метрика, и автору компонента ничего делать не надо. Если в одном
> компоненте два дерева, у дерева появляется необязательное имя, чтобы их состояния не смешались.
> Годится, или в наборе для компонентов лучше не трогать API и оставить запоминание только
> встроенным метрикам?
Давай и там и там. У дерева проп id по которому и различаем

> [AI] Понял всё, давай переходить к плану.

> [AI] План, кусок 1 — где запоминается и как доезжает до дерева.
>
> - `packages/abstract-react-client/src/features/map-object/_metrics/compose` — сборка клеток
>   метрик, у неё есть доступ к хранилищу редактора. В `metric-grid.tsx` рядом с уже живущим
>   «какие метрики свёрнуты» заводится второе состояние того же рода: на объект и метрику —
>   список открытых папок. Хранится у человека в редакторе, в репозиторий не попадает. Сетка и
>   отдельный таб метрики рисуются этим же компонентом, поэтому состояние у них одно само собой.
> - `.../_metrics/ui` — отрисовка, к хранилищу доступа не имеет и не получит. `file-tree.tsx`
>   перестаёт помнить «открыта ли» внутри строки: спрашивает это и сообщает о клике через
>   пропсы, `Display` передаёт их в ветку дерева.
> - Решил сам: папка опознаётся цепочкой имён от корня, а не номером строки — так раскрытие
>   переживает пересчёт метрики, когда файлов стало больше. Исчезнувшие папки из списка не
>   вычищаю: лишние строки у человека в памяти вреда не делают. Пока сохранённое не пришло из
>   редактора, вложенные папки не рисую, чтобы дерево не мигало закрытым и не прыгало.

> [AI] План, кусок 2 — деревья в своих компонентах метрик.
>
> - `packages/display` — публичные типы набора для авторов компонентов. У `FileTree` появляется
>   необязательный проп `id`: он различает деревья внутри одной метрики. Одно дерево — `id` не
>   нужен.
> - `packages/abstract-react-client/src/features/map-object/_metrics/ui` — `display-kit.tsx`,
>   набор, который получает компонент. Раскрытие доезжает до `FileTree` набора тем же
>   контекстом, которым уже едут ссылки и кнопки экшонов: автор компонента ничего не передаёт.
> - `.../_metrics/compose` — `metric-component.tsx` кладёт в этот контекст раскрытие своей
>   метрики из первого куска.
> - Решил сам: у метрики одно хранимое состояние, внутри него — по дереву на `id`. Встроенное
>   дерево и дерево компонента без `id` делят одно имя «по умолчанию»: если у метрики сменить
>   встроенное отображение на свой компонент с одним деревом, раскрытие сохранится.
> - `packages/docs/ru/metrics.md` — в абзаце про набор одна фраза про `id` и про то, что
>   раскрытие запоминается само.

> [AI] План, кусок 3 — чистая модель раскрытия и тесты.
>
> - `packages/abstract-react-client/src/features/map-object/_metrics/pure-model` — чистая логика
>   метрик без React. Новый `tree-open.ts`: путь папки из цепочки имён, «открыта ли» и
>   «переключить» над сохранённым состоянием метрики с деревьями по `id`. Кускам 1 и 2 остаётся
>   только позвать его. Рядом `tree-open.test.ts`: путь не зависит от порядка строк, переключение
>   одного дерева не трогает соседнее, дерево без `id` и встроенное — одно и то же.
> - `apps/vscode-extension/src/apps/webview` — там уже есть тест, который разово рисует компонент
>   метрики в строку (у клиента для этого нет нужной библиотеки). В `component-display.test.ts`
>   добавляю: с сохранённым состоянием папка приходит открытой и во встроенном дереве, и в
>   `FileTree` набора с `id`.
> - Решил сам: клик так не проверить — рисунок только первого кадра. Что клик сохраняет и
>   переживает перезагрузку окна, смотрим руками на этапе «Тестирование».
>
> План собран, давай переходить к реализации.
