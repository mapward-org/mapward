{
  "updatedAt": "2026-09-20T13:31:19.221Z",
  "ok": true,
  "data": {
    "text": "! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n    ,-[packages/abstract-server/src/entry/mcp.ts:52:18]\n 51 |     ...counts,\n 52 |     files: shown.map((file) => ({\n    :                  ^|^\n    :                   `-- This map call spreads an object\n 53 |       ...fileOf(file),\n    :       ^^^^^^^|^^^^^^^\n    :              `-- These spreads allocate new values on each iteration\n 54 |       status: file.status,\n 55 |       // Идущий этап видно с первой секунды: отметку ставит run_directive — решение 0017.\n 56 |       ...(file.run === undefined ? {} : { run: file.run }),\n    :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n 57 |     })),\n    `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\n  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n     ,-[packages/abstract-server/src/entry/mcp.ts:115:21]\n 114 |     layout: { preview: object.previewLayout, details: object.detailsLayout },\n 115 |     metrics: wanted.map((metric, at) => ({\n     :                     ^|^\n     :                      `-- This map call spreads an object\n 116 |       key: metric.key,\n 117 |       address: metric.address,\n 118 |       label: metric.config.label ?? metric.key,\n 119 |       refresh: metric.config.refresh ?? \"manual\",\n 120 |       display: metric.config.display?.kind,\n 121 |       configPath: metric.configPath,\n 122 |       // Заведена не здесь, а у прототипа: инвариант 0015 спрашивает именно это.\n 123 |       ...(metric.owner === undefined ? {} : { owner: metric.owner }),\n     :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^|^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n     :                                      `-- These spreads allocate new values on each iteration\n 124 |       // Конфиг целиком: в нём видно и коллекторы, и `exclude` у карты детей.\n 125 |       config: metric.config,\n 126 |       value: values[metric.address],\n 127 |       ...(logs?.[at] === undefined ? {} : { logs: logs[at] }),\n     :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n 128 |     })),\n     `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\n  ! eslint(no-await-in-loop): Unexpected `await` inside a loop.\n    ,-[packages/abstract-server/src/entry/mcp.test.ts:94:67]\n 93 |   // Ответ уходит из промиса, поэтому ждём микротасками: прогон здесь весь на моках.\n 94 |   for (let tick = 0; tick < 1000 && replies.length === 0; tick++) await Promise.resolve();\n    :                                                                   ^^^^^\n 95 | \n    `----\n  help: Collect all promises into an array and use `Promise.all()` to run them in parallel, rather than awaiting each one sequentially inside the loop.\n\n  ! eslint(no-await-in-loop): Unexpected `await` inside a loop.\n     ,-[packages/abstract-server/src/entry/mcp.test.ts:212:17]\n 211 |   for (const { name } of index.sections as { name: string }[]) {\n 212 |     const doc = await call(\"read_docs\", { section: name });\n     :                 ^^^^^\n 213 |     const text = String(doc.text);\n     `----\n  help: Collect all promises into an array and use `Promise.all()` to run them in parallel, rather than awaiting each one sequentially inside the loop.\n\n  ! eslint(no-await-in-loop): Unexpected `await` inside a loop.\n     ,-[packages/abstract-server/src/entry/mcp.test.ts:434:67]\n 433 |   handler?.({ jsonrpc: \"2.0\", id: 1, method: \"tools/call\", params: { name, arguments: args } });\n 434 |   for (let tick = 0; tick < 1000 && replies.length === 0; tick++) await Promise.resolve();\n     :                                                                   ^^^^^\n 435 | \n     `----\n  help: Collect all promises into an array and use `Promise.all()` to run them in parallel, rather than awaiting each one sequentially inside the loop.\n\n  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n    ,-[packages/abstract-server/src/features/map-object/application/use-cases/read-map.ts:57:10]\n 56 |   return Promise.all(\n 57 |     list.map(async (file) => {\n    :          ^|^\n    :           `-- This map call spreads an object\n 58 |       const base = file.name.replace(/.md$/, \"\");\n 59 |       const state = await files.read(join(objectPath, STATE, `${base}.state.json`));\n 60 |       if (!state) return { ...file, status: \"new\" as const };\n    :                            ^^^|^^^\n    :                               `-- This spread allocates a new value on each iteration\n 61 | \n 62 |       const text = await files.read(file.path);\n 63 |       try {\n 64 |         const saved = JSON.parse(state) as {\n 65 |           directive?: string;\n 66 |           run?: { stage: string; startedAt: string; finishedAt?: string };\n 67 |         };\n 68 |         const run = saved.run;\n 69 |         // Этап, который не помечает выполнение, копии не снимает: прогон был, а директива\n 70 |         // по-прежнему не сделана — решение 0017. Без копии сравнивать не с чем.\n 71 |         if (saved.directive === undefined) return { ...file, status: \"new\" as const, run };\n 72 |         const same = norm(saved.directive) === norm(text);\n 73 |         return { ...file, status: same ? (\"done\" as const) : (\"changed\" as const), run };\n 74 |       } catch {\n 75 |         return { ...file, status: \"new\" as const };\n    :                ^^^^^^^^^^^^^^^^^|^^^^^^^^^^^^^^^^^\n    :                                 `-- Map returns the spread here\n 76 |       }\n    `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\n  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n    ,-[packages/abstract-server/src/features/map-object/application/use-cases/read-map.ts:57:10]\n 56 |   return Promise.all(\n 57 |     list.map(async (file) => {\n    :          ^|^\n    :           `-- This map call spreads an object\n 58 |       const base = file.name.replace(/.md$/, \"\");\n 59 |       const state = await files.read(join(objectPath, STATE, `${base}.state.json`));\n 60 |       if (!state) return { ...file, status: \"new\" as const };\n 61 | \n 62 |       const text = await files.read(file.path);\n 63 |       try {\n 64 |         const saved = JSON.parse(state) as {\n 65 |           directive?: string;\n 66 |           run?: { stage: string; startedAt: string; finishedAt?: string };\n 67 |         };\n 68 |         const run = saved.run;\n 69 |         // Этап, который не помечает выполнение, копии не снимает: прогон был, а директива\n 70 |         // по-прежнему не сделана — решение 0017. Без копии сравнивать не с чем.\n 71 |         if (saved.directive === undefined) return { ...file, status: \"new\" as const, run };\n    :                                                     ^^^|^^^\n    :                                                        `-- This spread allocates a new value on each iteration\n 72 |         const same = norm(saved.directive) === norm(text);\n 73 |         return { ...file, status: same ? (\"done\" as const) : (\"changed\" as const), run };\n 74 |       } catch {\n 75 |         return { ...file, status: \"new\" as const };\n    :                ^^^^^^^^^^^^^^^^^|^^^^^^^^^^^^^^^^^\n    :                                 `-- Map returns the spread here\n 76 |       }\n    `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\n  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n    ,-[packages/abstract-server/src/features/map-object/application/use-cases/read-map.ts:57:10]\n 56 |   return Promise.all(\n 57 |     list.map(async (file) => {\n    :          ^|^\n    :           `-- This map call spreads an object\n 58 |       const base = file.name.replace(/.md$/, \"\");\n 59 |       const state = await files.read(join(objectPath, STATE, `${base}.state.json`));\n 60 |       if (!state) return { ...file, status: \"new\" as const };\n 61 | \n 62 |       const text = await files.read(file.path);\n 63 |       try {\n 64 |         const saved = JSON.parse(state) as {\n 65 |           directive?: string;\n 66 |           run?: { stage: string; startedAt: string; finishedAt?: string };\n 67 |         };\n 68 |         const run = saved.run;\n 69 |         // Этап, который не помечает выполнение, копии не снимает: прогон был, а директива\n 70 |         // по-прежнему не сделана — решение 0017. Без копии сравнивать не с чем.\n 71 |         if (saved.directive === undefined) return { ...file, status: \"new\" as const, run };\n 72 |         const same = norm(saved.directive) === norm(text);\n 73 |         return { ...file, status: same ? (\"done\" as const) : (\"changed\" as const), run };\n    :                  ^^^|^^^\n    :                     `-- This spread allocates a new value on each iteration\n 74 |       } catch {\n 75 |         return { ...file, status: \"new\" as const };\n    :                ^^^^^^^^^^^^^^^^^|^^^^^^^^^^^^^^^^^\n    :                                 `-- Map returns the spread here\n 76 |       }\n    `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\n  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n    ,-[packages/abstract-server/src/features/map-object/application/use-cases/read-map.ts:57:10]\n 56 |   return Promise.all(\n 57 |     list.map(async (file) => {\n    :          ^|^\n    :           `-- This map call spreads an object\n 58 |       const base = file.name.replace(/.md$/, \"\");\n 59 |       const state = await files.read(join(objectPath, STATE, `${base}.state.json`));\n 60 |       if (!state) return { ...file, status: \"new\" as const };\n 61 | \n 62 |       const text = await files.read(file.path);\n 63 |       try {\n 64 |         const saved = JSON.parse(state) as {\n 65 |           directive?: string;\n 66 |           run?: { stage: string; startedAt: string; finishedAt?: string };\n 67 |         };\n 68 |         const run = saved.run;\n 69 |         // Этап, который не помечает выполнение, копии не снимает: прогон был, а директива\n 70 |         // по-прежнему не сделана — решение 0017. Без копии сравнивать не с чем.\n 71 |         if (saved.directive === undefined) return { ...file, status: \"new\" as const, run };\n 72 |         const same = norm(saved.directive) === norm(text);\n 73 |         return { ...file, status: same ? (\"done\" as const) : (\"changed\" as const), run };\n 74 |       } catch {\n 75 |         return { ...file, status: \"new\" as const };\n    :                  ^^^|^^^\n    :                     `-- This spread allocates a new value on each iteration\n 76 |       }\n    `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\n  ! eslint(no-await-in-loop): Unexpected `await` inside a loop.\n     ,-[packages/abstract-server/src/features/map-object/application/use-cases/read-map.ts:208:49]\n 207 | ): Promise<void> {\n 208 |   for (const child of object.children as Raw[]) await inheritMetrics(files, child, mapPath, seen);\n     :                                                 ^^^^^\n 209 | \n     `----\n  help: Collect all promises into an array and use `Promise.all()` to run them in parallel, rather than awaiting each one sequentially inside the loop.\n\n  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n     ,-[packages/abstract-server/src/features/map-object/application/use-cases/read-map.ts:253:8]\n 252 |       .filter((file) => !mine.has(file.name))\n 253 |       .map((file) => (file.owner ? file : { ...file, owner: from })),\n     :        ^|^                                  ^^^|^^^\n     :         |                                      `-- This spread allocates a new value on each iteration\n     :         `-- This map call spreads an object\n 254 |     ...own,\n     `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\n  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient\n     ,-[packages/abstract-server/src/features/map-object/application/use-cases/read-map.ts:264:8]\n 263 |       .filter((stage) => !mine.has(stage.name))\n 264 |       .map((stage) => (stage.owner ? stage : { ...stage, owner: from })),\n     :        ^|^                                     ^^^^|^^^\n     :         |                                          `-- This spread allocates a new value on each iteration\n     :         `-- This map call spreads an object\n 265 |     ...own,\n     `----\n  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading\n  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.\n\nFound 12 warnings and 0 errors.\nFinished in 13ms on 37 files with 133 rules using 16 threads."
  }
}
