{
  "runs": {
    "Проверка": 1,
    "Реализация": 1,
    "Тестирование": 1
  },
  "run": {
    "stage": "Тестирование",
    "startedAt": "2026-09-21T00:16:38.269Z",
    "finishedAt": "2026-09-21T00:20:12.446Z"
  },
  "directive": "\n## \n\nСейчас если метрики большие то они начинают не влезать.\n\nДавай сделаем в доку правку что бы по умолчанию проставляло метрикам min-max что бы они начинали скролиться если вылезают на пределы\n\nНу и нужно дорабоать интерфейс и везде этот minmax проставить в grid-template-rows\n\n## Проверка\n\n- `minmax` в рядах сам по себе скролла не даст: у тела метрики нет `overflow` —\n  `packages/abstract-react-client/src/features/map-object/ui/metric-cell.tsx:73`\n  (`min-h-0 min-w-0 flex-1`). `minmax(0, …)` только разрешает клетке быть ниже содержимого;\n  без `overflow` содержимое вылезет наружу ровно как сейчас. Во всём клиенте `overflow-auto`\n  стоит в двух местах — `features/maps/ui/accordion.tsx:44` и\n  `features/map-object/_meta/ui/meta-view.tsx:62`. Пока правки такой не будет, задача\n  директивы не решается ни дефолтом, ни ручным проходом по карте.\n- У колонок `minmax` уже стоит по умолчанию — `_metrics/pure-model/grid.ts:29`,\n  `repeat(width, minmax(0, 1fr))`. Рядов код не задаёт вовсе: `gridTemplateRows` приходит\n  только из `style` карты (`_metrics/compose/metric-grid.tsx:47`).\n- «Везде» — это 7 файлов, все в `details-metrics-layout.style`: `ru/map/_index.json:35`,\n  `ru/map/prototypes/system/_index.json:23`, `ru/map/prototypes/package/_index.json:68`,\n  `ru/map/packages/abstract-react-client/_index.json:65`,\n  `ru/map/packages/abstract-server/_index.json:61`,\n  `ru/map/apps/vscode-application/_index.json:48`, `ru/map/apps/cli-application/_index.json:52`.\n  `preview-metrics-layout` со `style` в карте нет, словарей по ширине контейнера нет ни одного.\n- Ручной проход закрывает не всю карту: у `relations/*`, `shared-metrics/*`, групп `apps`,\n  `packages`, `prototypes` и прототипа «Базовый» (`ru/map/prototypes/base/_index.json`)\n  раскладки нет вовсе — проставлять `gridTemplateRows` там не во что, их закрывает только\n  дефолт в коде.\n- Дефолт против `0003`: `ru/docs/decisions/0003-index-format.md:20` — «style прокидывается\n  в контейнер как есть — своего языка раскладки не заводим». Дефолт, который `style` карты\n  перекрывает (неявные ряды), решению не противоречит; дефолт, дописывающий `minmax` внутрь\n  заданного картой `gridTemplateRows`, — противоречит.\n- «Дока» — `packages/docs/ru/index-format.md:26`, раздел «Раскладки»; она же приходит\n  `read_docs { section: \"index-format\" }` (`ru/requirements/архитектура/документация-едет-с-инструментом.md`).\n  Но умолчание формата описано и в `0003`, а решения здесь не переписывают: `0031` в этой же\n  карте помечает переопределённое цитатой и заводит новое\n  (`ru/map/_directives/2026-09-21-0031-исправить-багу-отображения-метрик.md:135`, номер `0025`:\n  последнее — `0024`, `0021` пропущен и не переиспользуется).\n- `ru/requirements/ui/метрики/сетка-метрик.md` — `status: implemented`, про скролл и про\n  умолчания рядов в нём ничего. Правка вида метрики потребует либо дополнить это требование,\n  либо завести соседнее.\n- Идея уже записана: `ru/docs/ideas.md:12` «Добавить возможность скролла метрик» — после\n  реализации её вычёркивают. Соседняя `ideas.md:13` (секции метрик) в директиву не входит.\n- В рабочем дереве висят незакоммиченные правки `0031` по тем же файлам\n  (`features/map-object/compose/map-object.tsx`, `ui/icons.tsx`, удалённый `ui/menu-button.tsx`).\n  По разделу «Вокруг прогона» их коммитят до Реализации, иначе результат этой директивы\n  диффом не отделить.\n- Не сказано, какое значение подставлять: `minmax(0, 1fr)` уравняет ряды и съест нынешнее\n  `auto` у шапок. Заголовок директивы пустой (`## `).\n\n## Уточни\n\n> [AI] Дефолт кодом или ручная правка семи файлов? Это взаимоисключающие пути: если код\n> ставит ряды по умолчанию, проход по карте не нужен, а карта, где `gridTemplateRows` задан,\n> дефолт всё равно перекроет — семь правок ничего не изменят. Предлагаю дефолт в коде\n> (`gridAutoRows` рядом с `columns` в `grid.ts:29`), карту не трогать: тогда закрываются и\n> объекты без раскладки, и `0003` остаётся в силе.\n\nНе, проставь руками в зависимости от смысла метрики\n\n> [AI] Что значит `minmax` для рядов? Предлагаю менять только нижнюю границу: `auto` →\n> `minmax(0, auto)`, `1fr` → `minmax(0, 1fr)`. Вид остаётся прежним, но ряд получает право\n> быть ниже содержимого — без этого `overflow` внутри клетки не сработает. `minmax(0, 1fr)`\n> на все ряды сделает их равными и сломает нынешние шапки.\n\nУ карты мин должне быть например. У делервье файловых наоборот max например 300 пикселей\n\n> [AI] Скролл добавляем в `metric-cell.tsx:73` (`overflow-auto` на теле метрики)? И заводим\n> ли `0025` про умолчания сетки — или считаем это уточнением `0003` и правим только\n> `packages/docs/ru/index-format.md`? Предлагаю: `overflow-auto` добавить, решение завести —\n> умолчание рядов это новый договор кода с картой, а не редакционная правка.\n\nдавй сделаем мелкое решение + явно отменим что если в раскладке метрики нет оно и не показывается\n\n## Реализация\n\n- `metric-cell.tsx`: тело метрики получило `overflow-auto` — содержимое прокручивается внутри\n  клетки, а не выталкивает соседей.\n- `grid.ts`: `placed` теперь только названные в `areas` метрики; `metric-grid.tsx` отбирает по\n  нему метрики **до** подписки — неназванная не показывается и не собирается. Тест на это\n  переписан.\n- Ряды проставлены руками в семи раскладках: `minmax(240px, 1fr)` карте детей,\n  `minmax(0, 300px)` деревьям (файлы, требования, решения, доки, прототипы, общие метрики),\n  `minmax(0, auto)` остальному — ссылкам, статусам, версии, спискам расхождений.\n- `abstract-server`: в раскладку добавлен ряд `decisions` — метрика у объекта есть, в `areas`\n  её не было, и по новому правилу она бы пропала.\n- Решение `0029` «Ряд метрики отмеряет высоту, лишнее прокручивается»; в `0003` цитатой\n  помечено, что судьбу неназванной метрики в деталях оно переопределило.\n- Дока `packages/docs/ru/index-format.md`: раздел «Высота рядов» и правило про состав\n  раскладки; пересобрана `content.generated.ts`.\n- Требование `ui/метрики/сетка-метрик` дополнено: отбор по раскладке, высоты рядов, прокрутка.\n- Из `ideas.md` вычеркнуты «скролл метрик» и «метрика без раскладки не рисуется».\n- Проверки с корня: typecheck, lint, format:check, build и 170 тестов зелёные.\n\n### Решил за тебя\n\n- **«Явно отменим…»** прочитано как «метрика, которой нет в раскладке, не показывается» —\n  так же, как идея `ideas.md` «Метрика без раскладки вообще не рисуется». Это отменяет правило\n  `0003` про «покажется за пределами сетки», а не наоборот.\n- **Отбор идёт до подписки**: неназванная метрика ещё и не собирается — по образцу вкладок\n  (`0025`). Можно было только прятать, но собирать невидимое незачем.\n- **Значения**: `300px` взяты из твоего ответа и розданы всем деревьям, не только файловым —\n  дерево требований и дерево решений растут так же. Карте детей минимум `240px`: меньше граф\n  не читается.\n- **`decisions` у `abstract-server`** добавлен в раскладку, а не выброшен: у остальных пакетов\n  он есть, и выглядит это пропуском, а не решением.\n- **Нижний ряд больше не `1fr`** там, где он был у дерева файлов: с потолком `300px` тянуть\n  нечем, и низ страницы остаётся пустым. У корня остаток высоты забирает карта детей.\n- **Умолчания в коде нет** — ни `gridAutoRows`, ни дописывания `minmax` в заданный картой\n  `gridTemplateRows`: ты выбрал ручную правку, и `0003` про «`style` как есть» остаётся в силе.\n\n### Поправка после показа\n\nПервый заход не сработал, и это не подбор чисел, а не тот вид трека. `minmax(0, 300px)` — не\nпотолок, а **цель роста**: грид наливает в такой ряд свободное место, не спрашивая содержимое,\nи дерево из трёх строк раздувалось до трёхсот пикселей. Гибкий ряд карты (`1fr`) наполняется\n**последним**, остатком, — остатка не оставалось, и карта сидела на своём минимуме.\n\n`minmax(auto, 300px)` это бы не починило: предел роста у него тот же — 300px.\n\nСтало: деревьям `fit-content(300px)`, спискам расхождений `fit-content(200px)` — «сколько нужно\nсодержимому, но не больше»; ссылкам, статусам и версии `max-content`; карте детей\n`minmax(320px, 1fr)` — минимум поднят с 240px. Решение `0029` и дока переписаны под это,\nошибка с `minmax` в них названа прямо, чтобы на неё не наступили снова.\n",
  "status": "done",
  "ran": "2026-09-21T00:20:12.446Z"
}
