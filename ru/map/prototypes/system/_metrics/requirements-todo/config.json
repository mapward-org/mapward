{
  "label": "Требования для реализации",
  "refresh": "on-display",
  "collectors": [
    {
      "kind": "read-dir",
      "basePath": "${{ mapward://~#props.requirementsPath }}",
      "include": ["**/*.md"],
      "content": true
    }
  ],
  "transforms": [{ "kind": "script", "run": "node \"${{ mapward://prototypes/system }}/_metrics/requirements/status.mjs\" --own --todo" }, { "kind": "git-status" }],
  "display": { "kind": "tree", "empty": "всё сделано" }
}
