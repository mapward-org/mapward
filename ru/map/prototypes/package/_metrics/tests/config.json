{
  "label": "Тесты",
  "refresh": "manual",
  "collectors": [
    { "kind": "script", "run": "pnpm vitest run ${{ mapward://~#props.codePath }}" }
  ],
  "transforms": [
    { "kind": "prompt" }
  ],
  "collectorsCache": true,
  "transformsCache": true,
  "display": { "kind": "status" }
}
