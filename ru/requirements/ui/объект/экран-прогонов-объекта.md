---
status: implemented
object: mapward://packages/abstract-react-client
---

# Прогоны объекта видно на своём экране

Экран прогонов — третий вид объекта рядом с метриками и «об объекте»: виды переключаются
мини-вкладками под шапкой. Над списком — вкладки «все», «экшоны», «метрики», «автоматические
метрики»; хранится десять последних прогонов на экшон или метрику. Слева прогоны объекта, свежие
сверху, идущий обновляется живьём;
справа выбранный: конфиг после подстановок, данные формы, шаги с логом и выводом, «остановить».
Общего списка на карту нет. Решение `0038`.

У метрики зелёной точки нет, только красная, и она ведёт на последний прогон метрики.

## Сценарий

Метрика покраснела — клик по точке открывает её последний прогон, где у упавшего шага виден лог.
