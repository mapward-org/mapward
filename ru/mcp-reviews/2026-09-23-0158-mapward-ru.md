# Реализация «доработки-лейаута», правка «прижать кверху»

Пользовался: `run_directive { stage: "Реализация" }`, `finish_directive`.

- Причину «метрики расползаются» нашёл не через карту: сравнил `grep` по `dist/webview.mjs`
  установленного расширения (`~/.vscode/extensions/mapward.mapward-0.0.0`) и свежей сборки —
  в установленном старая сетка `h-full`. Память об этом уже была и сработала.
- Было бы удобно, если бы карта сама показывала, какая сборка обслуживает вид (время `dist`
  или хэш), — «правка не видна» тогда проверялась бы одним взглядом.
