# Брейншторм, круг 5 · 2026-09-21-0246-улучшение-промптов · mapward://

- `run_directive` и `read_index { file, tail: 5 }` — как в прошлых кругах, без затруднений.
- Автор хочет, чтобы агент сам переходил между этапами. Для этого MCP уже годится:
  `finish_directive` старого этапа и `run_directive` нового из той же сессии. Но состояние
  хранит один `run`, и по карте не видно, что этап сменил агент, а не человек.
