# Отзыв: брейншторм «нотификации по этапам», второй прогон, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 20 }` — хвоста хватило, чтобы
увидеть ответ автора на прошлую реплику; `lines` подсказал, что целиком читать незачем. Для
фазы 4 решения искал grep по `ru/docs/decisions` — через MCP реестр решений не спросить.

Что не получилось: ничего.

Как было бы удобнее: в фазе 4 нужен вопрос «какие решения говорят о теме X» — поиск по
решениям через карту был бы кстати, сейчас это grep мимо неё.
