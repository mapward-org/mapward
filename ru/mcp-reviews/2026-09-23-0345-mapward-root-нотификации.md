# Отзыв: брейншторм «нотификации по этапам», вопрос про видимость, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 14 }`. Для фазы 2 смотрел требования
к сайдбару (`ru/requirements/ui/карта`, `объект`) через `ls` и `cat`: там несколько карт
аккордеоном, значит кнопка одна на весь сайдбар.

Что не получилось: ничего.

Как было бы удобнее: требования по UI сайдбара приходится искать папками. Вопрос к карте
«требования, чей object — abstract-react-client, по теме сайдбара» одним вызовом сэкономил бы
обход.
