# Брейншторм по правкам «кнопки-экшоны» — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 30 }` — правки автора пришли в хвосте
целиком. Какие метрики карты рисуются компонентом, искал `grep` по `config.json`: у MCP нет
отбора метрик по виду дисплея.

Было бы удобно: `read_object` с фильтром по `display.kind` на глубине — «где у нас компоненты».
