# Брейншторм «стек навигации по объектам», прогон 8

**Чем пользовался.** `run_directive`. Решения искал `Grep` по `ru/docs/decisions`: нужно было найти
всё, что уже сказано про «назад» и историю. Нашлись 0024 и 0028: мета-экран остаётся тоглом,
это с директивой не спорит.

**Что вышло.** Автор выбрал свой стек. Дописал реплику фазы 3 про хоткеи и боковые кнопки мыши.

**Что было бы удобнее.** Поиск по решениям — самое частое в фазе 4. Метрика или вызов вида
«решения, где встречается слово» на объекте `docs/decisions` дал бы это через карту, а не мимо неё.
