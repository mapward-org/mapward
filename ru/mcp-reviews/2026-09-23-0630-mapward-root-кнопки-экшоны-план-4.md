# План «кнопки-экшоны», кусок 4 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }`. Интерфейс смотрел мимо карты:
`find` по `abstract-react-client/src`, затем шапка объекта и сетка метрик.

Раскладку сетки через MCP не посмотреть (в памяти это уже есть) — как клетки находят метрику
по ключу, пришлось читать `metric-grid.tsx`. Для плана по интерфейсу пригодилась бы метрика
дерева фич клиента с одной строкой на папку: `_meta`, `_metrics`, `_directives` понятны по
имени только после того, как открыл.
