# Сжатие · 2026-09-21-0246-улучшение-промптов · mapward://

- `run_directive` дал старый промпт Сжатия (три раздела, ответы дословно). Директива сама
  решила новый формат, поэтому пошёл по ней.
- Файл директивы читал с диска: `read_index { file }` отдаёт текст целиком, но для Сжатия
  нужен весь тред, и `Read` по частям в той же сессии дешевле.
- Коммит `git commit -- <путь>` по памяти карты: общий индекс не тронут.
