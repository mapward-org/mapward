# План «реакт-компонент-дисплей», кусок 4, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }` — ответа под куском 3 нет,
значит принят; `finish_directive`.

Что не получилось: политику безопасности вебвью (`script-src` только по nonce) нашёл `grep` по
`apps/vscode-extension/src`. Это ограничение решает, можно ли вообще выполнить код компонента,
а на карте его не видно нигде: ни в `props` объекта расширения, ни в метриках.

Как было бы удобнее: у объекта приложения — метрика или поле «ограничения среды» (CSP вебвью,
что едет в `.vsix`, какие `require` допустимы). Для директив, добавляющих исполняемое в
интерфейс, это первый вопрос.
