# Брейншторм «файловые-деревья», круг 2

Пользовался: `run_directive`, `read_index { file }` — ответ автора виден сразу, статус тоже.

- Хранилище вида (memento редактора) и набор `@mapward/display` для компонентов нашёл grep-ом по
  коду и `ru/docs/decisions`: у карты нет метрики «какие решения касаются этой темы», поиск
  решений по слову идёт мимо MCP.
- Файл директивы снова без перевода строки в конце — проверил `tail -c 1`, как советует память.
