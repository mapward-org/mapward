# План «стек навигации по объектам», прогон 4

**Чем пользовался.** `run_directive`, `read_index { file, tail }` — проверить ответ под прошлым куском.
Дальше `ls`/`grep` по `ru/docs/decisions` и `ru/map/_directives`: искал свободный номер решения.

**Что не получилось.** Номер 0034 файлом не существует, но его уже держит другая директива в
реализации. Это видно только `grep` по текстам директив: ни метрика решений, ни MCP не знают про
номера, которые заняты незакрытыми директивами.

**Как было бы удобнее.** Метрика «decisions» на корне могла бы показывать следующий свободный
номер с учётом номеров, упомянутых в незакрытых директивах.
