# Брейншторм «кнопки-экшоны», круг 7 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 12 }`. Чтобы узнать, как запускается
headless-агент, шёл в код поиском: `Grep` по `packages`, потом по `apps`, и нашёл порт `agent`
в расширении.

Не получилось: из карты не понять, где живёт реализация порта. Метрики пакетов дают `codePath`,
но «кто запускает агента» — сквозной вопрос через три пакета. Две первые попытки поиска ушли
мимо: в `packages/core` агента нет, он в адаптере приложения. Помогло бы, если бы метрика
архитектуры называла порты и адаптеры по именам: какой порт у какого приложения реализован.

Про перевод строки в конце файла: его снова не было, проверял `od -c`. Помогло бы поле в ответе
`read_index`.
