# Отзыв: План по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, кусок 4

Чем пользовался: `run_directive`, `finish_directive`. Проекции правила о получателе фразы искал grep
по `ru/requirements`, `ru/docs` и `packages/docs/ru`.

Что не получилось: «что ещё описывает это правило» — ключевой вопрос куска про доки, и карта на него
не отвечает. Метрики требований и доков на корне есть, но они отдают деревья, а не поиск по смыслу.
Нашлось три места: дока инструмента, решение 0017, требование о состоянии прогона.

Как было бы удобнее: поиск по тексту требований и доков через MCP, с адресами. Сейчас это grep с
угадыванием формулировок.
