# Отзыв: план «нотификации по этапам», кусок 3 и готовность, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }`. Под куском 2 ответа нет,
третий раз подряд принял это за «ок». Для куска про доки — `ls` решений и grep по двум докам
и по 0017: через MCP вопрос «какие доки и решения описывают поведение X» не задать.

Что не получилось: ничего.

Как было бы удобнее: кусок «доки» пишется каждый раз, и каждый раз руками ищется, где описано
затронутое поведение. Метрика «доки и решения, упоминающие модуль» на объекте расширения
отвечала бы на это одним `read_object`.
