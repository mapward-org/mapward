# Брейншторм «стек навигации по объектам», прогон 6

**Чем пользовался.** `run_directive` (текст этапа), остальное уже было в контексте сессии.
Файл директивы дочитал с диска: автор ответил в него между прогонами.

**Что вышло.** Автор попросил сохранять историю переходов. Дописал реплику фазы 2: нужно ли
сохранять ещё открытую вкладку метрик, мета-экран и метрику во весь экран. Хранилище видов
(`useViewState` в `abstract-react-client/src/services/state`) нашёл поиском по имени.

**Что было бы удобнее.** Ни одна метрика не отвечает на вопрос «где в клиенте лежит
состояние видов и какой у него срок жизни». Пришлось искать по имени хука. Метрика
«сервисы клиента» со ссылками на файлы сняла бы этот поиск.
