# План «лаконичные ссылки на табы», mapward://, кусок 1

## Чем пользовался

- `run_directive { stage: "План" }`, `read_index { file, tail }`.
- Код смотрел `sed` и `grep` по `packages/abstract-react-client/src/features/map-object`: для
  UI-правки метрики корня не говорят, где живёт компонент. Версию tailwind брал из
  `pnpm-workspace.yaml` (v4, значит вариант `in-data-*` есть).

## Что не получилось / было бы удобнее

- Прошлая реплика приклеилась к ответу автора: файл кончался без перевода строки. Записал это в
  память. Если бы карта сама дописывала реплику в тред (`append_reply`), такого бы не случалось.
- Карты клиентского UI по фичам в MCP нет: вопрос «где ссылки открываются табом» решается
  только grep-ом.
