# План «доработки-лейаута», кусок 2

Пользовался: `read_index { file, tail: 25 }` — проверить, ответил ли автор на кусок 1 (не
ответил), `run_directive { stage: "План" }`, `finish_directive`.

- `tail` здесь ровно то, что нужно: последний кусок и ответ под ним, 25 строк вместо 115.
- Код куска (`grid.ts`, `metric-cell.tsx`, `schema.ts` с `layoutVariants`) опять искал `grep`-ом:
  указателя из карты на клиент редактора нет.
