# План «кнопки-в-дерективе», кусок 1, третья версия, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }`. Этого хватило: правка автора
была в хвосте, код и доки для куска я уже смотрел в этой сессии.

Прогон прервали и запустили заново. Второй `run_directive` прошёл молча, повторный старт того же
этапа ничем не отличается от первого. Было бы полезно, если бы ответ говорил, что прошлый прогон
этого этапа не закрыт: тогда видно, что прерванное надо доделать, а не начинать заново.
