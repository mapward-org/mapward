# Отзыв: план «нотификации по этапам», кусок 2 (сайдбар), mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 5 }`. Мост и клиент смотрел кодом:
`packages/core/src/contracts/` (как подключаются контракты), `entry/app.tsx` клиента и
grep по `openPath`, чтобы понять, чем карта открывает файлы.

Что не получилось: ничего.

Как было бы удобнее: вопрос «чем клиент открывает файл» — это возможность хоста
(`capabilities.openFile`) плюс вызов моста `openPath`, и они лежат в разных местах. Метрика
«контракт моста» на объекте core свела бы их в один ответ.
