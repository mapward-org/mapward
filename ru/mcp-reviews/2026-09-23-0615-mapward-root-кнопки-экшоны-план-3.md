# План «кнопки-экшоны», кусок 3 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }`. Контракты моста, инструменты
MCP и команды CLI искал `grep` по `packages/core/src/contracts`, `entry/mcp.ts`, `apps/cli`.

Не хватило: у карты нет ответа «какие вызовы есть у сервера» — ни списка методов моста, ни
команд CLI. Инструменты MCP видны самому агенту, а мост и CLI только в коде. Метрика со
списком методов контракта сняла бы поиск по `.d.ts`, где совпадения дублируются.
