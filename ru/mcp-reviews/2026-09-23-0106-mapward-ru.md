# План, кусок 5 (вторая версия) · 2026-09-21-0246-улучшение-промптов · mapward://

- `run_directive` и хвост файла.
- Нашёл тонкость MCP: `finish_directive` на этапе с `marksDone` ставит отметку сразу, других
  вариантов у него нет. При треде Тестирование идёт в несколько кругов, и закрыть промежуточный
  круг, не пометив директиву выполненной, нельзя. Пригодился бы `finish_directive { done: false }`.
