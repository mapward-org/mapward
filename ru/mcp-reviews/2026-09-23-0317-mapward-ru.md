# Отзыв: План по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, кусок 2 и правка промпта Плана

Чем пользовался: `run_directive`, `read_index { stage: "План" }`, `finish_directive`.

Что получилось: `read_index { stage }` сразу дал путь и `owner` файла этапа
(`prototypes/base/_directives.workflow/план.md`). Искать, где лежит промпт Плана, не пришлось.

Что не получилось: решение, которое автор назвал «последним по этому поводу», карта не подсказывает.
Искал глобом по номерам в `ru/docs/decisions/` и проекции — grep по фразе («пятнадцати»). Нашлось
три места одного правила: файл этапа, решение 0031 и гайд `development-guide/directives.md`.

Как было бы удобнее: если бы у файла этапа в ответе `read_index { stage }` были обратные ссылки —
какие решения и доки его описывают. Правка правила процесса почти всегда трогает все три места.
