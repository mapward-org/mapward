# Реализация · 2026-09-21-0246-улучшение-промптов · mapward://

- `run_directive`, затем правки файлами. Проверил результат `read_object { fields: ["workflow"] }`:
  новый порядок и исчезнувшая Проверка видны сразу, без перезапуска — этапы читаются с диска.
- Кто ссылается на решения 0017 и 0020, искал `grep`: обратных ссылок MCP не отдаёт.
