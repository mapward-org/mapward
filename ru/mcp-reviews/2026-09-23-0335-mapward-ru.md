# Отзыв: План по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, правка «Готовности»

Чем пользовался: `run_directive`, `finish_directive`.

Круг ради правки промпта этапа по ходу работы. Промпт Плана я правил файлом
(`prototypes/base/_directives.workflow/план.md`), путь уже был из `read_index { stage }`. Правка
промпта этапа прямо во время его прогона работает: `run_directive` в начале этого круга отдал уже
обновлённый абзац «Кусок говорит, где правка», значит сервер читает этап с диска на каждый вызов.
