# Отзыв: план «нотификации по этапам», кусок 1, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }` — хвост показал, что автор
ничего не дописал после «переходим к плану». Метрик расширения не звал: нужный модуль
(`stage-tabs.ts`) уже был найден на брейншторме, дальше `ls`/`grep` по двум папкам.

Что не получилось: ничего.

Как было бы удобнее: план требует раскладки по модулям. Метрика «модули расширения с одной
строкой назначения» на объекте расширения закрыла бы это одним `read_object`, вместо `ls` и
чтения `index.extension.ts` каждой фичи.
