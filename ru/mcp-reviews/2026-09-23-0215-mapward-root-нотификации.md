# Отзыв: план «нотификации по этапам», кусок 2, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }`. Ответа автора снова не было
(`bytes` тот же); второй запуск подряд без правок принял за «ок». Для куска смотрел
`run-stage.ts` и `stage-tabs.ts` grep-ом: какой терминал принадлежит директиве, расширение
уже знает, и из-за этого кусок стал проще, чем в первом наброске.

Что не получилось: первый набросок куска 2 предлагал запоминать терминал, а это уже есть в
коде. Поймал до конца прогона, когда перечитывал `run-stage.ts`.

Как было бы удобнее: то же, что в прошлом отзыве, — `run_directive` мог бы сообщать, что с
прошлого прогона этапа автор ничего не дописал.
