# Отзыв: этап «Тестирование» (второй круг, коммит), директива «сохранять состояние карты при сворачивании», mapward://

**Чем пользовался.** `run_directive`, `read_index { file, tail: 14 }` — ответ автора «Комить» был
последней строкой, хвоста хватило.

**Что не получилось.** Ничего. `MEMORY.md` несёт чужую незакоммиченную строку, поэтому память и
отзывы в коммит директивы не взял.

**Как было бы удобнее.** Всё было под рукой.
