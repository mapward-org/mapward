# План «кнопки-экшоны», кусок 1 — mapward://

Чем пользовался: `run_directive`. Код для куска искал `Grep` по `packages/*/src` и читал
`core/src/model` и `read-map.ts` — по карте не видно, где собирается объект, метрика
архитектуры это не называет.

Номер свободного решения (0038) пришлось выводить: `0037` уже упомянут в коде незакоммиченной
директивы про компонент-дисплей, файла ещё нет. Как и записано в памяти — только grep.

Было бы удобно: метрика «где что реализовано» по понятиям модели — экшон, метрика, этап —
со ссылкой на файл сборки и на файл схемы.
