# План по правкам «кнопки-экшоны», кусок 4 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 5 }`. Какие файлы лежат у демо-метрик
(компонент, сбор, схема), смотрел `ls`: `read_object` отдаёт конфиг метрики, но не соседние файлы
её папки.
