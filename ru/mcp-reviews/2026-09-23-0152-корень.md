# Брейншторм: 2026-09-23-0008-давай-добавим-нотификцию-по-этапам.md (mapward://), круг 2

**Чем пользовался.** `run_directive`, `read_index { file, tail: 15 }`: ответ автора пришёл
одним хвостом, целиком файл читать не пришлось. `tail` здесь очень кстати.

**Что не получилось.** Ничего не сломалось.

**Как было бы удобнее.** Хорошо бы, чтобы `run_directive` сам возвращал хвост директивы после
последней реплики `> [AI]`. На брейншторме это первое, что читается после промпта, и уходит
отдельный вызов, где число строк приходится угадывать.
