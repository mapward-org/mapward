# Отзыв: повторный брейншторм «нотификации по этапам» после правок автора, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 20 }` — правки автора лежали
разделом «### Правки» под моим вопросом «коммитить?», хвоста хватило.

Что не получилось: ничего.

Как было бы удобнее: директива вернулась к брейншторму из тестирования, а в `run_directive`
этого не видно — только по тексту. Поле «прошлый этап был Тестирование» в ответе сразу
сказало бы, что это новый круг поверх сделанной реализации, а не продолжение старого треда.
