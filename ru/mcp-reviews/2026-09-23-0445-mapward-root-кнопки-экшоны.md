# Брейншторм «кнопки-экшоны», круг 9 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 10 }`. Больше ничего не понадобилось.

Не хватило того же: не видно, кончается ли файл переводом строки. Его снова не было, проверял
`od -c`.
