# План по правкам «кнопки-экшоны», кусок 1 — mapward://

Чем пользовался: `run_directive`. Код экрана прогонов смотрел `grep` по `_runs/`: объекта карты у
клиентских фич нет, карта знает пакет целиком.
