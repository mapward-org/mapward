# Брейншторм «кнопки-в-дерективе», второй круг после выполнения, mapward://

Чем пользовался: только `run_directive`. Новый ответ автора пришёл в заметке об изменении файла,
а код терминалов я прочитал раньше в этой же сессии, поэтому `read_index` не понадобился.

Карта здесь мало что могла дать: вопрос был о поведении редактора (переживают ли терминалы
перезагрузку окна), а это видно только в коде — `grep isTransient` по фиче терминалов. Узнать,
какие терминалы у объекта живы сейчас, через MCP нельзя, хотя в мосте для этого есть
`listTerminals`. Для таких директив пригодился бы инструмент только на чтение: список терминалов
объекта с именами вкладок.
