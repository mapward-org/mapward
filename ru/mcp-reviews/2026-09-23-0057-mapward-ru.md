# Брейншторм «доработки-лейаута», четвёртая реплика

Пользовался: `read_docs { section: "mcp" }` и `{ section: "index-format" }`, `run_directive`,
`read_index { file }`, `finish_directive`.

- Хватило текста директивы и раздела про раскладки: треду нужна была одна развилка (как
  переопределять раскладку для сайдбара), и её подсказала сама дока — там описан словарь
  с css-условиями, который в прошлой реплике нашли неработающим.
- Обзорный `read_object` не звал: на этой реплике карта объекта ничего не добавляла, а статус
  директивы пришёл в `read_index { file }`.
- Неудобно: дописывая реплику, я правлю файл с диска — `read_index` отдаёт текст, но Edit
  требует прочитать файл `Read`-ом. Лишнее чтение того же текста второй раз.
- Имя отзыва: в папке уже лежат файлы с минутами позже текущего времени оболочки (часовой
  пояс?) — свободную минуту подбирал по списку.
