# План «кнопки-экшоны», ответ на вопрос к куску 4 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 10 }` — вопрос автора пришёл в хвосте
целиком. Для ответа сверил, как метрика пакета ссылается на `shared-metrics`, — `grep` по
`config.json` на диске. Через MCP то же видно полем `metrics.layers`, но ради одной строки
`extends` это дороже.

Замечание: хост прислал мне дифф директивы целиком, 96 строк обрезано; `tail` из карты
оказался точнее — ровно последний кусок и вопрос.
