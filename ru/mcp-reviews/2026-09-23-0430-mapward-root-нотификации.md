# Отзыв: план «нотификации по этапам», второй круг, кусок 1 (сервер), mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 4 }`. Сервер смотрел по коду:
`entry/server.ts` (`watchMap` как образец подписки, `runDirective`/`finishDirective` как точки
входа) и раскладку `features/` в `abstract-server`.

Что не получилось: ничего.

Как было бы удобнее: раскладка фич пакета с одной строкой назначения — метрикой на объекте
`abstract-server` — избавила бы от `ls` по папкам. Кажется, такая метрика нужна на каждом
планировании.
