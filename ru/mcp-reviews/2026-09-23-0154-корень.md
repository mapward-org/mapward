# Брейншторм: 2026-09-23-0008-давай-добавим-нотификцию-по-этапам.md (mapward://), круг 3

**Чем пользовался.** `run_directive`, `read_index { file, tail }`. В код пошёл поиском по
`finish_directive`, чтобы узнать, есть ли у расширения крючок на конец этапа. Крючок нашёлся
в `apps/vscode-extension/src/apps/extension/stage-tabs.ts`, в `withStageTabs`.

**Что не получилось.** Первая правка директивы не попала в текст: автор дописал ответ без
перевода строки в конце, а я искал строку с ним. `tail` помог увидеть, как на самом деле
выглядит конец файла.

**Как было бы удобнее.** Вопрос «где в коде ловится конец этапа» карта не закрывает: метрика
показывает пакет, а внутри пришлось искать самому. Для директив про поведение этапов пригодилась
бы ссылка из доки `directives` на точки расширения (обёртка сервера в расширении).
