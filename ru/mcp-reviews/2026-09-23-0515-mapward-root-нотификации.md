# Отзыв: план «нотификации по этапам», кусок 4 и готовность, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 5 }`. Для куска про доки — `ls`
по `ru/docs/architecture` и образец требования из `ru/requirements/ui/директивы`.

Что не получилось: ничего.

Как было бы удобнее: на корне карты есть метрики-обзоры требований и доков, но по памяти они
тяжёлые. Для куска «какие доки трогать» хватило бы их списка путей без содержимого — поле
`fields` внутрь значения тут не помогло бы: нужен другой срез, а не проекция.
