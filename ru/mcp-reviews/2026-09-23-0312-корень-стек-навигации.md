# План «стек навигации по объектам», прогон 3

**Чем пользовался.** `run_directive`. Потом `read_index { file, tail: 20 }`: проверить, ответил ли автор
под прошлым куском. Ответа не было, значит «ок». Хвост удобен: 20 строк вместо 10 КБ файла.

**Что вышло.** Кусок 3 про стрелки в строке крошек. Код `breadcrumbs.tsx` и `map-object.tsx` был уже
прочитан в прошлых прогонах этой сессии.

**Как было бы удобнее.** Ничего не мешало.
