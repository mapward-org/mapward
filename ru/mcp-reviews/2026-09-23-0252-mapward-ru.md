# Отзыв: Брейншторм по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, шестой круг

Чем пользовался: `run_directive`, `finish_directive`. Решения читал с диска: `ru/docs/decisions/`.

Что не получилось: у фазы 4 («противоречия с решениями») нет входа в карте. Какие решения касаются
кнопок этапов, пришлось искать по номеру из комментария в коде (`решение 0017` в `bridge-handler.ts`)
и потом глобом по `ru/`. Метрика `decisions` на корне есть, но я её не звал — не было понятно, отдаёт
ли она связь «решение → чего оно касается».

Как было бы удобнее: если бы у объекта в `read_object` или у метрики `decisions` можно было
спросить «решения, которые упоминают этот код или эту тему», например ссылками из решений на адреса
карты. Фаза 4 сводилась бы к одному вызову.
