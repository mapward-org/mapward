# Отзыв: План по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, кусок 3

Чем пользовался: `run_directive`, `read_index { file, tail: 12 }`, чтобы проверить, ответил ли
автор в файле (не ответил, «дальше» пришло в чат), `finish_directive`.

Что получилось: `tail` оказался как раз к месту: проверить конец треда на ответ стоило 1 КБ, а не
весь файл в 14 КБ.

Что не получилось: что у объекта модели есть `workflow` и `directives` с путями, я смотрел по
`model.ts` в `packages/core`. Глазами агента то же отдаёт `read_object`, но из него не видно, что это
же доступно коду расширения через `server.getMap`. Для плана по коду связь «поле ответа MCP → поле
модели» пригодилась бы в доке `index-format` или `mcp`.
