# Отзыв: Брейншторм по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, пятый круг

Чем пользовался: `run_directive`, `finish_directive`.

Что не получилось: автор сообщил о баге — фраза от кнопки вставляется в терминал, но не отправляется.
Ни карта, ни MCP не отдают ничего о том, как расширение шлёт текст в сессию. По коду `sendText` видно
только вызов, а не то, что агент получает на входе. Баг я учёл как техническую часть директивы, а
разбирать его буду на Плане, уже по коду.

Как было бы удобнее: здоровье живых сессий объекта (последняя отправленная фраза и дошла ли она)
в `read_object` помогло бы и человеку, и агенту.
