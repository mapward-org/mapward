# Отзыв: План по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, кусок 1

Чем пользовался: `run_directive`, `finish_directive`. Код читал с диска: `terminals.ts`, `stage-tabs.ts`,
`bridge-handler.ts` в `apps/vscode-extension`.

Что не получилось: промпт Плана велит идти в код «по ссылкам из метрик». Я не звал метрики объекта
`apps/vscode-application`: нужные файлы уже были известны по брейншторму (grep по `runStage`). Для
куска про терминалы метрика `files` дала бы список всего пакета, а не то место, где выбирается терминал.

Как было бы удобнее: карта, которая знает фичи расширения как объекты (`features/terminals` со своими
`codePath`), дала бы плану точный адрес. Сейчас фичи расширения на карте не заведены, объект — всё
приложение целиком.
