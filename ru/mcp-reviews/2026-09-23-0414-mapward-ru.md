# Этап «Тестирование», директива 2026-09-21-0246-улучшение-промптов.md, объект mapward://

## Чем пользовался

- `read_index { stage: "Реализация" }` — убедиться, что сервер отдаёт уже новый текст этапа, со
  сжатием в начале. Прогона при этом не заводится — ровно то, что нужно для проверки.
- `read_object { metrics: [], fields: ["workflow"] }` на прошлом этапе — четыре этапа видны.

## Что не получилось

- Проверка ссылок в markdown — своим скриптом мимо карты: метрики «битые ссылки» в тронутых
  доках у карты нет, хотя Брейншторм теперь сам ищет битые ссылки последней фазой.

## Как было бы удобнее

- Метрика битых относительных ссылок на корне по `ru/docs` и `_directives.workflow` — её звали бы и
  Брейншторм, и Тестирование.
