# Отзыв: Брейншторм по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, седьмой круг

Чем пользовался: `run_directive`, `finish_directive`.

Круг закрывающий: автор одобрил новое решение, и я написал строку готовности к Плану. Нового к MCP
нет. По всему треду повторюсь в одном: `run_directive` на каждом из семи кругов отдавал один и тот же
текст этапа целиком — на этом этапе это главный лишний вес.
