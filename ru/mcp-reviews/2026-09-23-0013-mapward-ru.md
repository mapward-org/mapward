# Брейншторм, круг 8 · 2026-09-21-0246-улучшение-промптов · mapward://

- `run_directive` и `read_index { file, tail: 5 }`.
- Правило «после запуска не коммитим» проверял в `packages/docs/ru/directives.md` поиском, хотя
  тот же текст отдаёт `read_docs { section: "directives" }`. Но у `read_docs` нет номеров
  строк, а для ссылки `путь:строка` они нужны.
