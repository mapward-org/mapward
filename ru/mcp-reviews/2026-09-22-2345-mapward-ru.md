# Брейншторм, круг 4 · 2026-09-21-0246-улучшение-промптов · mapward://

- `run_directive` и `read_index { file, tail: 4 }`. Для треда хватает. Но ответ автора
  растёт вниз, и сколько строк брать, заранее неизвестно: хвост пришлось угадывать, а
  diff с прошлого прогона пришёл от харнесса, а не от карты.
- Удобно было бы получать «что изменилось в директиве с конца прошлого этапа»: копия
  текста в `.state.json` у сервера есть, и дифф по ней — ровно новый ответ автора.
