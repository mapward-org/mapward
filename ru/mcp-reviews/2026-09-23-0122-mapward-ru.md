# План «доработки-лейаута», кусок 1

Пользовался: `run_directive { stage: "План" }`, `finish_directive`. Карту объекта не читал:
кусок про код клиента, а метрики, которая вела бы в код вида объекта, на корне нет.

- Код находил `grep` по `packages/abstract-react-client` (`<MetricGrid`, `planGrid`). Для
  директив про редактор полезен был бы объект клиента на карте с метрикой «фичи и файлы» —
  тогда кусок плана опирался бы на ссылки из метрики, как просит промпт.
- Про то, что превью-раскладка не рисуется клиентом вовсе, MCP ничего не говорит: поле
  `preview-metrics-layout` в схеме есть, в доке описано, а в клиенте не используется.
