# Брейншторм «кнопки-экшоны», круг 12 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 10 }`. Круг последний: автор закрыл
фазу 4, остальные противоречия (MCP «только читает» при запуске экшона, статус требования,
который ставит экшон) решаются по аналогии с `run_metric` и уже принятой границей — спрашивать
не пришлось.

Перевода строки в конце файла снова не было, проверял `od -c`.
