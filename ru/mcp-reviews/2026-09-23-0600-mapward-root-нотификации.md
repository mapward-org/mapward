# Отзыв: реализация «нотификации по этапам», второй круг, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail }` — хвост и потом директива целиком
для сжатия (110 строк хвоста хватило на всё, что ниже прошлого сжатия). Дальше по коду: сервер,
контракты моста, клиент, тема вебвью.

Что не получилось: первая запись сжатой директивы упала — редактор считал файл изменённым после
коммита, хотя диффа не было; понадобилось перечитать. Проверить кнопку живьём из сессии нельзя:
MCP обслуживает установленную сборку.

Как было бы удобнее: `read_index { file }` целиком для сжатия — нормально, но хорошо бы уметь
просить «текст ниже последней строки "Сжато до коммита"», раз этап сам так формулирует границу.
