# Брейншторм «стек навигации по объектам», прогон 7

**Чем пользовался.** `run_directive` — текст этапа. Дальше файл директивы и поиск по `package.json`:
нужно было проверить, есть ли уже роутер в зависимостях. Его нет.

**Что вышло.** Автор хочет, чтобы после перезагрузки экран восстанавливался точь-в-точь, и
предложил React Router. Дописал реплику фазы 3: адрес-строку с query берём, роутер нет, вместо
него свой стек.

**Что было бы удобнее.** Узнать внешние зависимости пакета клиента из карты: метрика «deps»
у объекта пакета ответила бы без поиска по `package.json`.
