# Реализация правки «вкладки видов» — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 25 }` — правка автора пришла в хвосте.
Шапку объекта искал `grep` по `compose/map-object.tsx`; карта здесь ничего не добавила бы: правка
в одном файле клиента.
