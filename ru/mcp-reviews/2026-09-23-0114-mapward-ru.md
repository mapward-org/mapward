# Брейншторм «доработки-лейаута», седьмая реплика

Пользовался: `run_directive`, `finish_directive`. Ответ автора пришёл изменением файла.

- Фаза 4 — сверка с решениями. Какие решения касаются раскладки, MCP не подсказывает:
  искал `grep` по `ru/docs/decisions` (`gridTemplateRows`, `max-width:`), нашлись 0029 и 0003.
  Метрика решений на корне есть, но поиска по содержимому у неё нет — для фазы 4 вопрос
  «какие решения говорят о X» самый частый.
- `cd` в команде оболочки сменил рабочую папку сессии — дальше пути лучше писать абсолютными.
