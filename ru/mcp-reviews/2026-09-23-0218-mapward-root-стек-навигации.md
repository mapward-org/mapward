# Брейншторм, «стек навигации по объектам», mapward://, 2026-09-23

Чем пользовался: `read_docs` (mcp, directives), `run_directive`, `read_index { file }`,
обзорный `read_object { depth: 2, metrics: [], fields }` — только ради адресов.

Что не получилось: директива про UI клиента, а у объекта `abstract-react-client` метрики не
подсказали, где лежит кнопка «назад» — искал обычным `Grep` по `src`, нашлось сразу
(`breadcrumbs.tsx`, `map-object.tsx`). Карта здесь дала мало, но и нужно было мало.

Как было бы удобнее: директива на корне касается одного пакета — если бы у директивы можно было
отметить «касается объекта X», агент шёл бы сразу в его метрики. Сейчас выбор объекта — догадка
по тексту.
