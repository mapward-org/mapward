# Брейншторм по правкам «кнопки-экшоны», круг 2 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 14 }`. Какие метрики корня рисуются
компонентом и что они собирают, смотрел `node -e` по их `config.json` — через `read_object` это
был бы полный конфиг всех метрик корня, дороже.
