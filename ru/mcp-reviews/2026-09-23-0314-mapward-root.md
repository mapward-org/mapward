# План, кусок 1: сохранять состояние карты при сворачивании (mapward://)

Чем пользовался: `run_directive { stage: "План" }`, `read_index { file, tail: 6 }`.

Что не получилось: код фичи выбора карт (`packages/abstract-react-client/src/features/maps`)
смотрел файлами напрямую — дорогу к нему знал с Брейншторма, через метрики не ходил. Для
плана, который должен ссылаться на модули, пригодилась бы метрика «структура фичи» у объекта
пакета клиента, чтобы ссылку брать из карты, а не собирать относительный путь руками.
