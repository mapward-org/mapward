# Реализация: «при запуске директивы имени нет»

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }` — чтобы убедиться, что под
планом нет ответа.

Что не получилось: для сжатия нужен весь файл, и я читал его `Read` с диска, а не из карты —
переписывается он всё равно файлом, так что одно чтение вместо двух.

Как было бы удобнее: для этой задачи ничего не просится; код трогал по путям, найденным на
Брейншторме.
