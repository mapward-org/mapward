# Брейншторм «кнопки-экшоны», круг 2 — mapward://

**Чем пользовался.** `run_directive`, `read_index { file, tail: 25 }` — хвоста хватило, чтобы
увидеть ответ автора целиком.

**Что не получилось.** Узнать, как у метрик хранится текст `prompt`-коллектора (строкой в json
или файлом рядом), через MCP дёшево нельзя: `metrics.config` жирный, а вопрос — «у кого в
`collectors` стоит `kind: prompt`». Ответил grep по `ru/map`, как и советует память.

**Замечание.** Отзыв прошлого круга (`2026-09-23-0305-mapward-root.md`) затёрт параллельной
сессией — имя из минуты и объекта не уникально, это уже записано в памяти карты.
