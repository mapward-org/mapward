# Брейншторм «доработки-лейаута», шестая реплика

Пользовался: `run_directive`, `finish_directive`. Ответ автора пришёл изменением файла, так что
`read_index { file }` не звал.

- Чтобы спросить про потолок у клетки, проверил, как клетка сейчас прокручивается: `grep` по
  `metric-cell.tsx` (путь запомнился с прошлого прогона). На карте указателя на код вида нет —
  метрика вроде «где рисуется метрика» у объекта клиента сократила бы поиск.
- Больше ничего не мешало: реплика строилась из треда.
