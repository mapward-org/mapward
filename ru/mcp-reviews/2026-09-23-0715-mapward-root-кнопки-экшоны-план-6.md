# План «кнопки-экшоны», кусок 6 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 6 }`. Экран «Об объекте» и клетку
метрики смотрел в коде клиента: `ls _meta`, `grep` по `map-object.tsx` и `metric-cell.tsx`.

Хвоста в шесть строк хватает, чтобы понять, есть ли ответ под куском: если последняя строка —
цитата `>`, ответа нет. Новых затруднений не было.
