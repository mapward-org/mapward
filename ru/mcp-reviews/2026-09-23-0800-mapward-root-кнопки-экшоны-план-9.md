# План «кнопки-экшоны», кусок 9 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 12 }` — вопрос автора стоял под строкой
«план собран», хвоста хватило. Контракт компонента смотрел в `packages/display/src/index.ts`:
пакет `@mapward/display` на карте объектом не заведён, в обзоре его нет — нашёл `grep`-ом по
`package.json`.

Стоило бы завести `packages/display` объектом карты: это публичный контракт для авторов
компонентов, и план, который его трогает, должен находить его обзором.

Перевода строки в конце файла не было, проверял `od -c`.
