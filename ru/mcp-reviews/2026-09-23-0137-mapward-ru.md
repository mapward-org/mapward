# План «кнопки-в-дерективе», кусок 3, вторая версия, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 12 }`. Ответ автора был приклеен к
последней строке моей реплики без пустой строки, но `tail` его показал.

Замечаний к карте на этот прогон нет: правка была только в тексте плана.
