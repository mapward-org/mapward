# План «реакт-компонент-дисплей», кусок 3, mapward://

Чем пользовался: `run_directive` первым вызовом (в прошлый раз забыл), `read_index { file, tail: 10 }`
— проверить, есть ли ответ под куском 2, `finish_directive`.

Что не получилось: как вебвью собирает tailwind и откуда берутся цвета темы, узнал из
`apps/vscode-extension/src/apps/webview/index.css` и `displays.tsx` клиента — файлами. Метрики
объекта «Расширение VS Code» на вопрос «где стили и тема» не отвечают.

Как было бы удобнее: всё то же, что в прошлых отзывах, — точки входа модулей со строкой назначения.
Отдельно: в тексте этапа теперь сказано, что запуск без ответа под куском значит «ок». Это
снимает вопрос, на котором я споткнулся два прогона назад.
