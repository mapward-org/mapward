# Отзыв: этап «Реализация», директива «сохранять состояние карты при сворачивании», mapward://

**Чем пользовался.** `run_directive`, `read_index { file, tail: 25 }` — хвоста хватило, чтобы
убедиться, что под планом ответа автора нет. Больше MCP не понадобился: файлы правки назвал план.

**Что не получилось.** Ничего. Коммит перед прогоном — `git commit -- <пути>`, как велит память.

**Как было бы удобнее.** `read_index { file }` мог бы отдавать и последний ход в треде — чья
реплика нижняя, AI или автора. Сейчас это видно только по тексту хвоста.
