# Брейншторм «доработки-лейаута», пятая реплика

Пользовался: `run_directive` (текст директивы пришёл изменением файла, отдельный `read_index`
не понадобился), `finish_directive`.

- Чтобы предложить режим «во всё окно» обычным css, понадобилось увидеть, кто прибивает сетку
  к высоте. Метрики, которая указала бы на код сетки, у корня нет — искал `grep` по
  `gridTemplateAreas`, нашёл `metric-grid.tsx` (`h-full min-h-0`). Для директив про вид
  редактора была бы полезна метрика «где рисуется объект» на объекте клиента.
- Поиск `@container` по `apps` ничего не дал: вебвью-клиент лежит в `packages/abstract-react-client`,
  а не в `apps` — по имени папок не угадать.
