# Отзыв: брейншторм «нотификации по этапам», фаза 4 и готовность, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 10 }`. Для фазы 4 сверял решение о
клиенте и сервере (`0014`) grep-ом: держит ли сервер состояние в памяти и чей порт «открыть
файл». Противоречий не нашлось.

Что не получилось: ничего.

Как было бы удобнее: инварианты решений были бы кстати отдельным списком через карту — фаза 4
по сути сверка с ними, а сейчас это grep по тексту решений наугад.
