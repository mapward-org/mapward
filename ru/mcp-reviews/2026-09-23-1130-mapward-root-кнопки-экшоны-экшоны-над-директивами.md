# Реализация правки «экшоны над директивами» — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 15 }`. Требование о порядке разделов
мета-экрана нашёл `grep` по реестру: у карты нет поиска требования по тому, что оно обещает.
