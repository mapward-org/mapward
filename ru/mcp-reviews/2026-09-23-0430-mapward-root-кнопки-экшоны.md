# Брейншторм «кнопки-экшоны», круг 8 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 12 }`. Для этого круга хватило:
вопрос о параллельных прогонах следует из треда, в код идти не пришлось.

Не хватило того же, что и раньше: не видно, кончается ли файл переводом строки. Его снова не
было, проверял `od -c`.
