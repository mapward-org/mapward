# Отзыв: Брейншторм по 2026-09-22-2315-кнопки-в-дерективе.md, mapward://, второй круг

Чем пользовался: `run_directive`, `finish_directive`. Разделы доков уже были в контексте с прошлого
круга, поэтому заново их не звал.

Что получилось: круг короткий. Ответ автора в директиве я увидел по уведомлению редактора об изменении
файла, так что `read_index { file, tail }` не понадобился.

Что неудобно:
- Вопрос этого круга — как выбирается терминал для кнопки, а это целиком внутреннее устройство
  расширения (сессии, привязанные к объектам). Карта об этом ничего не отдаёт, пришлось искать
  `pickSession` в коде. Было бы удобно, если бы у объекта в `read_object` были видны его живые
  сессии и то, какой этап в какой из них шёл: это как раз то, что агенту нужно знать про кнопки.
