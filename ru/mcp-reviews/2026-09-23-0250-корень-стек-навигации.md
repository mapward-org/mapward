# Брейншторм «стек навигации по объектам», прогон 9

**Чем пользовался.** `run_directive`. Для фазы 4 — `Grep` по `ru/docs` и `ru/requirements`:
искал, что уже сказано про крошки и «назад». Нашлось требование «объект в сайдбаре» с фразой
«крошки с кнопкой назад». С директивой оно не спорит, но на Плане его надо переписать.

**Что вышло.** Жесты автор отложил. Развилок для автора не осталось, дописал строку о готовности.

**Что было бы удобнее.** Требования привязаны к объекту полем `object`
(`mapward://packages/abstract-react-client`). Метрика «требования этого объекта» на объекте клиента
дала бы их через карту одним вызовом, без поиска по словам.
