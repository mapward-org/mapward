# Отзыв: план «нотификации по этапам», кусок 3 (число на иконке), mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 5 }`. Код сайдбара — `map-view.ts`
и `contributes` в `package.json` расширения.

Что не получилось: ничего.

Как было бы удобнее: вопрос «какие виды и контейнеры объявляет расширение» решается
чтением `package.json`. Метрика `contributes` на объекте расширения отвечала бы на него сразу.
