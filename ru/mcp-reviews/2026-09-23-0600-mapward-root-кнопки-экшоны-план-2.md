# План «кнопки-экшоны», кусок 2 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }` — ответа под куском 1 нет,
значит «ок».

Код смотрел мимо карты: `abstract-server/src` — порты, `collect.ts`, `turn-store.ts`. Карта
знает пакет `abstract-server`, но не его фичи: какой модуль держит прогоны, хранилище метрик,
список «ждут ответа». Метрика по фичам пакета с одной строкой о каждой сэкономила бы три
`ls`.
