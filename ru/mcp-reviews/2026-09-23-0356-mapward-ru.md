# Отзыв: План по «улучшение-промптов», кусок 2

Директива: `2026-09-21-0246-улучшение-промптов.md`, объект `mapward://`, этап «План».

## Чем пользовался

- `read_index { file, tail: 12 }` — ответ автора под первым куском, без перечитывания всей
  директивы;
- `run_directive` / `finish_directive` — прогон второго куска.

## Что не получилось или было неудобно

- Ссылки на этап «Сжатие» в решениях, доке и памяти опять пришлось искать `grep`: у карты нет
  обратных ссылок по имени этапа.
- Промпт этапа велит писать «давай переходить к сжатию», а план этот этап убирает. Так вышло
  потому, что директива правит сам воркфлоу. Написал «к реализации» по смыслу плана.

## Как было бы удобнее

- То же, что в прошлом отзыве: одним вызовом узнавать, где упоминается этап или файл карты.
