# План «доработки-лейаута», кусок 3

Пользовался: `read_index { file, tail: 25 }` (ответа под куском 2 нет), `run_directive`,
`finish_directive`.

- Какие раскладки на карте пишут `gridTemplateRows`, считал `grep` по `ru/map/**/_index.json` —
  через MCP раскладку не спросить (`groups` обрезан, об этом уже есть память). Нашлось 12
  раскладок в 8 файлах; это пойдёт в следующий кусок.
- Имя файла с суффиксом: минута 0131 уже занята чужим отзывом.
