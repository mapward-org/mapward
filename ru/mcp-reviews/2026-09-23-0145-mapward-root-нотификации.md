# Отзыв: план «нотификации по этапам», кусок 1 во второй версии, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 16 }` — сверить дописанное после
правки. Ответ автора пришёл уведомлением редактора об изменении файла, в карту за ним ходить не
пришлось.

Что не получилось: ничего.

Как было бы удобнее: кусок переписывается по ответу «нет, давай так», и старая версия остаётся
в треде выше. Карта могла бы помечать реплику `> [AI]` как заменённую, чтобы на реализации
не читать отвергнутый вариант как действующий.
