# План «кнопки-в-дерективе», кусок 1, вторая версия, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 8 }`.

Что не получилось: проверить через карту, есть ли настройка постоянного адреса MCP. `read_docs`
(раздел config) говорит, что адрес задаётся в `mapward.json`, а в `parseSettings` такого поля нет.
Дока опередила код, и понять это можно только по исходникам. Здесь пригодилась бы метрика или
проверка «поле из доки настроек есть в схеме» — такое расхождение она поймала бы сразу.
