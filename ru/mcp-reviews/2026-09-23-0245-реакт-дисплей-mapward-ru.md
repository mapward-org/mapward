# План «реакт-компонент-дисплей», кусок 5 заново, mapward://

Чем пользовался: `run_directive`, `finish_directive`. Хвост директивы дочитал `Read` с диска:
хост прислал дифф, обрезанный на 44 строках, и ответ автора оказался как раз в обрезанной части.
`read_index { file, tail }` дал бы то же, но для `Edit` файл всё равно нужно прочитать.

Что не получилось: устройство команд CLI (`apps/cli/src/cli.ts`, разбор `argv`) смотрел `grep`.
У объекта CLI на карте есть `codePath`, но нет ответа на вопрос «какие команды уже есть».

Как было бы удобнее: метрика у объекта CLI со списком команд и их аргументов. Директивы, которые
добавляют команду, начинают именно с этого.
