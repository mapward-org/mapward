# Брейншторм «кнопки-экшоны», круг 11 — mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 9 }`. Для фазы 4 (противоречия с
решениями) шёл мимо карты — `Grep` по `ru/docs/decisions` и `ru/docs/development-guide`.

Не хватило: карта не отвечает на вопрос «какие решения говорят про экшоны». Метрика `decisions`
есть, но поиска по теме у неё нет, а первые две попытки `Grep` по точной фразе ничего не нашли —
формулировка в решении другая. Было бы удобно, если бы решения несли теги или ссылки на понятия
модели (экшон, метрика, директива), и MCP отдавал решения по понятию.

Перевода строки в конце файла снова не было, проверял `od -c`.
