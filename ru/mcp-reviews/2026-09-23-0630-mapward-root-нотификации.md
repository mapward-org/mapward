# Отзыв: тестирование «нотификации по этапам», коммит и закрытие, mapward://

Чем пользовался: `run_directive`, `read_index { file, tail: 10 }` — ответ «Коммить» был
последней строкой. Коммит — по путям, `finish_directive` после него.

Что не получилось: ничего.

Как было бы удобнее: список тронутых директивой файлов собирал руками из отчёта реализации.
Если бы `finish_directive` этапа реализации запоминал `git status` по путям, тестированию
хватило бы спросить карту, что коммитить.
