# Брейншторм, круг 3: сохранять состояние карты при сворачивании (mapward://)

Чем пользовался: `run_directive`, `read_index { file, tail: 12 }` — хвоста хватило на ответ автора.

Что не получилось: вопрос «как вебвью помнит своё между перезагрузками» картой не отвечается —
нашёл `useViewState` grep-ом по `packages/abstract-react-client`. Метрики у объекта пакета
показали бы файлы, но не то, какой из них служит хранилищем вида.

Что было бы удобнее: ничего нового к прошлому кругу; автор снова ответил без перевода строки
в конце файла.
