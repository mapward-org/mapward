# План первой версии

Поведение — `ru/docs/requirements.md`, модель — решения `0002`–`0007`, устройство кода —
`Architecture.md`. Пункты идут так, чтобы каждый давал проверяемый результат, а не слой
без применения.

## 1. База расширения

- [x] манифест: `engines.vscode`, `activationEvents`, `contributes` — контейнер в activity
      bar и вью внутри него
- [x] две сборки: `extension` (cjs, node, `vscode` внешний) и `webview` (esm, браузер)
- [x] tailwind в сборке вебвью
- [x] активация: провайдер вью, html вебвью с csp и nonce
- [x] мост `postMessage`: типы сообщений в `kernel`, транспорт в `shared` — переедет
      в `adapters` фичи, когда появится первая фича
- [x] вебвью рисует заглушку и отвечает на пинг — цепочка host ↔ webview проверена
- [x] `.vscode/launch.json`, README с тем, как запускать и разрабатывать

## 2. Чтение карты

- [ ] `mapward.json`: поиск снизу вверх, `mapUrl`, `baseUrl`, дефолты
- [ ] нет конфига — кнопка создать
- [ ] обход карты: объекты, группы, метрики, директивы, экшоны
- [ ] `extends` объектов и метрик, мердж по `0003`
- [ ] адресация `mapward://` — путь, значение, `~`, `@`
- [ ] подстановки `${{ }}` после резолва extends

## 3. Объект в сайдбаре

- [ ] название в форме «прототип: объект»
- [ ] хлебные крошки, переход внутрь, кнопка назад
- [ ] кнопка «открыть объект как таб»

## 4. Метрики: сбор

- [ ] пайплайн `collectors → transforms → display`
- [ ] коллекторы `static`, `script`, `read-dir`
- [ ] кэш `collect.json`: `{ updatedAt, ok, data }`, при неуспехе данные не затираются
- [ ] логи `collect.logs.json`
- [ ] запуск: при рендере объекта, вручную, интервалом пока объект отрисован

## 5. Метрики: показ

- [ ] дисплеи `text`, `link`, `status`, `list`, `tree` — по компоненту на тип
- [ ] ошибка валидации, когда форма данных не та
- [ ] сетка по `areas`, адаптивность через container queries
- [ ] время с последнего обновления рядом с label
- [ ] кнопка обновить, лоадер, крестик прерывания
- [ ] точка статуса, по клику открываются логи
- [ ] тогглер скрыть, состояние в `workspaceState`

## 6. Переходы по ссылкам

- [ ] ссылка на файл — открыть в редакторе рядом
- [ ] ссылка на объект карты — открыть в сайдбаре или в табе через меню

## 7. Вотчеры

- [ ] карта, конфиги метрик, состояния директив следятся на файловой системе
- [ ] изменения подхватываются без перезапуска

## 8. Директивы и экшоны

- [ ] список экшонов, клик открывает файл
- [ ] список директив со статусами: новая, изменилась, выполнена
- [ ] три кнопки: валидация, тестовый запуск, запуск
- [ ] терминал claude на директиву, правая кнопка — новый или другой существующий
- [ ] терминал без директивы — разговор об объекте
- [ ] кнопка создать директиву

## 9. Уведомления

- [ ] сессия сообщает о вопросе и о завершении
- [ ] клик по уведомлению открывает нужный терминал

## 10. Карта графом

- [ ] дисплей `map` на xyflow
- [ ] коллектор `object-children-map`
- [ ] `map-state.json`: координаты и модификаторы линий

## 11. Остальной пайплайн

- [ ] коллектор `prompt`
- [ ] трансформы `script` и `prompt`, кэш `transform.json` и логи
