/**
 * Классы, по которым холст карты детей (`@xyflow/react`) узнаёт части карточки объекта. Карточка
 * и холст — разные фичи и друг друга не знают, поэтому договариваются здесь, а не строкой в
 * двух местах.
 *
 * - за шапку карточка тащится, а клики в её теле идут к метрикам;
 * - колесо над прокручиваемым телом прокручивает его, а не масштабирует холст.
 */
export const canvasClasses = { dragHandle: "mw-card-drag", noWheel: "nowheel" } as const;
