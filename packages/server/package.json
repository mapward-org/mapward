{
  "name": "@archmap/server",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Local server: watches the map, keeps agent conversations, serves the model to the UI.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@archmap/core": "*"
  }
}
